# Writing State

Updated: 2026-09-01 19:10 (Asia/Shanghai), source-task revision 1

## Current mode

- Workflow: ARS academic-pipeline; Stage 1 is complete and the Stage-2 implementation/evidence set is frozen for mandatory integrity, split-suite verification and independent review.
- Nature axes: `manuscript / algorithmic / method+experiments+discussion / zh-to-en / generic`.
- Reader deliverable: Chinese technical report plus an English evidence-gated manuscript scaffold.
- Style contract: claim-first, direct and concise; preserve cryptographic correctness, security, reproducibility and claim–evidence boundaries.

## Central argument

The paper tests whether a high-precision triangle-ring $\mathbb{Z}_{2^n}$ layer, range-safe branch extraction and prediction-preserving binary/radix-aware compilation can move LCPDTE's correctness-constrained Pareto frontier under matched CKKS security, workload and model semantics.

## Draft maturity

| Surface | State | Promotion gate |
|---|---|---|
| Chinese technical report | Reviewable rolling draft | Update on substantive evidence changes |
| English Introduction / Background / Problem Contract | Drafted from verified sources and bounded implementation facts | Refresh after the final paper title and venue are selected |
| Method / Implementation | Canonical configuration identity, combined vendor observation, RBDFT type-01/02 streaming, live-capacity v4 design, and payload-free `AuthorizeBuild` retain their bounded independent verdicts. C67–C75 retain the author-side Route-B chain through selected-child depth two. C76-SEC binds the exact C75 key/sample inventory and both secret-role sensitivity rows in an author-side conditional profile. C76-R4 freezes the same-feature interval-node model, exhaustive binary equivalence, telescoping terminal, exact binary control and serialization counters. | Complete split-suite verification and the Standards/Spec review; keep the security assumptions and public-model boundary explicit |
| Evaluation | C69 prefix, C70 first-round, C71 complete A2B, C72 root, C73 all-node depth-2, C74 small-profile selected-child and C75 Route-B selected-child author-side rows are populated. C76-SEC populates the assumption-explicit application-security row. C76-R4-CB adds 16 alternating-order measured pairs (32 processes) after its registered IQR extension trigger; all runs have zero mismatch. The direction rule passes, but circuit/lifecycle arm-median changes of 3.94%/1.25% remain below the 10% promotion gate and paired deltas are high-variance. | Preserve the operation/level result and host-specific timing direction; do not promote a stable speedup or memory gain. Move new experiment budget to common-prefix conversion, A2Sign/B-A2B and cross-query scheduling |
| Abstract / Discussion / Conclusion | Discussion partially populated with the C76-R4 negative result; abstract and conclusion remain structural | Populate the abstract and final conclusion only after broader matched systems and security gates |
| Citation and integrity audit | Stage-1 DA2 passed; Stage-2.5 material, claim, artifact and declaration audit is active | Close the audit record, artifact manifests and author-only declaration gates before finalization |

## Current evidence frontier

- Supported: LCPDTE source-level cost decomposition; the corrected epsilon-aware triangle oracle; fail-closed transform provenance; the demo-only codec's post-fix functional regression; the A2B/B2A/B-A2B plaintext conformance layer; the independently accepted current exact-precision A2A-I contract; the pinned minimal standalone-B2A graph, raw coefficient oracle and independently accepted B2A profile-v2 adaptation; the source-pinned sine/three-double-angle profile, 208-probe plaintext oracle and independently accepted bounded nine-level ciphertext functional slice; the independently accepted complete fixed-parameter A2A-e graph within its dense/no-switch functional boundary; the source-mapped A2B contract, executable special-$b0$/plaintext-oracle sub-primitives, independently accepted exp46 profile, reproducibly frozen ID/MSB profiles, independently accepted all-256 first low-block refresh-to-kernel and four-word same-chain B2A seams; the independently accepted direct rank-one sign-fusion subgraph and fixed full-serial A2B graph; the frozen signed-int8 no-overflow proof/API contract and independently accepted exhaustive Phase-A range certificate; exact generic/plaintext 64-bit tree constants and comparison policies; the ordered-float32 R0 comparator view; serialization-independent R0 certificates; fail-closed tree/forest contracts; the corrected comparison-provenance fork; the independently accepted typed R0 schedule planner; and the frozen D=8/10/12 same-feature opportunity scan.
- Newly supported in this sync: the fixed periodic scalar-selector bridge passed final independent audit and a separate integration-contract review at 0C/0M/0m, then completed frozen count-five in 589.760 s. Acceptance covers all-16 public/opaque behavior, 19 states, exact 7,996-byte online and 49,408-byte retained ledgers, a true 27-case zero-operation matrix, exact Galois inventory/identity and the scalar-leaf bridge. The exact unlimited-sample estimator conversion/transcript also passed fresh independent audit at 0C/0M/0m, including known-answer inventory, two-run determinism, 386/386 converter, 3/3 manifest and 23/23 metadata tamper rejection and 17-object immutability. Its Q/QP minima remain 150.672 classical bits and 136.740 quantum bits; the LogN=5 negative control remains 11.680/10.600 bits and `FAIL`. The exact-only `integer/secureprofile` adapter passed independent 0C/0M/0m review for exact candidate admission, 8,192-word static capacity, ScaleDown correction, MR0 target and static topology.
- New audit evidence: selector profile v5 and the four-query producer-bound functional prefix remain independently accepted at 0C/0M/0m. Fixed ingress-6 payload profile v2 now seals all `2+3+2` actual encoded factors by metadata, sorted diagonal indices and serialized plaintext payloads. Seven in-place coefficient mutations fail before HE; the 18-case graph/cache length-payload gate, expanded pre-HE mutation matrix, all-256 oracle, accepted-L20 differential and 25-state real-byte ledger pass. The replacement snapshot returned 0C/0M/0m with profile digest `3cc7441e…70d7dc`, 10,938 online bytes and 101,534 retained local-evidence bytes. The earlier apparent pass remains withdrawn. The formal record labels the matrix as 50 cases, while the current test table exposes 48 named entries; prose withholds the exact count pending reconciliation.
- New resource evidence: the secure-N16 capacity profile derives 6.4873046875 GiB STC plus 2.16015625 GiB CTS storage, a 9,814,429,440-byte resident estimate and an 18,211,426,048-byte construction-order peak bound; these are estimates/bounds, not measured RSS. The deterministic no-artifact contract uses checked arithmetic and a strict `< floor(total×4/5)` policy plus guard. After repairing an `ADMITTED` permit re-sealing flaw, three attack classes fail with zero constructor calls; final independent review returned 0C/0M/0m. The current host remains `RESOURCE_BLOCKED`. Route B now has independently accepted `LogSlots=11`/512-word capacity/profile evidence for physical Trace rotations `[2048,4096,8192,16384]`, the exact 38-element literal-derived Galois union, three fixed keys, a 7,842,848,076-byte guarded requirement and 2,585,547,267-byte margin. The repaired 25-case matrix traverses real constructors, `Evaluate`, report/permit validation and the permit-gated caller; every zero/nil/invalid-snapshot case fails closed with zero constructors. Final review returned 0C/0M/0m with stable production hash, exact key derivation and seven digests. Route A remains an unimplemented 8,192-word source-faithful target.
- New precision/construction evidence: C55/C56 retain separate `PASS SUPPORT SEAM` verdicts; C57 v3 retains `PASS DESIGN`; C58 accepts the five public-entry counter/private-core routes. C62 independently accepts the sole observed endpoint and pure `Matrix.ValidateAgainst` on `46fe170f…340d2` / `738523ab…a1d87` / `e58f8317…e7e1` / `e858351e…47b3` / `0eb100b7…6cae`: exact `0/0/0/1` routing, typed 256/256 transfer, 13/18 traces, shared-cell terminal move, honest error/panic cleanup and structural validation pass. C65 separately accepts payload-free authorization; C66 composes a small-profile private consumer/receipt/artifact at author gate, pending independent audit and canonical L11 evidence.
- New preparation evidence: pure `PrepareParameters` independently passed `PASS SUPPORT PREPARATION（0C/0M/0m）` on `e6cb5fb2…01ec4` / `0017fc56…ca40` / `0eda413d…9648`. It seals detached raw/effective DFT literals, exact float metadata, iterations and derived Mod1 values; invalid numeric configurations fail before construction. Pure and stock DFT deltas are `0/0/0/0` and `2/0/0/0`. This accepts preparation, not prebuilt matrices, artifact construction, LogN16 allocation, HE, RSS, security or MR0.
- New authorization-wire design: `RBAUTH-v1` freezes four bounded Build/Ready records, inert serialized reports, capacity-before-Prepare ordering, an owner-bound lineage cell and write-once private anchors. Design repair v2 is independently accepted at `0C/0M/0m` on `4a305efc…f6e7`, C61 closes the pure-wire codec/golden/fuzz gate, and C65 instantiates payload-free `AuthorizeBuild`. C66 exercises `BeginBuild` and a live receipt/artifact at author gate; independent audit, receipt/payload/Ready anchor acceptance and canonical L11 remain open.
- New wire-codec audit evidence: C61 has `PASS PURE WIRE（0C/0M/0m）` on `2d528eb4…d517f` / `4f52a936…074ff`. Independently encoded and fully parsed canonical type-05/type-07 RBDFT fixtures replace placeholders; exact `wire_fixture_only` scope, bounded resealed deep-body/nullable-float/length/count/allocation fuzz, focused/package/build/vet/format/diff gates and 77,327 fuzz executions pass. Live authority, builder, receipt, artifact and HE remain outside the verdict.
- New assembly-boundary evidence: C63 first review found the rehash-before-CAS race and incomplete donor postconditions. Repair `cb8a6d82…f9213` / `cb20541c…d65d2` makes owner-bound `ready→installing` CAS the first action, holds exclusivity through admission/rehash/keygen/move/assembly, fails terminally and rehashes before first HE. Re-audit returned `PASS DESIGN（0C/0M/0m）`; project implementation and runtime outcomes remain open.
- New assembler audit evidence: C64 has `PASS SUPPORT ASSEMBLY` after two independent audits each returned `0C/0M/0m` on `0365d844…92dc1` / `e436f609…967aa` / `f9075a1b…efa2d`. Stock/prebuilt share one source seam, donors are consumed before validation, all error/panic/counter paths return nil, C2S/S2C roles are exact, deltas are `2/0/0/0` / `0/0/0/0`, and count-20/package/vet gates pass. Default-precision success is structural only, with no 256/256 authority, secureeval wrapper, private artifact, key generation, installation, L11, MR0, HE, RSS, security or performance evidence.
- New RBDFT wire audit evidence: types 03–07 have `PASS PURE WIRE（0C/0M/0m）` on `cc60d292…0601` / `f5441010…64e3`. Type-01/02 first returned `REVISE（0C/1M/1m）`; repair `f6f074f4…52c4f` / `99d26d8c…e8199` closes the independent ordered five-factor ledger and post-prefix fault gaps and has `PASS STREAMING PAYLOAD（0C/0M/0m）`. Reports remain inert; fixture wall/RSS values are grammar data, not measurements. C65 accepts authorization separately, but no artifact claim follows from the RBDFT verdicts.
- New configuration-identity evidence: the first constructor audits returned `REVISE（0C/2M/4m）` on `f40b9243…55aa20` / `28a4d62d…19c6c`. Repair `9f8d0e06…f5bea7` / `4ba17878…975dff` directly proves caller/accessor/iteration/polynomial detachment, derives qDiff/effective scalings independently from raw Q0, repeats preparation and closes eight identities, a resident compatibility fingerprint, 53 mutation classes and seven narrowing cases. Two fresh final audits each returned `0C/0M/0m`; unique configuration identity is accepted.
- New observed-Trace design: repair SHA `8fed1461…650363` received `PASS DESIGN（0C/0M/0m）` after adding tagged ordinary exponents, explicit ring type, the exact `NthRoot-1` order-two event and nested-panic nil normalization. The contract freezes two observed entries over shared stock private cores, invocation-local begin/complete events, L11 source order, stock-compatible error/panic/alias/NTT behavior, defensive reports and terminal Route-B discard. The following RLWE and bootstrap verdicts independently accept the bounded implementation layer.
- New RLWE implementation verdict: repair `44d329d7…79979` / `a5ac3224…4a713` / `c5138544…02229e` closes the first `REVISE（0C/1M/2m）` findings with actual N16/L11 four-key success, strict failure-ledger validation and independent stock/observed counters. Final audit returned `PASS OBSERVED TRACE（0C/0M/0m）`. Resource figures remain acceptance-test records, not benchmarks.
- New bootstrap implementation verdict: repair `048e95ef…1e13a` / `ae1b0cda…56e9c` / `abfd4a76…75d4` adds real Standard N16/Q2/P1/H32 count-one and exact nested order; final audit returned `PASS OBSERVED MODUP（0C/0M/0m）`. Combined vendor observation is accepted within dispatch-evidence scope.
- New payload checkpoint: RBDFT type-01/02 first audit returned `REVISE（0C/1M/1m）` on `f6f074f4…52c4f` / `0b2c2dc1…b4e1`. Test repair `99d26d8c…e8199` adds independent ordered STC2+CTS3 byte expectations and proves source error, payload flush failure and physical overrun enter `WriteTo` after the prefix. Final audit returned `PASS STREAMING PAYLOAD（0C/0M/0m）`; no live Authority, aggregate/receipt mint, artifact or L11 allocation follows.
- New live-capacity design verdict: the first audit returned `REVISE（0C/2M/0m）` because dynamic margin/excess were frozen and caller snapshots lacked trusted current provenance. v4 separates four static builder sizes from per-sample saturating fields, makes all five gates sample internally on Windows/Linux, defines global one-shot generations and non-adjacent lineage, closes Ready publication with `readying`, and isolates CAS-loser pure preparation from side effects. Final audit returned `PASS DESIGN（0C/0M/0m）` on `bd7332cf…b1c4e` / `16447073…a898` with audit `eac61047…3d0a`. This authorizes implementation, not a live capability, permit, artifact, RSS or HE result.
- New live-Authority implementation verdict: dynamic RBAUTH, platform samplers and payload-free `AuthorizeBuild` pass implementation audit at `0C/0M/0m` on Authority hashes `fc90e111…c23175` / `efd38f07…f34837a` and the frozen supporting hashes. The public constructor accepts no capacity declaration; each attempt samples internally, consumes a globally newer generation, validates the exact L11 plan/report/permit, prepares only after admission, and mints an owner-bound process-local lineage over inert wire records. Dynamic arithmetic, malformed sampler inputs, replay/foreign owner, typed-nil, copied-handle concurrency, generation exhaustion, source gate order, real Windows admission and Linux compilation pass. C65 supports `AuthorizeBuild` only; its verdict does not establish `BeginBuild`, factors, receipt, artifact, install, MR0 or RSS.
- New `BeginBuild` author gate: the recorded small-profile C66 snapshot executes observed STC2+CTS3, generates and cross-validates RBDFT types 03–07, requires aggregate counter delta `0/0/0/2`, and returns an owner-bound live receipt plus opaque shared-cell artifact. Fresh capacity/preparation order, one-winner copied permits, capacity-block preservation, terminal error/panic behavior, focused count-20, complete package, vet, format and Linux compile pass. Independent audit, canonical L11, Ready/install, MR0, isolated RSS, performance and security remain open.
- New canonical repair chain: C67 preserves the one-process installed-preflight fail-close that exposed repeated whole-lifecycle charging after artifact/Ready/41-key/install reachability. C68 retains the same strict 80% policy and binds lineage-relative guarded envelopes of `7,842,848,076 / 3,504,934,656 / 1,409,810,432 / 5,571,084,288 / 1,518,862,336 B` to the five consuming gates. One frozen author-side L11 run admitted every gate; its smallest margin was `4,768,443,596 B` at Install.
- New measured R1 vertical gate: C69 freezes 59,231-B JSON `4f963b88…21b28`. The one-process wall is `34,996,007,900 ns`; cumulative peak RSS is `3,273,887,744 / 6,568,656,896 / 7,184,281,600 B` after build/install/prefix; DFT delta is `0/0/0/2`; levels are `20→17`; source-order Trace and `ctImag=nil` pass. `DecodedZeroMaxAbs=0.37500000002891437` is a finite ModUp/C2S-prefix diagnostic because EvalMod is absent. Formal status is author-side `PASS MEASURED R1 VERTICAL GATE / INDEPENDENT AUDIT PENDING`.
- New first-round A2B evidence: C70 freezes 446,637-B JSON `2728fc0b…572a0`; the bounded report records `PASS AUTHOR-SIDE FUNCTIONAL GATE — complete low-nibble Gao A2B round; independent audit pending`. One process evaluates 512 words containing two copies of all 256 8-bit values and validates 2,048 ID plus 2,048 MSB slots with zero mismatches at `5×10^-4`; maximum ID/MSB/imaginary errors are `9.146×10^-8 / 9.237×10^-8 / 8.938×10^-11`. The wall is `39,860,389,200 ns`; cumulative peak RSS is `3,202,564,096 / 6,568,886,272 / 9,819,832,320 B` after build/install/first round. Exact metadata-only normalization bridges the observed level-17 C2S scale to `0x1p+43` with log2 drift `2.305×10^-19`. This is author-side first-round evidence pending independent audit, not complete 8-bit A2B or performance.
- New complete-A2B evidence: C71 freezes 455,026-B JSON `e97f8148…785d` and full-report digest `764715f2…19d42`. The independent observed-streaming `L3→L1` STC preserves the 38-key inventory and completes the 32-state serial graph. All 2,048 low plus 2,048 high Boolean slots over `0..255` repeated twice match at `5×10^-4`; maximum low/high/imaginary errors are `1.369×10^-7 / 8.403×10^-8 / 8.550×10^-11`. Lifecycle/full-circuit wall is `54.297 / 25.830 s`, cumulative post-full-A2B peak RSS is `12,827,648,000 B`, and DFT delta is `0/0/0/3`. Hash-locked replay passes at count 20. This is author-side complete-A2B functional evidence pending independent audit, not B-A2B, private-tree, performance or security evidence.
- New root-tree evidence: C72 freezes the 267,104-B public-model signed-int8 root artifact `2410dc16…fb67c`. One CT–PT threshold subtraction, complete C71 A2B, a Q4/P6 four-diagonal direct Boolean sign broadcast, complement and public real-leaf mux evaluate threshold `0` with leaves `-2.25/3.5`. Two copies of every signed byte produce 2,048 output slots with zero mismatches at `5×10^-4`; maximum leaf/imaginary error is `4.159×10^-7 / 6.313×10^-10`. Lifecycle/root/A2B wall is `56.871 / 27.696 / 25.825 s`, cumulative post-tree peak RSS is `13,130,219,520 B`, DFT delta is `0/0/0/3`, and hash-locked replay passes at count 20. This is author-side R1 public-root/public-leaf functional evidence pending independent audit and repeated encrypted execution, not private depth-2, exact float32 parity, security or performance evidence.
- New depth-2 evidence: C73 freezes the 281,296-B public-model node-batch artifact `8b99e96e…85d8`; the current 11,911-B bounded report has file SHA-256 `6fed463d…a1f5` and records canonical report digest `54fb7d52…db1a`. It packs 170 queries as adjacent root/left/right words, runs one complete A2B over all 512 words, broadcasts the three sign bits, applies physically rescaled Q3 role masks and evaluates the registered four-leaf mux. All 680 active outputs and 1,368 inactive slots match at `5×10^-4`; maximum active/inactive/imaginary magnitudes are `4.006×10^-7 / 9.355×10^-10 / 1.091×10^-9`. Lifecycle/depth-2/A2B wall is `58.761 / 28.894 / 26.801 s`, cumulative post-circuit peak RSS is `13,206,462,464 B`, DFT delta is `0/0/0/3`, and hash-locked replay passes. This is author-side R1 public-model all-node functional evidence pending independent audit and repeated encrypted execution. It does not establish selected-child traversal, model privacy, exact float32 parity, multidepth/radix improvement, security or performance.
- New selected-child control evidence: C74 freezes the 9,047-B artifact `4dc5d51f…12b97` and record `575f1d21…33850`. One typed N32 `FUNCTIONAL-NOT-SECURE` circuit executes the source-ordered depth-2 path with zero mismatches over four words. Its timings remain outside Route-B comparisons. C74 supplies the semantic control for C75; it does not establish application security, float32 reproduction, arbitrary depth or radix improvement.
- New C75 encrypted evidence: the 296,089-B Route-B artifact `4a7fbd44…abbb2` executes 512 queries and validates all 2,048 slots. Paths `[00,01,10,11]` occur `[127,129,130,126]`; root/selected-left/selected-right equality counts are `2/2/2`; real/imaginary maxima are `5.917×10^-7 / 3.219×10^-7`; mismatch count is zero at `5×10^-4`. Setup/online/postprocess/lifecycle wall is `26.403 / 73.776 / 0.622 / 101.746 s`, selected-child/root-A2B/child-A2Sign wall is `69.671 / 30.130 / 20.914 s`, cumulative post-evaluation peak RSS is `13,889,568,768 B`, and DFT delta is `0/0/0/4`. The exact capacity envelope is `2,765,426,432 / 3,302,297,344 B`. Hash-locked replay, complete `secureeval`/`secureprofile` and relevant vet gates pass. C75 is author-side public-model signed-int8 functional evidence with `security_unverified` status, pending independent audit and repetition.
- New C76 application-security evidence: profile file `5c29142e…e8fd8` with canonical digest `49db5395…fadefb` binds the C75 artifact/report, exact N16/Q/P/H/h tuple, 38 Galois elements, 41 evaluation keys, 121 evaluation-key ring samples and 124 total fresh ring samples. Main/ephemeral exposures are 123/1. Two h32 estimator outputs are byte-identical; the selected attack-sensitivity rows retain main 150.672/136.740 bits, return ephemeral `+Infinity/+Infinity`, and retain the 11.680/10.600-bit negative control. The author-side decision is `CONDITIONAL-PASS` under explicit RLWE/KDM/circular/cross-key and semi-honest assumptions; `unconditional_128_bit_claim=false`. Profile tests, CLI replay and vet pass. Independent application-security audit remains open.
- New C76 radix evidence: the preregistered same-feature interval node uses thresholds `[-32,0,32]`, leaves `[-3.25,-0.75,2.5,5.125]` and 170 queries across 510 active words. All 192 admitted values match the exact binary control; correctness artifacts `5b9bd7ff…f7eda` / `622e43f9…1f0b` validate 680 active plus 1,368 inactive slots with zero mismatch. The telescoping terminal removes 3 CT–CT products, 3 relinearizations, 3 path rescales, 2 complements and 1 path addition and retains one level. The fixed-order discovery is preserved as `DESCRIPTIVE_ORDER_CONFOUNDED`. The preregistered alternating-order confirmation extended to 16 measured pairs (32 processes), all zero-mismatch, and freezes summary `f221ff7c…aeee`, canonical digest `9188e9ac…90a6` and manifest digest `9d220927…b8ad`. Binary/radix arm medians are `34.794/33.423 s` circuit, `67.298/66.456 s` lifecycle and `0.13499/0.01776 s` terminal. Aggregate paired binary-minus-radix medians are `0.568/0.645 s`, both order strata remain positive, and the direction rule passes; aggregate paired IQRs `1.114/1.306 s` exceed those deltas. Output `BinarySize` remains `1,048,894/2,097,486 B`. The structural ablation and workload-specific direction are supported, but the 3.94%/1.25% arm-median changes do not pass the 10% candidate-promotion gate.
- New terminal evidence: C52 remains `PASS FUNCTIONAL（0C/0M/0m）` after a standalone build found five masked references to a test-only comparator. The production-private equivalent restores buildability; exact inverse-diff, build, focused-ledger, compile-only package and vet gates pass on `e33d7d32…30c9` / `1bf2f687…20735` / `9b281be3…a8028` without changing the authenticated/numerical path. C74 separately composes the terminal in one author-side N32 functional path; independent orchestration audit, Route-B performance and security remain open.
- New child-comparator evidence: the typed fixed-L6 implementation materializes one subtraction, fixed ingress-6, direct sign fusion, negation and arithmetic-root-one composition. Independent graph/runtime and semantic/evidence reviews returned 0C/0M/0m on frozen hashes `104e41ff…9ccca` / `2b10fd31…17d2` / `cc2c3a87…9577`. The complete 91-case pre-HE mutation matrix, exact nested runtime/key/Mod1/DFT/payload checks and post-operation preflight pass; public/opaque/endpoint-equality encrypted count-five completed in 666.280 s. This duration is functional acceptance evidence, not a benchmark. The high-precision Encoder support repair seals exact `buffCmplx` outer backing plus an order-independent scratch-element identity multiset, while immutable roots remain order-sensitive with numeric digests. Same-pointer evaluation-key coefficient payload mutation remains outside the identity/topology threat model.
- Partially supported: `MultShort`/`MultFull` functional behavior passes after the oracle correction. The bounded tree components, Route-B readiness and accepted support layers retain their stated scopes. C67–C75 establish the author-side lifecycle through selected-child depth two. C75 proves the declared public-model R1 function for one batch, not private-model security, B-A2B, independent lifecycle acceptance or repeated performance. C76-SEC separately supports an author-side application-security `CONDITIONAL-PASS`, pending independent audit and without an unconditional 128-bit claim. C76-R4 now supports plaintext/encrypted equivalence, the implemented operation-count ablation and a completed descriptive repeated series; that series does not support an end-to-end speedup.
- Direct sign-fusion evidence: one sealed rank-one LT maps the LSB-first high-half MSB from $L5,S35$ to an arithmetic sign word at $L4,S35$. All 256 synthetic Boolean halves match the arithmetic oracle and generic B2A control, while a four-word actual Gao-high output composes directly. Stable independent audit returned 0C/0M/0m. The repaired fixed signed-int8 serial-A2B-high composition is now independently accepted under C43; the A2Sign cost claim still requires a true sign-only specialization or matched ablation.
- Functional audit closed: every initial Major and Minor is fixed. Stage 2 has also closed the two carried contracts: invalid Galois construction now returns an error, and the public `z2n` polynomial constructor enforces the tested per-coefficient epsilon-significance domain across all four word widths.
- Stage-1 DA2 closed: after a first final pass found 1 Major and 3 Minor drifts, the fixed-point rerun returned 0 Critical, 0 Major and 0 Minor. The 96-query search, 25-source synthesis, four R0 schedules and two claim manifests now form the bounded Stage-1 research baseline.
- Evidence package frozen: 26/26 formal artifact hashes match and the aggregate manifest SHA-256 is `b2b7d5…f3c1f3ea`. The Material Passport remains `UNVERIFIED` until mandatory integrity; it does not cover manuscript sidecars, future experiments or uncommitted implementation slices.
- Reproduction boundary: the original Z-To-C/C-To-Z pair alone is not A2A-I. Stage 2 connects the full-packed $n=8$ high-refresh graph as a guarded Lattigo adaptation. Its observed levels, scale error and post-ModUp residual are one-profile functional diagnostics. The accepted opaque circuit seals normal V/U, privately owns main+DFT execution, rejects evaluator/key-graph replacement and binds 192-bit numeric DFT payload; this still does not prove fused-Truncate or exact-q0 raw-state equivalence.
- Stage-2 schedule frontier: five typed labels separate the source-faithful OBO control from conservative CTCT, eager-active CTCT, eager-all CTPT and state-aware fused LUT schedules. Source/radix/request/structure/full-observation certificates, typed `not_measured` fields, structured `external_unverified` proof references and fail-closed metrics are implemented. The stable independent rerun returned 0 Critical / 0 Major / 0 Minor. Physical HE measurement and encrypted execution remain open.
- Stage-2 conversion frontier: pinned-source mapping separates A2A-e from A2B and fixes their shared refresh backbone and mutually exclusive nonlinear paths. The exact Gao nonlinear profile, plaintext oracle, isolated ciphertext sine/3DA path, complete fixed A2A-e graph and fixed full-serial A2B graph are independently accepted at 0/0/0 within bounded functional profiles. The A2B acceptance covers the execution-derived ledger, ordered 24-state S35 trace, all-256 source residual and same-element foreign-secret key negatives; final-halves-to-B2A, $n=32$ cutoff, raw upstream, security and performance remain open. Direct rank-one sign fusion, the signed-int8 Phase-A no-overflow range seam and the repaired fixed Phase-B CT--PT/CT--CT comparator are independently accepted at 0/0/0. The Phase-B boundary remains functional-not-secure and trusts declared-source admission; count-five, pinned release, secure-profile and performance gates remain separate. B2A profile v2 remains independently accepted.
- Stage-2 radix frontier: the preregistered analysis proves that independent radix-$r$ comparisons increase relative to binary at fixed leaf count. The D=8/10/12 scan finds only 1.45%–4.82% same-feature internal edges and no complete uniform height-2 block, demoting exact R0 fusion to a sparse special case. C76-R4 instantiates that case. Its counterbalanced confirmation passes the registered direction test, but the 3.94% circuit and 1.25% lifecycle arm-median changes remain below promotion and high-variance. The terminal graph is a supported reusable optimization; same-feature radix is not the main systems optimization for the supplied models.
- Hypothesis: private below-root model operands; independently audited C68–C76 execution; broader T0 leaf/multidepth generalization; end-to-end A2Sign cost advantage; Route-A full-packed execution; decision-tree-aware shared A2B/B-A2B scheduling; and candidate A. C75 settles one bounded public-model functional question; C76-SEC supplies a conditional author-side security profile; C76-R4 settles encrypted same-feature equivalence and rejects ≥10% terminal-only gain for the registered workload. It does not settle broader trained radix models or independent security acceptance.
- Blocked: end-to-end integer-aware encrypted PDTE, broader matched performance/Pareto results and classical 128-bit security claims; the current all-at-once secure-N16 full-slot artifact path is additionally resource-blocked on the audited host. C76-R4 itself is no longer waiting on repeated measurements.
- Novelty boundary: the 96-query corpus adds HEGIDE's discrete-CKKS private-program/private-tree system and Character Block Encodings as a direct Lattigo LUT/radix-comparator baseline. The manuscript now permits only a frozen-corpus candidate-composition gap for Gao–Zheng triangle/A2B inside LCPDTE OBO/BSGS, plus separately proved or measured implementation contributions.

## Current checkpoint

- Stage 1 status: `completed`; frozen 26-file package unchanged.
- Pipeline state: Stage-2 implementation evidence is frozen; mandatory Stage-2.5 integrity, split-suite verification and independent Standards/Spec review are the active checkpoint chain.
- Allowed automatic writing: supported background, problem contract, method, proof obligations, experiment protocol and evidence-bounded implementation text.
- Active boundary: report C42 T0, C41 selector, C50 prefix/ingress, C51 child comparator and C52 child decoder/terminal only within their exact `FUNCTIONAL-NOT-SECURE` evidence sets. Report C72 as a public-model R1 root slice, C73 as public-model all-node depth-2, C74 as the N32 source-ordered control, and C75 as one author-side Route-B selected-child functional row under trusted no-overflow admission. Report C76-R4-CB as a direction-pass/high-variance counterbalanced comparison whose structural ablation succeeds and whose ≥10% promotion hypothesis fails. Report C76-SEC separately as an assumption-explicit conditional security profile; keep general superiority and private-model claims empty.
- Multiway boundary: report the corrected 1.5× naive radix-4/binary refresh-kernel ratios as matched algorithm counts, not timings. Report exhaustive equivalence, terminal operation removal, the 86.85% counterbalanced arm-median terminal reduction, 3.94% circuit and 1.25% lifecycle arm-median changes, paired/order-stratified estimands and high variance together. Do not turn the direction pass into a stable acceleration factor or general radix dominance. Candidate A and common-prefix conversion scheduling remain live performance tests.
- Security boundary: report C59/C60/C61/C62/C63/C64/W-RBDFT within their accepted scopes. Report C67 as the pre-repair failure chain and C68–C75 as the author-side Route-B execution chain. C75's functional artifact remains `security_unverified`; C76-SEC is a separate `CONDITIONAL-PASS / independent_security_audit_pending` profile. Keep unconditional 128-bit security, KDM/circular proof, independent lifecycle/security acceptance, private-model security, B-A2B and Route A gated.

## Automatic update rule

The heartbeat may add or revise prose when new local evidence changes a claim's status. It must preserve placeholders for unsupported results and stop at ARS integrity, review-decision and finalization checkpoints for user review.
