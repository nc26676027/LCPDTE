# Progress Log — LCPDTE × CKKS Integer Evaluation

## Session: 2026-08-29

### Phase 0: Intake, inventory, and pipeline setup

- **Status:** complete
- **Started:** 2026-08-29 (Asia/Shanghai)
- Actions taken:
  - Created an active Codex goal for the complete paper-analysis, Lattigo reproduction, decision-tree integration, experiment, and innovation cycle.
  - Fully read the root instructions for `academic-research-suite`, `graphify`, `planning-with-files`, `implement`, `tdd`, and `hero-anti-overdefense`.
  - Routed the work to ARS `academic-pipeline` and read its workflow plus the current intake/orchestration and state-tracking agent contracts.
  - Ran planning session recovery; the first attempt exposed the Windows Store Python alias, and the second used the bundled Codex Python successfully.
  - Confirmed that no prior planning files, `CONTEXT.md`, or existing Graphify graph were present.
  - Created the persistent plan, findings record, and progress log.
  - Initialized Graphify with the bundled Python runtime and detected 260 supported files (248 code, 12 documents; approximately 278,296 words).
  - Extracted and merged a 3,559-node graph; clustered it into 206 communities, generated the audit report and interactive HTML, and ran the graph-health diagnostic.
  - Recorded graph-health limitations (754 dangling edges; 567 undirected endpoint collapses) and opened the HTML graph in Codex.
  - Inventoried git state, project files, Go/Lattigo versions, model/data assets, and package boundaries.
  - Downloaded/selected Go 1.23.11 through the declared toolchain and ran the baseline package compile test.
  - Recorded benchmark-host CPU/memory/OS and verified an Ubuntu WSL2 C/C++ toolchain for upstream reference builds.
  - Completed the project-owned file/entrypoint/mode/data inventory and source-checked README/runbook mismatches.
  - Reconstructed the binary CKKS tree protocol, SIMD/bit packing, comparison, parity traversal, bootstrap batching, and source-derived complexity.
  - Audited the effective Lattigo dependency, identified four required vendor patches, and selected the local functional-bootstrap seam for a clean integer-operator layer.
  - Classified `fhe-simd-alu` as a C++ OpenFHE reference oracle rather than a reusable Go library and recorded the WSL build path.
  - Persisted the ARS pipeline state at the pipeline-start confirmation boundary.
- Files created/modified:
  - `task_plan.md` (created)
  - `findings.md` (created)
  - `progress.md` (created)
  - `graphify-out/graph.json` (created)
  - `graphify-out/graph.html` (created)
  - `graphify-out/GRAPH_REPORT.md` (created)
  - `graphify-out/manifest.json` and `graphify-out/cost.json` (created)
  - `research/pipeline_state.json` (created)

### Phase 1: Primary-source research and reference reproduction

- **Status:** complete; FULL checkpoint awaiting user confirmation
- **Started:** 2026-08-29 (Asia/Shanghai)
- Actions taken:
  - Entered ARS deep-research full mode after the goal continuation signal.
  - Produced the FINER-scored Research Question Brief and mixed technical Methodology Blueprint.
  - Ran an independent Devil's Advocate Checkpoint 1; the initial `REVISE` identified five Major issues and no Critical issue.
  - Revised the RQ into a bounded Pareto-frontier question and froze the threat/security model, R0/R1 semantics, integer acceptance protocol, benchmark repetition/resource rules, and tiered completion gates.
  - Re-ran the independent checkpoint and received `PASS`; one Minor source-derived design-manifest item remains due before comparative timing, not before source investigation.
  - Verified through official conference/author sources that the CCS 2026 target protocol is CKKS-based, correcting the initial BFV/BGV premise.
  - Downloaded both primary manuscripts from IACR ePrint, recorded SHA-256 digests and byte sizes, extracted layout-preserving text, rendered the algorithm/table pages, and visually cross-checked the rendered pages against the extracted text.
  - Verified the LCPDTE Algorithms 1--5, Gao--Zheng representation/conversion algorithms, principal parameter tables, and the distinction between per-ciphertext amortization and matched machine-word/query throughput.
  - Began a pinned WSL2 build of the authoritative `fhe-simd-alu` repository as an external semantic/vector oracle; the full secure benchmark remains gated by host memory and build completion.
  - Completed an independent LCPDTE paper-to-code audit covering threat model, input transform, GEQ/OBO/GenOH/BSGS/level-major algorithms, pseudocode errata, exact tensor-multiplication versus relinearization counts, effective parameter differences, and the limits of the monolithic benchmark harness.
  - Completed an independent Gao--Zheng full-paper/source audit covering triangle and Boolean representations, exact decode/noise conditions, A2A/A2B/B-A2B/B2B/B2A, level/parameter budgets, source-level transform/LUT conventions, the overflow-unsound generic comparison wrapper, and a minimal Lattigo port sequence.
  - Pinned the authoritative `fhe-simd-alu` checkout at `08f1eb87434e7be072cba889270a8400bbffc08e`, including all four submodules, and preserved a source-clean WSL build transcript.
  - Determined the exact reference-build boundary: the checkout reaches the new Z bootstrap/operator objects, but a complete executable requires Clang, NTL/GMP development headers, Intel HEXL, and tcmalloc; those packages cannot be installed without the unavailable WSL sudo credential. No upstream source was patched and no memory-heavy benchmark was started.
  - Downloaded, hashed, page-counted, and layout-extracted thirteen related-work manuscripts in addition to the two focal papers, spanning PDTE, CKKS comparison/bootstrapping, functional bootstrapping, application-aware security, sparse-secret bootstrapping, and exact integer computation.
  - Froze the first Lattigo design and benchmark manifests, including representation metadata, range-safe comparison variants, matched physical-ciphertext/query throughput, R0 semantics-preserving radix grouping, R1 model-changing multiway trees, correctness gates, and host-resource gates.
  - Started three independent TDD implementations: pure triangle-ring algebra, high-precision repeated-block linear transforms, and radix/tree cost modeling.
  - Completed the 15-source annotated bibliography and cross-source synthesis with page-level evidence anchors; all 43 synthesis references have an explicit source anchor and all 15 corpus slugs are represented.
  - Completed the plaintext tree compiler/planner and generic model-private evaluator seam. It distinguishes R0 semantics-preserving grouping from R1 model-changing multiway trees and prevents combining OBO's `D` comparisons with a plaintext-threshold comparator cost.
  - Completed the high-precision full-slot block-diagonal homomorphic transform seam. Four-word encrypted tests cover boundary masks, fused tau/tau-inverse, special-b0, exact rotation-key enumeration, and no cross-block contamination.
  - Pinned `malb/lattice-estimator` at `53da5982597709ba0fdf94ea37a84d822310fd84`. The initial missing-Sage blocker was removed with an isolated Sage 10.9 environment and exact package lock; the later exact unlimited-sample sensitivity bundle is independently accepted, while the candidate's application-security label remains `INCONCLUSIVE` pending finite exposure evidence and a secure circuit profile.
  - Started independent Devil's Advocate Checkpoint 2 and a separate read-only mathematical/code audit of the completed integer/tree slices.
  - Checkpoint 2 returned `REVISE` with no Critical findings. Six design/accounting issues were corrected in the frozen manifests, official LCPDTE ancestry was independently verified, and a bounded four-axis counterexample search was launched for the remaining literature-reproducibility issue.
  - The mathematical/code audit returned no Critical findings and identified four Major implementation issues. The triangle canonicalizer now uses Gao--Zheng's exact epsilon-shifted boundary, and the homomorphic transform specification now owns immutable trusted state and publishes full-slot-compatible parameters; typed tree/radix hardening remains in progress.
  - Added coefficient-level regressions at `2^(-n-1)` for every registered word width and repeated the triangle-ring and encrypted evaluator suites after the canonicalization correction.
  - Extended the DA2 search from 70 to 96 dated queries by adding generic private-function/private-program/oblivious-processor and character-block families. The final bounded synthesis contains 25 sources (18 locally inspected, 7 official-web-only; 21 peer reviewed, 4 preprints), with PPFE and Faster Logical Operations retained as context-only screens.
  - Added HEGIDE as a mandatory general private-program/full-path tree baseline, Character Block Encodings as a mandatory Lattigo representation/comparator/LUT baseline, and Sparse-THI as a functional-bootstrap/noise sensitivity baseline. Broad private-program, private-tree, Lattigo-integer, comparator, LUT, and multiway priority claims are now prohibited.
  - Corrected the frozen R0 accounting into four schedules: sequential active CT--CT, eager active-supernode CT--CT, eager all-supernode CT--plaintext, and state-aware fused LUT. The older combined Go cost placeholders are explicitly quarantined from benchmark evidence until Stage 2 replaces them.
  - Split paper claims from pinned-source-derived operation counts in the claim-intent manifest, labeled adaptations and hypotheses, and schema-validated both claim manifests.
  - Completed tree hardening and an independent rerun: treeplan/treecompile/treeeval passed with 0 Critical, 0 Major, and 0 Minor findings. The separate z2n/homchain rerun passed with two Minor API/precision-contract findings carried to Stage 2.
  - Re-ran the complete repository test suite, `go vet ./integer/...`, `gofmt -d`, `git diff --check`, JSON/YAML parsing, and both ARS claim-manifest schemas; all checks passed.
  - The final independent DA2 pass first returned `REVISE` (0 Critical, 1 Major, 3 Minor). The stale three-schedule preregistration, query-map scope, historical-core count, and Kim publication count were corrected; the targeted rerun returned `PASS` with 0 Critical, 0 Major, and 0 Minor findings.
  - Created a 26-file Stage-1 SHA-256 manifest and an ARS Material Passport. All 26 hashes match; the aggregate manifest hash is `b2b7d5b6bdec140a89292b8ba581ddd45428f04cbe0ff2840c76e4edf3c1f3ea`. Passport status remains honestly `UNVERIFIED` until the later integrity stage, with `repro_lock: null`, `slr_lineage: false`, and no externally executed experiments declared.
  - Added the user-authorized final delivery step: only after all technical and review gates pass, make the GitHub repository private and push the final commit.
- Files created/modified:
  - `research/phase1_scoping/rq_brief.md` (created)
  - `research/phase1_scoping/methodology_blueprint.md` (created)
  - `research/phase1_scoping/devils_advocate_checkpoint1.md` (created)
  - `research/phase2_investigation/lcpdte_paper_code_map.md` (created)
  - `research/phase2_investigation/gao_paper_code_map.md` (created)
  - `research/phase2_investigation/source_manifest.md` (created)
  - `research/phase2_investigation/search_strategy.md` (created)
  - `research/phase2_investigation/candidate_bibliography.md` (created)
  - `research/phase2_investigation/source_verification.md` (created)
  - `research/phase2_investigation/upstream_oracle/provenance_build_source_map.md` (created)
  - `research/phase2_investigation/upstream_oracle/build_commands_and_outputs.md` (created)
  - `research/phase3_design/lattigo_integer_design.md` (created)
  - `research/phase3_design/benchmark_manifest.md` (created)
  - `research/phase3_design/security_estimator_pin.md` (created)
  - `research/phase2_investigation/annotated_bibliography.md` (created)
  - `research/phase3_analysis/synthesis_report.md` (created)
  - `research/phase3_analysis/devils_advocate_checkpoint2_final.md` (created)
  - `research/stage1_artifact_hashes.sha256` (created)
  - `research/material_passport.yaml` (created)
  - `integer/z2n/` (created and committed as the pure algebra/oracle slice)
  - `integer/evaluator/` (created)
  - `integer/homchain/` (created)
  - `integer/treeplan/` (created)
  - `integer/treecompile/` (created)
  - `integer/treeeval/` (created)

### Phase 2: Lattigo design and implementation

- **Status:** in progress
- **Started:** 2026-08-29 (Asia/Shanghai)
- Actions taken:
  - Classified the preceding goal turn as progress: Stage-1 evidence, audits, and the 26-file hash freeze changed authoritative repository state.
  - Accepted the active-goal continuation as the Stage-1-to-Stage-2 transition signal while retaining the Stage-2 FULL checkpoint and mandatory Stage-2.5 integrity gate.
  - Reloaded ARS, Graphify, planning-with-files, implementation, TDD, and proportionality contracts for the new stage.
  - Queried the existing knowledge graph for the A2A-I/A2B/comparator/tree seams, then confirmed the current package/API inventory from source.
  - Re-ran `go test -count=1 ./integer/...`; all six integer/tree packages passed before Stage-2 edits.
  - Dispatched independent read-only source maps for the homomorphic transform/A2A-I path, the Gao upstream conversion graph, and the encrypted-tree integration path.
  - Closed the first carried audit advisory by changing `TransformSpec.GaloisElements` to return an error for invalid transform state. The public-seam test failed first on the old one-result API, then passed after the minimal production change.
  - Closed the second carried audit advisory by making `Ring.validate` check every coefficient's ring precision and operational ability to distinguish Gao--Zheng's `2^(-n-1)` canonical epsilon. A public constructor test first failed for all four word widths, then passed while retaining admissible mixed-precision inputs.
  - Completed the three independent Stage-2 source maps. They fixed the exact A2A-I/A2B/B-A2B/B2A call graphs, exposed the `-16` versus `-24` benchmark-cutoff discrepancy, separated Gao Truncate from Lattigo ScaleDown, and identified the missing encrypted comparator/selector/refresh/counting contracts in the current tree seams.
  - Selected the first production vertical slice as a full-packed `n=8` A2A-I high refresh with a nonzero integral overflow component. The red/green acceptance surface includes modular residue preservation, actual overflow removal, input immutability, raw level/scale state, and bounded `ScaleDown` error; ordinary DFT round-trip tests are explicitly insufficient.
  - Assigned the bounded A2A-I slice to an isolated worker owning only the new homchain DFT/refresh files, while preserving the existing z2n, transform, evaluator, and tree packages from concurrent overwrite.
  - Added `research/phase3_design/stage2_lattigo_port_contract.md`, freezing the raw-state promotion gates, typed metadata, Gao operator dependency DAG, range-safe comparison contract, encrypted-tree primitives, R0/R1 isolation, parameter profiles, non-goals, risks, and the Stage-2 exit gate.
  - Added the exact plaintext integral-overflow oracle used by A2A-I. Its first assertion intentionally exposed a test-oracle error (raw arithmetic encodings are not always coefficient-canonical); the corrected test compares against canonicalized arithmetic encoding and passes for eight boundary words.
  - Completed the independent ordered-float32 R0 compiler view with strict red/green provenance. It canonicalizes signed zero, rejects all non-finite patterns, deep-copies the source forest, and preserves routes and leaf bits across boundary rows plus 100,000 fixed-seed random finite comparisons.
  - Completed exact plaintext conformance oracles for Gao's paired A2B LUT, LSB-first Boolean halves, standalone B2A, strict direct B-A2B configuration, and pairwise output order. Each API explicitly states that homomorphic conversion is not yet implemented.
  - Completed and independently accepted the first real full-packed `n=8` A2A-I Lattigo execution chain as a guarded ScaleDown/ModUp adaptation. The repaired opaque `A2AIHighCircuit` owns normal V/U transforms, binds encoder/raw-DFT/parameter/key/circuit provenance, constructs a private main+DFT execution graph, and checks both source and sealed graph/key identities before the first homomorphic operation. A later root inspection reopened this gate after finding that vendored `NewMatrixFromLiteral` generates DFT roots at the parameter default 53 bits even when supplied a 192-bit encoder. The local constructor now uses the operational encoder precision for both generation and encoding, and binds a numeric-payload-only matrix witness separately from declared precision. The first precision repair was rejected because its test hashed precision metadata; the corrected 53-vs-192 payload test and exact mirror were independently accepted at 0C/0M/0m.
  - Implemented the typed R0 schedule planner, then rejected its first green result after an independent review found four Major audit gaps. Added separate source-faithful and conservative OBO controls, provenance-separated comparator charges, structured external-unverified proof artifacts, typed physical profiles, request/structure/full schedule certificates, controlled measurement attachment, and fail-closed metrics.
  - Corrected the source-faithful root accounting from pinned code evidence: one CT--plaintext root comparison, `D-1` selected CT--CT comparisons, and no width-one root blind selector. Predicate-stage dependency rounds are now distinct from unknown end-to-end HE depth and bootstrap count.
  - Re-ran the complete `integer/...` suite, vet, formatting, and whitespace checks after the schedule repair; all passed at the stable treeplan boundary. The second independent schedule audit returned 0 Critical / 0 Major / 0 Minor, accepting the planner slice; later whole-integer checks remain separate from concurrent homchain work.
  - Drafted the fail-closed public integer metadata contract required before heterogeneous operator composition. It separates integer semantics, coefficient/root-slot domain and half role, packing, exact CKKS state, range evidence, fidelity, security maturity, diagnostics, and logical versus optional physical observations; caller declarations cannot promote verified states.
  - Preregistered the radix/integer-node break-even analysis. For fixed leaves, naive radix-`r` comparison count is `ln(L)(r-1)/ln(r)` and increases beyond binary; candidate gains are therefore restricted to shared A2B/bit decomposition, exact batching, unary-to-digit fusion, or saved refresh/selection rounds, with R0 same-feature fusion and R1 retraining evaluated separately.
  - Completed and independently accepted the standalone full-packed `n=8` B2A production slice as an explicit Gao-to-Lattigo adaptation: internally fixed fused-`t^-1` U transforms, one addition, one rescale, exact state/key/profile provenance, 256-word raw-coefficient conformance, and source-distinguishing negative controls. Its first audit found two Major admission gaps; profile v2 binds encoder parameters/precision and the exact non-modular default input scale, and the stable rerun closed all findings at 0C/0M/0m.
  - Added and independently accepted the same-chain full-packed B2A adapter for the fixed A2B functional parameters. At Boolean ingress level 5 it compiles the fused-`t^-1` U pair internally, uses the exact `q_5` matrix scale, and returns arithmetic output at level 4 with the unchanged exact default scale. Its first audit returned 0C/3M/1m and superseded profile `f6794435...0ad1284`. The repair requires exact equality with `GaoA2BKernelFunctionalParameters`, rejects reordered and alternate same-bit Q chains, binds numeric transform source digest `6f812fdd...cf6b6d99` and compiled-plan digest `1bcf11a7...4a129227`, and rechecks the bound keyset plus key identities before the first operation. The accepted chain-only `b2a-full-chain-v1` profile digest is `01d138a6...c168588a3`; five repetitions, post-bind deletion/swap/foreign-secret negatives, the real kernel-to-B2A composition, and the unchanged standalone `b2a-full-v2` 256-residue regression pass. The stable re-audit returned 0C/0M/0m.
  - Completed the independent A2A-e/A2B source map and froze it as `a2ae_a2b_port_contract.md`. It corrects the false dependency that A2B calls A2A-e, records the mutually exclusive special-b0 versus fused-`t` transform families, separates A2A-e's 16-level logical budget from its 13-level post-ModRaise physical depth, pins the exact sine/3DA and key contracts, and specifies source-distinguishing differential gates.
  - Extracted and froze Gao's exact A2A-e sine kernel: all 33 signed Chebyshev numerators over `2^128`, the one-time OpenFHE-to-Lattigo `c0/2` convention conversion, the normalized `x=u/16` domain, and three exact double-angle recurrences. The initial 5+3 Lattigo prediction was falsified by the real encrypted path: vendored simulation says five, but the public evaluator and three ciphertext traces consume six polynomial levels. The fixed slice now uses ten Q primes and records `[9,3,2,1,0]` for the measured 6+3 path without dropping levels or retagging scale.
  - Added a reproducible raw-model scan for the exact R0 same-feature fusion prerequisite. Across D=8/10/12, only 1.45--4.82% of internal edges keep the same feature, every component has two nodes, and no complete uniform height-2 block exists; the evidence demotes this optimization to a sparse special case and keeps R1 batching/refresh hypotheses as the main integer-node branch.
  - Completed the independent full-packed A2B source map and froze `a2b_lattigo_port_contract.md`: special-b0 ingress, two per-half refreshes, shared-power degree-15 ID/MSB LUT, residual peeling, exact low/high LSB order, 20-level provisional trace, minimal keys, and source-distinguishing negatives. It also proves that cutoff `-16/-24` needs an `n=32,w=4` differential because the first `n=8,w=4` slice cannot distinguish them.
  - Replayed and froze A2B's runtime-generated `p=16`, order-1 ID/MSB Hermite coefficients. A dependency-free C++ reproducer matches the pinned `std::complex<double>` DFT, `2^-32` cutoff, `c0/2`, and `BigFixedPoint::fromDouble` path; `-O0/-O2` agree byte-for-byte. The 32-row exact `2^-128` table has canonical digest `f3aa5c55...1564e3`, its Go profile imports 256-bit dyadics without runtime regeneration, and all 16 ID/MSB points pass below `1e-14` in five repetitions.
  - Extracted, imported, and independently accepted the separate degree-46 complex Chebyshev coefficient artifact and isolated normalized-variable oracle used before the shared A2B LUT. The 47-row `2^-128` artifact is frozen at digest `a960edc1...e36e4d`, applies `c0/2` exactly once, and passes 345 probes with maximum base/final errors `1.365e-9`/`5.459e-9`. The first live refresh trace corrected the standalone-kernel input interpretation: CTS Scaling=`1/16` already emits `y=(I+m/16)/16`; no second `/16` is permitted. A zero-lift code `m` therefore enters as `m/256`, and the encrypted composition gate is integer-lift periodicity plus `16*y-m/16 ~= Z`, not a deterministic `m/16` decode.
  - Implemented the initial isolated encrypted A2B exp/shared-power two-LUT kernel on the fixed chain. It consumes CTS-normalized `y=(J+p/16)/16` at level 17, uses one packed-vector degree-46 evaluation, two MulRelin/rescale squares, and exactly one `EvaluateMultiPoly` call for ID/MSB. The initial natural-scale trace put both outputs at L5 scale `0x1.fffffc9f400865b0222e011350815d12p+34`; that historical state later exposed and motivated the v3 kernel-to-B2A target-scale repair recorded below. The code/lift, wrong-`p/16`, shared-basis, and packed-256 sentinels remain part of the accepted v3 regression surface.
  - Independently audited the isolated encrypted A2B kernel at 0C/0M/2m. The evaluator's canonical parameters, sealed exp/ID/MSB payload, shared power basis, physical levels, operation-derived exact-scale gates, post-bind key mutation checks, integer-lift semantics, and bounded claim all passed. The two Minor findings were missing same-bit reordered-Q and per-ID/MSB/mapping tamper regressions; root added those tests plus a provenance-to-profile operand-digest equality assertion, and the targeted repair test passes. Independent repair rerun remains pending.
  - Added a real same-secret/same-`MemEvaluationKeySet` kernel-to-B2A composition test. Two kernel calls generate the low/high Boolean halves for `[00,10,A5,FF]` from normalized prefix codes, then feed those exact L5 ciphertexts to the chain B2A. The intentional red fails before mutation because the measured kernel scale `0x1.fffffc9f400865b0222e011350815d12p+34` differs from the B2A profile's synthetic exact-default S35 admission. The earlier B2A author gate is therefore reopened: repair must bind the actual producer scale or deliberately change the kernel target with a full numeric/audit rerun; `SetScale` is forbidden.
  - Repaired the real kernel-to-B2A scale boundary by using Lattigo `EvaluateMultiPoly`'s documented target-scale correction for the small natural root-scale deviation. The shared ID/MSB LUT now targets exact default S35 without another level, homomorphic operation, or `SetScale`; ID, MSB, and source `ID/16` states are L5/L5/L4 at exact `0x1p+35`. Kernel profile schema v3 seals Boolean output L5/S35 with digest `44471389...3986519e`. The same-keyset four-word kernel-to-B2A test, kernel/refresh/B2A targeted count-five suite, full homchain count-one, vet, and formatting all pass. Independent stable re-audit is active.
  - Closed the author-side refresh-to-kernel scale repair without an added level. Special-b0 now compiles with the actual `q20` matrix scale, the bootstrapping evaluator is fixed to `LogMessageRatio=15`, and separate 192-bit execution DFT literals fold in Lattigo's constructor initialization factors while the raw paper literals remain STC `1` and CTS `1/16`. Both halves follow `L20/S35 -> L19/S35 -> L18/S35 -> L16/S35 -> L0/S35 -> L20/S35 -> L17/S35`; the same keyset then enters the unchanged kernel. Five all-256 runs keep lattice distance below `8.6e-8`, observed `|I|<=5`, and ID/MSB errors below `1.31e-6`. Raw/execution DFT and full-profile digests are frozen; `NormalizedInputCertified` remains false and serial residual peeling remains unimplemented. Independent audit is active.
  - Independently accepted the refresh-to-kernel scale repair at 0C/0M/0m. The reviewer matched the raw/execution DFT factors to vendored Lattigo initialization, verified exact canonical parameters, q20/MR15, both L20-to-L17 S35 traces, no ModUp retag, same keyset/key pointers, immutable inputs, all-256 lattice/periodic/kernel oracles, and the bounded first-iteration claim. Refresh-specific count-five, homchain count-one, vet, and formatting pass; serial second-refresh work remains separate.
  - Independently accepted kernel profile v3 and the repaired same-chain B2A boundary at 0C/0M/0m. The prior two kernel Minor regressions and the B2A scale Major are closed; package-wide count-five passes with unchanged nonlinear operation counts and levels `[17,11,10,9,5,5,5,5]`. ID, MSB, and `ID/16` are exact S35, and the real two-kernel-to-B2A four-word composition preserves residues and inputs.
  - Reached the first real serial full-A2B ciphertext gate for `n=8,w=4,d=2`. After the first low kernel, the source `ID0/16` update lands the high core at L4/S35; the second mask reaches L3/S35, an independently encoded `LevelQ=3,[1,1]` STC reaches L1/S35, guarded ScaleDown/ModUp reaches L0/L20 S35, the shared CTS reaches L17/S35, and the second Gao kernel returns L5/S35. The A5 oracle yields low/high LSB-first `[1,0,1,0]`/`[0,1,0,1]` without an extra bridge. Profile, self-removal, all-256, key, and negative gates remain active, so this is a local milestone rather than an accepted full A2B.
  - Completed the author-side fixed `n=8,w=4,d=2` serial A2B gate. The sealed 24-state all-S35 graph uses the independent second `L3->L1` STC, two refreshes, two kernels, zero residual rotations, one shared keyset, and exact profile `d9c156fb...3dfc90`. Five exhaustive all-256 runs have zero bit/lift/root/ID failures; the maximum final low/high error is `1.22e-6`, high-update error `7.03e-8`, and self-removal error `1.12e-6`. The repaired exhaustive oracle derives `rawHigh-ID0/16` independently, distinguishing the source serial residual from fresh high-prefix re-encoding in 544/1,024 slots even though both final MSBs agree. Targeted count-five, homchain, full repository, vet, formatting, and whitespace gates pass. A fresh stable audit is active; fidelity remains `functional_lattigo_adaptation_not_secure_not_source_secure` and `NormalizedInputCertified=false`.
  - Completed the first independent stable serial-A2B audit at Spec 0C/0M/3m. The source graph, two refreshes/kernels, numerical envelopes, independent residual oracle, digest payload coverage, immutability, and bounded labels all pass. Acceptance remains REVISE because the trace copied operation counts from its sealed profile instead of accumulating them at runtime, the 24-state test verified an unordered map instead of exact serial order, and same-element foreign-secret relinearization/Galois pointer negatives were missing. Repairs are active; the Standards axis separately reported four heuristic maintainability Minors, of which the misleading fixed-S35 helper name is being cleaned while intentional opaque-seam/source-token sentinels remain.
  - Completed the author-side repair of all three stable-audit findings. `A2BFullTrace` now starts with a zero ledger and increments only after each successful physical operation, then compares that runtime ledger with the sealed profile before return; failure traces assert zero counts. The state gate is an ordered 24-entry sequence with stage, level, exact S35, degree-one, and dimensions checks. Same-element foreign-secret relinearization and Galois replacements now fail before counts/states or mutation. The fixed-S35 helper and completed-circuit comments are explicit, targeted count-five/full repository/vet/formatting pass, and all frozen profile/DFT digests are unchanged. Independent repair re-audit remains required.
  - Independently accepted the repaired fixed serial A2B slice at 0C/0M/0m. The reviewer verified the execution-derived zero-initialized ledger, ordered 24-state exact physical trace, genuine same-parameter foreign-secret relin and same-element Galois replacements, explicit S35 helper/comments, and unchanged frozen digests. Targeted count-five, homchain, full repository, vet, formatting, and whitespace gates pass. The accepted scope is fixed `n=8,w=4,d=2`, dense/no-switch, functional-not-secure, not source-secure, and `NormalizedInputCertified=false`.
  - Completed the first independent audit of the full A2A-e vertical chain at 0C/1M/2m. The Major finding is a self-referential ScaleDown exactness assertion: the code snapshotted `scaledDown.Scale` as both actual and expected, while a secondary numeric equality ignored precision/mode/modulus metadata. Minor findings require frozen profile/transform/STC/CTS digests and a nonempty key-preflight failure stage. Repairs are active; the full A2A-e slice remains REVISE despite green numerical tests.
  - Completed the author-side A2A-e audit repair. ScaleDown now constructs `q0*errScale` independently, compares immutable exact snapshots, and rejects modular error scales; equal-numeric/different-precision, rounding-mode, and modulus sentinels pass. Key failures report the new `key-preflight` stage with zero operations/states and immutable input. Profile/transform/STC/CTS digests are frozen at `6173b79d...280c18`, `238898a4...026c1fb`, `3b4394a6...2a735b`, and `24641e43...edcd4e0`. Combined A2A-e/A2A-I/sine count-five, vet, formatting, and whitespace checks pass; independent repair audit remains pending.
  - Independently accepted the complete functional-not-secure A2A-e repair at 0C/0M/0m. The reviewer confirmed the independent `q0*errScale` exact snapshot, all metadata sentinels, frozen digests, zero-op key-preflight failures, canonical parameters, full `16->15->13->0->16->13->7->6->5->4->3->3` state chain, constructor-probe key non-retention, and bounded fidelity labels. Targeted count-five, exact-scale count-five, full homchain count-one, vet, formatting, and diff checks pass.
  - Implemented and independently accepted the direct rank-one sign-fusion slice from Gao's full-packed Boolean high half. It maps the first column of fused `U0*t^-1` into column 3 of each four-slot word, uses BSGS `N1=2`, rotations `[1,2]`, and Galois elements `[5,25]`, and follows the exact state `L5/S35 -> L5/0x1.fffffe904p+69 -> L4/S35`. All 256 words, four-block isolation, an independent `Z2n` oracle, a semantic generic-B2A control, real Gao-kernel-high composition, graph/key/profile tampering, and input immutability pass; maxima are `2.71e-9` versus the oracle, `6.23e-9` versus semantic B2A, and `2.00e-7` for the real composition. Source/compiled/profile digests are `c5bda437...147849f`, `da982704...31b0b`, and `68b3bcc5...74a4ae`; the independent review returned 0C/0M/0m. This emits arithmetic `b7` only and makes no speed claim; the same-input encrypted-isolation baseline and complete two-path performance table remain deferred.
  - Froze the implementation-ready signed-int8 no-overflow comparator contract. It derives `d_min=x_min-t_max` and `d_max=x_max-t_min` with arbitrary-precision integers, admits only `[-128,127]` differences, composes CT--PT or CT--CT subtraction at L20/S35 with full serial A2B and direct sign fusion, and emits the CCS right-branch bit through arithmetic-root-slot `1-b7` at L4/S35. The contract also fixes nested counts, one-keyset provenance, exhaustive pair/range negatives, an explicit counted refresh interface for multi-depth trees, and the R0/R1 boundary: fixed signed-int8 is not an exact replacement for the repository's ordered-uint32 float32 model.
  - Completed the comparator's audit-gated Phase A without touching the encrypted composition. `Signed8NoOverflowRange` derives difference endpoints with `big.Int`, seals width/signedness/GE/child orientation/proof/overflow metadata, and revalidates its digest internally. Exhaustive singleton testing covers all 65,536 int8 pairs, accepting 49,152 no-overflow pairs and rejecting 16,384 overflow pairs; box, endpoint, machine-extreme, metadata-tamper, and foreign-digest negatives pass five times. The circuit/evaluator handles are compile-safe but execution remains hard-closed with `ErrSigned8ComparatorAwaitingA2BAudit` until serial A2B receives independent stable acceptance.
  - Independently audited comparator Phase A at 0C/0M/1m. The `big.Int` proof, exact 49,152/16,384 singleton partition, independently reproduced frozen digests, self-validation, opaque copy semantics, and absence of any A2B/sign call all pass. The remaining Minor is regression coverage: existing tests exercise nil receivers but do not construct a nonnil circuit or forge nonnil circuit/evaluator values, so a future nonnil-only opening could escape the hard-close gate. The implementation remains closed; repair tests are active.
  - Independently accepted the comparator Phase-A repair at 0C/0M/0m. Tests now invoke the constructor and nonnil circuit/evaluator public and opaque paths with nonnil handles/ciphertexts, proving that the audit gate returns the exact sentinel, nil outputs, zero trace, and immutable arrays/objects. With serial A2B and sign fusion also independently accepted, the encrypted Phase-B composition is now authorized.
  - Completed and independently accepted the second operational-precision repair of the isolated Gao degree-32 sine plus three-double-angle Lattigo slice. The functional-not-secure LogN=5 profile remains Q35x10/S35/P60 with measured 6+3 levels and exact state `[9,3,2,1,0]`, but all polynomial coefficients now travel through a full-slot `PolynomialVector` and every recurrence constant is a 16-slot exact vector on the private 256-bit encoder. A same-evaluator `2^200` rounding sentinel measures scalar-53 Add/MulThenAdd error `2.667e-17` and packed-256 error zero at 256-bit test precision. The 208-probe scalar maxima remain `6.69528901e-12`/`7.70672009e-11`; the latest five encrypted maxima span `1.533e-8..1.068e-7`. The independent second audit returned 0C/0M/0m; this is an accepted isolated kernel, not the complete A2A-e graph.
  - Completed the author-side gate for the complete functional-not-secure A2A-e vertical chain. The measured graph is `L16 -> fused-t V L15 -> STC L13 -> ScaleDown L0 -> ModUp L16 -> CTS L13 -> sine L7 -> three double-angle rounds L6/L5/L4 -> fused-tInv U L3 -> input-minus-recovered L3`, with one ScaleDown, one legal input DropLevel, no SetScale, and exact key union `[5,17,25,33,41,49,63]` plus relinearization. Three end-to-end repetitions preserve all four residues, keep the fixed-oracle error near `5.4e-8..9.4e-8`, and reduce the reported noise diagnostic from about `1.68e-3` to `1.66e-7..2.16e-7`. Profile digest `6173b79d...280c18` is frozen locally. Independent review is pending, so this is not yet an accepted slice and does not claim sparse-key/source-secure fidelity.
  - Completed and independently accepted the exact security-estimator conversion and two-run transcript at 0C/0M/0m. The converter authenticates both parameter manifests, exact modulus products, estimator commit/tree/archive, Sage 10.9 runtime, 389-package live lock, two attack paths and every returned result field; 386/386 converter, 3/3 manifest and 23/23 metadata tamper probes fail closed. Both 54,188-byte transcripts are byte-identical with SHA-256 `690a66c...db02`. Candidate exact-QP minima are 150.672 classical and 136.740 quantum bits; because every row uses `m=+Infinity` and finite key/sample exposures plus the secure circuit profile remain absent, the decision is `APPLICATION-SECURITY-INCONCLUSIVE`. The independent functional negative control fails at 11.680/10.600 bits.
  - Added and independently accepted the exact-only `integer/secureprofile` adapter at 0C/0M/0m without relaxing any accepted LogN=5 functional constructor. It rejects parameter, Q/P ordering, scale, secret and error drift; binds parameter digest `c37129e...a816` and profile digest `7e4f930...e99b89`; reports capacity 8,192 eight-bit words; and records the exact `8796093022208/8796090007553` ScaleDown correction, MR0, and static `20->17->5->4->3` target topology. Its maturity remains `parameter_candidate_unverified`; missing evidence is defensively copied and no sensitivity result is promoted to circuit or application-security evidence.
  - Independently accepted the repaired periodic selector reraiser at 0C/0M/0m and then completed the frozen full `count=5` gate in 589.760 seconds. The production path converts an authenticated L4 arithmetic-root branch bit to a repeated ordinary scalar at L8 through normal V, guarded refresh, shared CTS, exp46, two squares and one affine map. Profile v4 seals 19 states, runtime-derived operations, exact Galois inventory/identities, online bytes 2,942+5,054=7,996, and 49,408 bytes of separately labeled retained local evidence. The first audit found missing byte accounting and same-map cache deletion; the second left two missing nil regressions; the final true 27-case matrix, all-16 public/opaque tests and L8-to-L7 scalar-leaf bridge closed every finding. Frozen production/unit/integration hashes are `06123AA...16E4D3`, `218EC8...78ED5`, and `EEDF08...97BDC6`. The rewritten integration contract independently passed a separate 0C/0M/0m source-consistency review.
  - Completed a graph-first, source-verified route for the first source-faithful depth-2 prefix. It identified the selector's exact non-S35 output scale, derived the no-retag L8/R-to-L7/q7 conditioner, fixed the width-two feature/threshold delta selection at L6/S35, and proved that the next conversion must be a separately sealed `A2BFullIngress6Circuit`. The route also rejected bare DropLevel or ScaleDown/ModUp semantic reraising and any forged reuse of the L20 comparator/selector producer digests.
  - The first encrypted prefix implementation reaches `F1,T1:L6/S35` with one CT--CT product, two CT--PT products, one relinearization, three rescales and zero rotations; its four-query numerical gate passed. Independent review nevertheless keeps the slice at REVISE: the accepted selector result did not seal its output ciphertext payload, so replacing a same-state `L8/R` scalar before `BindSelectorResult` could be re-bound under a newly computed downstream digest. The repair now mints output payload/provenance at selector execution time and requires full result validation before conditioner admission; selector and prefix hashes must be re-frozen and re-audited.
  - The first fixed ingress-6 A2B implementation passed its L6 admission, 25-state functional graph, all-256 independent oracle and L20 differential checks, but its first independent audit returned 0C/3M/0m. Exact graph-slice cardinalities were not sealed, four second-round byte entries came from state templates rather than real runtime ciphertexts, and the evaluator-level zero-operation mutation matrix was incomplete. Repair is active with exact 2/3 STC/CTS slice lengths, real per-stage ciphertext serialization, frozen byte constants and a public-`EvaluateNew` mutation matrix; no child-comparator, depth-2, security or speed claim is admitted yet.
  - Completed the author-side selector-result sealing repair. Selector profile v5 now mints and validates an execution-time output-payload/result-provenance seal, the conditioner binds that upstream result provenance, and the depth-2 prefix still produces exact `F1,T1:L6/S35` without metadata retag. The expanded mutation matrix, selector and prefix count-five gates, compile, vet and formatting pass; the seven new hashes are frozen locally. A fresh independent audit is active, so this repair is not yet accepted.
  - Independently accepted the selector-result sealing and depth-2 prefix repair at 0C/0M/0m. The auditor rederived selector/conditioner/prefix/tree/schedule digests, matched all seven file hashes, rejected coefficient mutation and exact-same-state foreign replacement, and reran public/opaque selector smoke plus the four-query encrypted prefix gate. Maximum feature/threshold errors were `4.00e-6`/`4.07e-6`; prefix bytes remained external `22780`, internal-selector `9580`, outputs `7996`, retained `29494`. This accepts the functional prefix only, not the child comparator or complete depth-2 tree.
  - The first three ingress-6 Majors were repaired and all associated functional, byte-ledger and pre-HE mutation gates passed, but the apparent independent PASS was withdrawn after a targeted counterexample. The two first-STC, three shared-CTS and two second-STC compiled transforms sealed only literal digests and `Vec` map identities; an existing diagonal plaintext coefficient can be changed in place without changing either, and the evaluator then consumes the changed payload. The reopened audit is 0C/1M/0m. Repair now requires seven actual encoded-payload digests and three independent pre-HE mutation controls.
  - Completed and independently accepted ingress-6 payload profile v2 at 0C/0M/0m. The final graph and profile seal all 2+3+2 actual encoded DFT factors by metadata, ordered diagonal indexes and serialized plaintext payloads, enforce exact group lengths, and reject in-place coefficient mutation before HE. The full 50-case mutation matrix, 18-case length/payload gate, all-256 oracle, L20 differential and 25-state real-byte ledger pass; profile digest is `3cc7441e...70d7dc`, online bytes remain `10938`, and retained evidence remains `101534`. This replaces the withdrawn earlier PASS and authorizes the child-comparator slice only.
  - Completed a read-only secure-N16 capacity derivation. Encoded STC/CTS storage is 6.4873046875/2.16015625 GiB, and the resident artifact set is about 9.15 GiB before keys or evaluators. The current 31.309 GiB host has only 9.712 GiB of headroom before the 80% limit; non-streaming matrix generation and textual digest construction add several GiB of transient allocation. The exact profile is therefore `RESOURCE_BLOCKED` before full-slot DFT construction. A deterministic, no-artifact dry-run capacity contract is being implemented; this does not promote circuit or security maturity.
  - Implemented the no-artifact secure-N16 capacity contract and then reproduced its first independent-audit Major. The initial permit validator accepted a caller-resealed `ADMITTED` permit that referenced a real `RESOURCE_BLOCKED` report because it checked only digest syntax and the permit's self-seal; three deterministic forgeries reached the constructor wrapper. The repair now re-evaluates the current snapshot, requires its unique admitted report, rebuilds the expected permit and compares the complete value. All three re-sealed forgeries return typed block with constructor count zero; independent repair review is active.
  - Independently accepted the repaired secure-N16 capacity contract at 0C/0M/0m. The reviewer reran real-blocked, overflow-probe and arbitrary-digest re-sealing attacks, all of which now return typed block with zero constructor calls, while the unique admitted permit still validates. Exact shape/policy/plan and blocked report digests remain stable; final file hashes are `a1f91ec5...f610` and `b3b6737a...38e3`. The accepted decision is resource admission only, and the present host remains `RESOURCE_BLOCKED` before any full-slot artifact allocation.
  - Froze the secure-N16 capacity-unblock route. Full-packed `LogSlots=15` remains the 8,192-word source-faithful target but needs factor-at-a-time construction and disk/lazy evaluation keys; matrix streaming with all keys resident is still blocked. A separately digested `LogSlots=11`/512-word sparse-packing R1 gate has a runtime-adjusted guarded estimate of `7,842,848,076` bytes and `2,585,547,267` bytes of host margin, so it is the first encrypted secure-parameter vertical slice. `LogSlots=12` has only `459,715,075` bytes of margin and is deferred until measured L11 RSS supports it.
  - Completed the read-only source-faithful depth-2 terminal route. A child-specific periodic decoder authenticates the new child producer and returns `b1:L8/R`; its conditioning is fused into two leaf-delta plaintext multiplications. With the prefix's authenticated root selector `b0:L7/q7`, the terminal computes `A=l00+b1(l01-l00)`, `D=(l10-l00)+b1(l11-l10-l01+l00)`, and `y=A+b0D` at exact `L6/S` using two CT--PT, one CT--CT, one relin, three rescales and zero rotations. Leaves remain finite server-held float64 model constants under a magnitude certificate, and the output is an ordinary CKKS real ciphertext, not an int8/modular shortcut. Implementation waits for child-comparator independent acceptance.
  - Completed the author gate for the no-artifact secure-N16 `LogSlots=11` capacity slice. The versioned R1 profile seals the exact SplitRealAndImag STC/CTS literals, physical Trace rotations/elements, 38-element deduplicated DFT/Trace/conjugation union and three fixed keys without constructing any HE object or probing the OS. Exact guarded requirement is `7,842,848,076` bytes with `2,585,547,267` bytes of frozen-host margin; profile/shape/plan digests are `ea9e91c9...e0f85`, `61169b61...8eb4`, and `9c73127d...d06b6`. All focused count-five, package, vet, formatting and negative permit/promotion controls pass. Independent acceptance audit is active, so this remains author GREEN only.
  - The first independent `LogSlots=11` capacity audit returned 0C/0M/1m. Production arithmetic, exact Lattigo/BigInteger 38-key derivation, all capacity values, permit forgery controls, R1 labels and full `count=5` gates pass, with stable production/test hashes. The Minor is explicit regression coverage: zero profile/shape/plan/report/permit, critical nil slices and blank/zero/inconsistent snapshots were only statically fail-closed, not exercised through real constructors/validators with zero-constructor assertions. A test-only repair is active; the slice remains REVISE until fresh review.
  - The first independent child-comparator audit found a blocking nested-graph Major before acceptance. The wrapper sealed the top-level ingress/sign evaluator pointers but not every bind-time nested suffix/circuit/source/keyset/evaluator identity. A mathematically valid foreign-secret ingress suffix, or a coherently replaced sign subgraph, could therefore pass its own self-consistent nested preflight and be consumed after the wrapper's first subtraction. The child slice is REVISE; author work now starts from a reproducible foreign-graph RED and must bind every nested execution identity before the first HE operation, then pass a fresh audit.
  - Independently accepted the repaired `LogSlots=11` no-artifact capacity slice at 0C/0M/0m. The fresh reviewer verified that all 25 zero-value, nil-slice and invalid-snapshot cases enter real constructors/validators with typed fail-closed results and zero constructor calls, while production SHA `cefe6dd9...b749c`, all seven digests, the 38-key derivation and exact `7,842,848,076`-byte guarded requirement remain unchanged. This accepts only R1 capacity/profile admission for 512 words; sparse Trace, MR0, encrypted execution, full-packed Route A and application security remain pending.
  - Tightened the Route-B encrypted-construction contract after tracing the pinned vendor path: `bootstrapping.NewEvaluator` uses the S43 parameter's 53-bit default encoder for DFT generation. The secure adapter must instead accept capacity-admitted, prebuilt 256-bit factors, bind generator/encoder precision and payload digests, and prove a small-profile coefficient differential plus a rounding sentinel. Constructing default matrices and replacing them is forbidden because the duplicate resident set invalidates the capacity plan. This is a design finding; no encrypted MR0 result or measured RSS follows.
  - Completed the independently checked Lattigo runtime-identity support layer required by the child-comparator repair. Six focused package gates (`ring`, `core/rlwe`, `schemes/ckks`, CKKS `lintrans`, `polynomial`, and `dft`) plus `go vet` pass. The snapshots are value-only with private fields, remain stable across real Encode/Add/Decode and scratch changes, and detect whole-value/private-backing replacement, automorphism-map drift, DFT private-parameter drift and polynomial `CoefficientGetter` replacement. Final production hashes are `6a749b9b...f1f5`, `3957a2d6...bc7c`, `5fd02c14...8f3`, `87909ccd...8cea`, `85823911...3cc9`, and `c271aa91...c6a5`. The child wrapper has compiled against these APIs, but its own mutation and encrypted acceptance gates remain open.
  - The independent Route-B precision-construction design audit returned REVISE at 0C/2M/1m. It confirmed the S43 stock 53-bit generation path and proved that default-factor-then-replace exceeds the L11 margin by 55,815,677 bytes. The repaired design now separates raw and post-initialize effective DFT literal/scaling digests, introduces a second construction permit binding precision/builder/digest/schedule/manifest/peak semantics to capacity-v1, and requires private bundle ownership. The audit and repaired contract are recorded in `research/phase3_design/secure_n16_route_b_precision_audit.md`; implementation and encrypted MR0 remain pending.
  - The narrow Route-B precision-design re-audit accepted the repaired documents at 0C/0M/0m. It verified the raw/effective-literal edge, separate zero-allocation construction permit, exact 2,641,362,944-byte duplicate delta, private ownership and hard stop conditions without promoting builder, MR0, measured RSS, full-packed Route A or application security. Frozen document hashes are `0805718f...9390a`, `5616ec23...b39c4`, and `9dd8f2f9...ace99`.
  - Froze and independently accepted the repaired depth-2 child comparator at 0C/0M/0m from both graph/runtime and semantic/evidence reviews. The wrapper binds exact declared-versus-embedded key sets; value-only CKKS/RLWE/DFT/linear/polynomial runtime snapshots; fresh-derived Mod1 parameters; complete expanded gadget-key dimensions, levels and public metadata; and every same-key or foreign nested evaluator identity before HE. The independent 91-case zero-operation matrix passed in 162.236s, constructor/split-keyset gates in 32.935s, four semantic units in 16.786s, and `EncryptedSelectedChildren` including post-operation preflight in 43.689s. A first three-test count-five run reached only Go's default package timeout at 10 minutes with no assertion failure; the unchanged public/opaque/endpoint-equality gate then passed completely with `-count=5 -timeout=30m` in 666.280s. Final production/unit/integration hashes are `104e41ff...9ccca`, `2b10fd31...17d2`, and `cc2c3a87...9577`.
  - Repaired the 256-bit CKKS Encoder runtime snapshot false positive exposed by the encrypted child gate. High-precision IFFT legitimately bit-reverses the mutable `buffCmplx` pointer slice; the snapshot now retains its exact outer backing and complete element-identity multiset while ignoring only element order. High-precision Encode and polynomial-evaluation stability pass, and replacement of a scratch element or mutation of an immutable root still changes the snapshot. Vendor CKKS runtime/test and polynomial-test hashes are `8f01038b...69f9`, `4f1511ef...931a`, and `02c402e5...4299`.
  - Opened the child-specific decoder and terminal float64 mux as the active tree slice. The design is under a fresh numeric/provenance repair: it must use canonical `dL/dR/dX/cross` notation, exact float64-to-rational magnitude arithmetic, separately measured complex-modulus selector errors for `b0` and `b1`, and must label its 192-bit inequality as ideal centered-message headroom rather than a ciphertext-noise proof. No complete depth-2 tree is yet claimed.
  - Repaired and independently accepted the terminal implementation contract at 0C/0M/0m from separate numeric and API/provenance reviews. Frozen design SHA-256 is `a014a513...a760c`. The certificate derives exact rationals from frozen float64 bit patterns, rounds only cache sources with explicit 256-bit nearest-even semantics, records cache error, admits `|leaf|<=2^16` and derived bounds below `2^20`, and retains a minimum 200-bit ideal centered-message headroom (including `b1=289` and `b0=254`). It explicitly does not certify CKKS/RLWE noise. The closed terminal admits only `(childInput,childResult)`; decoded-output mutations are post-operation validator tests rather than impossible zero-work preflight cases. Implementation remains pending.
  - Independently accepted the bounded vendored DFT explicit-generator-precision seam at 0C/0M/0m. The stock wrapper remains exactly parameter-default precision and equals explicit 53-bit construction; explicit 256-bit generation uses a matching 256-bit encoder and matches an independent `GenMatrices(LogN,256)+Encode` reference factor-by-factor, including metadata, sorted diagonals, RingQP polynomials and canonical serialization. The frozen numeric sentinel is `0x1.6a09e667f3bccp-01` at 53 bits versus `0x1.6a09e667f3bcc908b2fb1366ea957d3e3adec17512775099da2f590b0667322cp-01` at 256 bits, and encoded payload digests differ (`4a1a71ae...55af1a` versus `a4376855...8790bc`). Production/test SHA-256 values are `15362546...5A611` and `41503D33...792B`. This is only a small-profile constructor support seam: factor-at-a-time generation, effective bootstrap preparation, private prebuilt installation, L11 artifacts, HE, RSS and security remain open.
  - The first independent child-specific decoder reviews returned REVISE at 0C/2M/0m in aggregate. One reproducible RED mutates top-level bootstrap execution metadata such as `CoeffsToSlotsParameters.LogSlots` or live Mod1 scale/ratio after binding; the previous preflight sealed nested runtime objects but not these value fields. Separately, failures after the first HE dispatch returned a zero trace because evidence stayed in a local helper until successful completion. Repair now binds the complete reusable bootstrap-parameter/Mod1 execution digests before and after evaluation and emits authenticated partial traces with attempted/completed work, actual existing-state bytes, failure stage and provenance. The terminal public four-path gate itself is author-green (52/52 pre-op rejects, 24/24 post-op validator rejects, 54,312 local bytes and 106,662 unique composed bytes in 560.627s) but remains deliberately unfrozen until decoder failure evidence is propagated and public/opaque gates are rerun.
  - Independently accepted the vendored DFT factor-at-a-time support seam at 0C/0M/0m. `ForEachMatrixFactor` and the streaming builder match an independent pre-streaming whole-vector reference coefficient-for-coefficient across encode/decode, standard/split/repack, forward/bit-reversed, nil/custom scaling and 53/128-bit cases; the encoded metadata, sorted diagonal inventory, complete RingQP payload and canonical bytes are exact. Sorted source-diagonal accumulation removes real 53-bit map-order drift, callback errors stop all future layers, observer gates bound logical live factors/layers at 1/1, roots and factors do not alias, and the frozen 53/256 numeric and payload sentinels remain unchanged. Package `count=100`, vet and diff checks pass. Final SHA-256 values are `93219e6b...4e2`, `49cc7c5d...43ba`, and `41503d33...792b`. This is a small-profile support seam only; receipt observation, pure bootstrap preparation, L11 artifacts, HE, RSS and security remain open.
  - Independently accepted the repaired child-specific decoder at aggregate 0C/0M/0m from graph/runtime and semantic/evidence reviews. The frozen production/unit/integration SHA-256 values are `70ef6127...7ae`, `c21f005b...279`, and `bd15c122...a3ed`. Nine bootstrap metadata/execution mutations now fail before HE with exact-zero evidence; bind-time and per-call digests cover residual/bootstrap parameters, complete DFT/Mod1 literals, circuit order, ephemeral weight and live Mod1 execution values. Trace v2 separates completed counts from an honestly named top-level `AttemptedOperationLowerBound`; successful runs require equality, while every error after dispatch returns a zero result plus authenticated nonzero partial evidence with real state-prefix bytes, failure stage, cache/preflight evidence and closed provenance. Three synthetic cuts, a real normal-V failure and best-effort-finalizer failure all pass. The bounded all-16 claim remains precisely “inherited base mathematical oracle plus child-seam mixed-vector integration,” not a direct child-fixture all-16 encrypted sweep. Terminal propagation remains open.
  - Reopened the Route-B construction contract after an independent reachability audit found 0C/2M/2m. The previously accepted object is internally sound only as a **post-artifact ReadySpec/ReadyPermit**: it requires real numeric/encoded payload digests before it authorizes the very builder that must create them. The executable state machine is now frozen as `CapacityPermit -> pure PrepareParameters -> BuildSpec/BuildPermit (no payload) -> STC-then-CTS factorwise build -> observed BuildReceipt/private artifact -> ReadySpec/ReadyPermit -> install -> first-HE preflight -> MR0`. The receipt must prove the interleaved generate/numeric-digest/encode/encoded-digest/release lifecycle, max one live numeric factor, 256/256 precision, zero default constructor calls and exclusive move semantics. No placeholder digest is admissible, and the earlier 0C/0M/0m result is narrowed rather than erased.
  - The first full audit of that two-stage design returned REVISE at 0C/3M/3m on SHA `d24494ec...6afc1`: its serializer was not a unique typed grammar, public factor callbacks could not authoritatively prove post-callback reference drops or real default-constructor counts, and an isolated process that returned only a receipt would lose the in-memory artifact before Ready/install/MR0. Design v2 candidate SHA `b9996997...c628` now assigns outer lifetime events to a private `secureeval` state machine and factor/reference events to a vendor-owned trace, requires real atomic constructor deltas `0/0/2` for build and `0/0/0` for assembly, defines exact factor/metadata/Q-then-P wire records and a frozen 81,967-byte packing-layout digest, keeps the entire encrypted route in one child, and accounts for simultaneous old/new numeric maps with 70,811,912-B STC and 36,208,904-B CTS phase maxima. Independent re-audit is active; no implementation or L11 allocation is authorized yet.
  - The v2 re-audit closed all six prior findings but returned REVISE at 0C/1M/0m on SHA `b9996997...c628`: public raw-numeric `ForEachMatrixFactor` and `GenMatrices` could allocate or retain whole numeric factors without changing the three constructor counters. The conservative STC bypass phase reaches 103,842,056 B, exceeding the envelope by 22,577,416 B. Design v3 candidate SHA `0da92f89...c031` adds an exclusive `RawNumeric` field, requires all public constructor/generator entries to increment exactly one counter and call a private core directly, freezes build `0/0/0/2` and assembly `0/0/0/0`, and adds direct `0/0/1/0` tests for both raw entry points. Fresh re-audit is active and no large allocation is authorized.
  - Independently accepted Route-B two-stage design v3 at 0C/0M/0m on frozen SHA `0da92f89...c031`. The reviewer exhausted the five existing public DFT generation entries, verified single-field counter routing plus the private observed core, and independently reproduced the 81,967-byte layout hash, 13,631,712/14,680,304-B STC/CTS polynomial sizes, 70,811,912/36,208,904-B admitted phase maxima and 103,842,056-B forbidden raw-numeric bypass. Pure Prepare, typed canonical wire, reference-drop ownership, same-child build-through-MR0 reachability and exact sparse runtime/key tuples remain accepted. Implementation and small-profile gates begin next; no N16 allocation, MR0, RSS or security claim follows from the design pass.
  - Implemented and independently accepted the first Route-B vendor enforcement slice at 0C/0M/0m. Four process-wide saturating atomic counters cover the five existing public DFT construction/generation entries exactly once; every wrapper increments before validation and enters a private core, raw `ForEachMatrixFactor`/`GenMatrices` calls report only `RawNumeric`, and `Delta` rejects saturation or non-monotonic snapshots before subtraction. Focused tests, vet, formatting and diff checks pass. Frozen hashes are `eec36859...5eca` (`dft.go`), `f6b222d9...c077` (counter production) and `70280987...023d` (counter tests). The observed endpoint remains intentionally unwired for the next slice.
  - A provisional terminal float64 mux acceptance was superseded by an additional independent audit, which returned REVISE at 0C/1M/1m on the same frozen hashes. The 34-point progress ledger and fused `MulRelinNew` accounting pass, as do the fresh public/opaque numerical gates, but the success validator checks each retained ciphertext only against a caller-resealable payload map and outer trace seal. `RawD` can therefore alias same-state `RawA`, or retained `Output` can be replaced by a same-state foreign ciphertext, if the corresponding payload and outer seal are recomputed. The claimed real decoder post-dispatch coverage is also a synthetic authenticated-partial injection. Repair now reuses the failure-prefix uniqueness/provenance validator on success, binds retained output to the result, adds resealed alias/foreign REDs, and either exercises a real decoder prefix or narrows the claim.
  - Completed the author-green pure bootstrap preparation slice. `PrepareParameters` detaches both DFT `Levels` slices, exact `big.Float` values and iteration metadata, derives and deep-copies Mod1 polynomials, separates raw from post-CI/runtime-scaled effective parameters, and seals the full value including `CircuitOrder` and float metadata. It creates no DFT object (`0/0/0/0`) and stock `NewEvaluator` now consumes the prepared state before building exactly two default matrices on a tiny profile. A dedicated RED also exposed that legacy `ShallowCopy` rebuilt nested DFT/polynomial evaluators with residual parameters; it now uses bootstrapping parameters and passes a CI-to-standard runtime-digest regression. Frozen legacy scale bytes and prepared/Mod1 seal `b74e38d7...c5e1`, CI, alias, nil/empty, internal-drift and invalid-input gates pass; candidate file hashes are `e6cb5fb2...01ec4`, `67fb81f2...6760`, and `47d997f1...859d`. Independent acceptance remains open.
  - Repaired and independently re-accepted the terminal float64 mux at 0C/0M/0m. Success and failure now share the exact retained-prefix pointer-uniqueness and decoder/root/input link validator; retained `Output` is separated from admitted/intermediate ciphertexts and bound to the returned result's pointer, payload and provenance. The new resealed `RawD -> RawA` and same-state foreign-output attacks fail, and the synthetic decoder-adapter test is explicitly limited to authenticated-partial lifting rather than a real `evaluateNormalV` injection. Fresh public, opaque and terminal-only gates passed in 944.120s, 112.014s and 327.793s; vet/format/diff checks pass. Frozen production/unit/integration hashes are `bb28a4a8...80ac0`, `1bf2f687...20735`, and `9b281be3...a8028`. Scope remains `functional_not_secure`; complete orchestration, performance and security stay open.
  - A real `go build ./integer/homchain` gate then exposed that terminal production called the test-only helper `equalUint64Slices`; package tests had hidden the compile defect by compiling test files into the package. The five call sites now use a production-private comparator with byte-for-byte-equivalent nil/empty, length, order and value semantics. A fresh read-only audit reconstructed the prior production hash exactly after reversing only that rename/helper insertion and closed at 0C/0M/0m. `go build`, focused failure-ledger test, package compile-only and vet all pass. Final production/unit/integration hashes are `e33d7d32...30c9`, `1bf2f687...20735`, and `9b281be3...a8028`; no ledger, numerical or security behavior changed.
  - Repaired both findings from the first pure-Prepare audit. Invalid negative degrees, negative double-angle, non-finite scaling and non-real scaling-root combinations now return errors before any `Depth` or Mod1 construction and leave all DFT counters zero. Metadata evidence uses exact-equal `0.5` fixtures that independently vary only precision, rounding mode or accuracy. The current focused suite passes in 0.622s and vet passes; candidate hashes are `e6cb5fb2...01ec4`, `0017fc56...ca40`, and `0eda413d...9648`. Fresh independent acceptance remains open.
  - Froze and independently accepted the separate `RBAUTH-v1` authorization grammar at 0C/0M/0m on SHA `4a305efc...f6e7`. The 16-byte magic and four record types are disjoint from `RBDFT-v1`; BuildSpec/Permit contain no actual payload, manifest, lifecycle or receipt, while Ready binds the build permit, receipt, pair manifest and four observed digest/byte tuples. Cross-record identities use exact trailing RBAUTH seals, capacity is admitted before every Prepare call, and an owner-bound lineage cell privately anchors all write-once Build/receipt/payload/Ready identities so parsing or coherent resealing cannot recreate capability. Ready replays only resident encoded payload; historical numeric identities remain fixed-size vendor-trace/receipt anchors with zero retained numeric aliases. Maximum records are 3,212/3,244/1,943/1,975 bytes. This accepts metadata/capability design only; no builder, artifact or MR0 exists yet.
  - Independently accepted pure bootstrap preparation and stock routing at 0C/0M/0m. Production rejects negative Mod1 degree/inverse/double-angle, NaN/Inf scaling and non-real scaling roots before the first `Depth` or Mod1 constructor while preserving defined negative-scaling paths. Exact-equal `0.5` fixtures vary precision, mode and accuracy independently; pure preparation has DFT delta `0/0/0/0`, the stock tiny constructor has `2/0/0/0`, frozen legacy/CI semantics match, and `ShallowCopy` keeps N2 bootstrapping parameters in nested DFT/polynomial evaluators. Final hashes are `e6cb5fb2...01ec4`, `0017fc56...ca40`, and `0eda413d...9648`. This authorizes prebuilt assembly work, not an artifact, L11 allocation or MR0.
  - Implemented and independently accepted the vendor observed DFT plus pure matrix-validation slice at 0C/0M/0m. The sole observed entry increments only `ObservedStreaming`, binds 256/256 typed invocation tokens, emits exact STC/CTS 13/18-event traces, records honest consumer error/panic cleanup after post-nil factor drops and generator return, and moves encoded transformations through a shared ownership cell so shallow copies cannot retain or replay the payload. `Matrix.ValidateAgainst` checks full literal/float metadata, grouped scale schedule, fixed LevelQ/P, dimensions, BSGS/N1, sorted diagonal topology and every Q/P limb shape without constructing DFT objects; bare Matrix remains explicitly insufficient proof of payload identity. Independent audit caught and closed the shallow-copy ownership-cell bypass before final PASS. Final five hashes are `46fe170f...340d2`, `738523ab...a1d87`, `e58f8317...e7e1`, `e858351e...47b3`, `0eb100b7...6cae`; writer `count=100` and package/vet gates plus independent count20/package gates pass. No HE or L11 allocation ran.
  - The first pure `RBAUTH-v1` codec audit is REVISE at 0C/1M/1m on production/test hashes `2d528eb4...517f` and `c033488a...849a`. The four RBAUTH encoders/parsers, record identities, bounded allocations, canonical nullable-`big.Float` encoding, inert reports and build/ready field separation pass. The Major is evidence validity: Ready golden records bind 43/40-byte `RBDFT-v1` header-plus-ASCII placeholders rather than canonical type-`0x07` receipt/type-`0x05` pair records, so the frozen cross-domain bytes do not prove the accepted grammar. The Minor is reachability: raw fuzz mutations almost always fail at the SHA seal and never exercise deep body parsers, standalone nullable-`big.Float` limits or allocation ceilings. A test-first repair with real independent RBDFT fixtures and bounded resealed deep mutations is active; no authority, artifact or HE path is accepted.
  - Repaired and independently accepted the pure `RBAUTH-v1` codec at 0C/0M/0m. Independent complete RBDFT encoders/parsers now produce and validate a 210-byte type-`0x05` pair (`f33c626d...c282fe`) and 585-byte type-`0x07` receipt (`aae3c2b5...48913a`) before their whole-record hashes enter Ready. Header-correct truncated/reordered bodies remain acceptable only to the deliberately scoped header/hash helper and fail the independent full wire authorizer. Four valid RBAUTH records plus absent/present nullable-float seeds and bounded resealed body mutations reach deep parsers without exceeding per-type ceilings; standalone length/count/`MaxUint32` cases pass. Scope is explicitly `wire_fixture_only` and excludes lifecycle private-anchor authority. Final production/test hashes are `2d528eb4...517f` and `4f52a936...74ff`; focused, package, build, vet, format, diff and 77,327 fuzz executions pass with no HE.
  - Amended the accepted Route-B assembly contract to remove an impossible trust claim before implementing the vendor seam. A bare `dft.Matrix` has no generator/encoder precision or authenticated payload digest, so the vendor assembler is limited to exact role/literal/shape, key inventory, zero-counter and consume-on-call move checks. The owner-bound `secureeval` ready-use gate must independently validate the observed 256/256 trace, re-stream resident encoded coefficients and compare receipt/lineage anchors before creating the general vendor carrier. The carrier uses one shared terminal ownership cell, zeros source matrices on mint and clears itself on every assembly outcome. Candidate design SHA-256 is `cb1865fa...ddc4`; fresh independent design review is pending.
  - The first assembly-authority amendment audit returned REVISE at 0C/1M/1m. Rehash-before-CAS allowed two shallow artifact handles to stream concurrently until one moved/zeroed the matrices, creating a real hash-versus-move TOCTOU/data race; the text also failed to freeze matrix/key donor and partial-evaluator postconditions on every non-counter error or panic. The repaired cross-document candidate makes the original owner-cell `ready -> installing` CAS the first install action, keeps the winner exclusive through capacity/Prepare/rehash/keygen/direct move/assembly, passes keys through a nilled one-shot donor, sends every error/panic to terminal `failed`, and requires a second installed-resident rehash before first HE. New two-stage/auth-wire hashes are `cb8a6d82...f9213` and `cb20541c...d65d2`; fresh re-audit is pending.
  - Independently accepted the repaired assembly-authority split at 0C/0M/0m on two-stage/auth-wire hashes `cb8a6d82...f9213` and `cb20541c...d65d2`. Install now linearizes at the original owner-bound `ready -> installing` CAS before any resident read; the sole winner remains exclusive through current capacity/Prepare checks, install-time rehash, keygen, same-cell move and assembly. Matrix carrier and key donor success/failure postconditions are exact, every error or panic is terminal with nil result and no retry, and first-operation preflight performs a separate installed-resident rehash before HE. This accepts the executable ownership design, not its project-level implementation.
  - Completed the author-green vendor prebuilt evaluator candidate. Stock and prebuilt routes share one matrix-source assembly core; the stock source preserves two default constructors, while the prebuilt source accepts a shared-cell one-shot matrix carrier and a `**EvaluationKeys` donor that is nilled before validation. Exact role/literal/shape and existing key checks run without any DFT constructor, and an outer recover/counter finalizer forces a nil evaluator on every error, panic or nonzero four-counter delta. Tiny gates cover donor clearing/replay, invalid prepared/role/shape/key, nil source, factory/source panic, all four hostile constructor counters, stock `[2,0,0,0]` and prebuilt `[0,0,0,0]`; the success fixture deliberately uses default-precision matrices to freeze that vendor structural acceptance is not 256-bit authority. Final candidate hashes are `0365d844...92dc1`, `e436f609...967aa`, and `f9075a1b...efa2d`. Two independent audits are active; no secureeval wrapper, L11 allocation or HE ran.
  - Completed a read-only implementation-boundary scan for the private Route-B layer. No production `RBDFT-v1` serializer, receipt, Authority, live permit, lineage or private artifact exists yet; current `secureeval` contains only inert RBAUTH value records. The minimal implementation is split into `route_b_rbdft_wire.go`, `route_b_authority.go`, `route_b_artifact.go` and `route_b_install.go`. Build-time evidence and Ready/install/preflight must share one canonical streaming encoder over sorted diagonals and exact `ringqp.Poly.WriteTo` bytes: build binds vendor per-factor evidence and receipt anchors, while later gates re-stream only resident encoded factors. Historical numeric identities remain trace-derived fixed-size anchors and are never regenerated.
  - Independently accepted the vendored prebuilt evaluator assembler through two separate final audits at 0C/0M/0m each. The first verified the shared atomic ownership cell, consume-before-validation ordering, key/matrix donor terminality, panic/error nil-result normalization and all four counter-hostile paths; the second verified fixed C2S=`HomomorphicEncode` and S2C=`HomomorphicDecode` role mapping, stock-path equivalence, exact `2/0/0/0` versus `0/0/0/0` deltas, intended default-precision structural acceptance and the absence of receipt/trace/provenance authority in the exported surface. Both rechecked frozen hashes `0365d844...92dc1`, `e436f609...967aa`, and `f9075a1b...efa2d`; independent count-20/package/vet gates and a root count-20 rerun pass. This accepts only the general vendor assembler. The owner-bound `secureeval` layer must still authenticate 256/256 provenance, resident payload, live lineage, install and first-operation preflight before HE.
  - Completed a read-only live-Authority implementation map and found a new hard prerequisite. Existing public L11 profile/shape/plan/Evaluate/ValidateReport/ValidatePermit accessors are sufficient to reconstruct capacity evidence without reflection or exposing opaque permit fields, and pure `PrepareParameters` is suitable after capacity admission. However, the repository has no production constructor for the unique Gao-N16/LogSlots11 raw `bootstrapping.Parameters`; `GaoCompatibleN16Parameters` supplies only CKKS parameters, while caller-selected raw can be internally valid yet foreign to the project. Lattigo `Parameters.Equal` also omits `CircuitOrder`, so it cannot close this admission alone. A canonical raw constructor plus frozen raw/prepared identities is now a required pre-Authority gate; a field-level derivation review is active and no live permit is authorized yet.
  - Implemented and independently accepted the allocation-bounded pure `RBDFT-v1` type-`0x03` through type-`0x07` wire at 0C/0M/0m. Fixed STC/CTS aggregate arity and contiguous indices, the frozen type-`0x05` pair, an exact 35-event success lifecycle plus legal failure prefixes, the strict type-`0x07` receipt, and complete aggregate->pair->lifecycle->receipt links all pass independent parsing, mutation and zero-on-error gates. Frozen production/test hashes are `CC60D292...DE0601` and `F5441010...F064E3`; the lifecycle is `1848 B/4db54a...539fb8`, pair `210 B/f33c...282fe`, and receipt `585 B/aae3...8913a`. Root count-20/package/build/vet/format gates pass. This is inert wire evidence only: type-`0x01/0x02` payload streaming, owner anchors, live authority, artifact and HE remain open.
  - Completed the field-level derivation for the canonical Route-B raw configuration and recorded a separately scoped transport-parameter design candidate. The uniquely fixed parts are the exact Gao-compatible N16 CKKS chain, L11 STC/CTS shapes and h-tilde=32; the project must explicitly freeze same residual/bootstrap parameters, present exact 256-bit scalings `1` and `1/16`, `Custom`, nil iterations and the full MR0 metadata literal. The candidate makes `{Q17,S43,SinContinuous,K1,degree3,MR0}` preparation-only, forbids every stock Bootstrap/EvalMod selector, and keeps Gao degree-32/R3 as the only executable kernel. Candidate document SHA-256 is `3EE3ABEB...D44C9FA`; independent design review is active, so no constructor or prepared golden is accepted yet.
  - The first canonical transport-parameter design audit returned REVISE at 0C/4M/0m. It corrected `CircuitOrder` to `DecodeThenModUp`, established that the degree-3 compatibility polynomial is physically materialized and installed even though project capability must give it zero dispatches, separated Route-B A2B degree-46/R2 from the independent A2A-e degree-32/R3 kernel, and found that stock `ModUp` internally executes an unobserved Trace. The v2 repair now requires a vendor-owned actual Galois-prefix report, forbids stock evaluator/Mod1/shallow-copy/construction paths, and records the qDiff metadata correction. Repaired transport/two-stage hashes are `A137EFDC...443F50` and `C796D5C6...4C7897`; re-audit is pending and no constructor, Trace seam or HE result is accepted.
  - Independently accepted both repaired canonical Route-B design documents at 0C/0M/0m on hashes `A137EFDC...443F50` and `C796D5C6...4C7897`. The final contract fixes `DecodeThenModUp`, the resident-but-zero-dispatch degree-3 compatibility object, the typed A2B degree-46/R2 versus A2A-e degree-32/R3 split, exact present 256-bit STC/CTS scalings, qDiff metadata correction, nil iterations, h-tilde 32, same-valued independently constructed N16 parameter sets, and an actual-dispatch observed ModUp/Trace prerequisite. This accepts design only.
  - Completed the author-green canonical raw/prepared implementation candidate. The private constructor independently builds both exact Gao-compatible N16 parameter values, freezes raw JSON diagnostic `96ae3abc...fd2661`, prepared seal `f5724cc3...e3f7b6`, and all eight typed raw/effective STC/CTS literal/scaling identities. A 43-case mutation matrix covers both CKKS parameter roles, h-tilde, both circuit orders, nil/non-nil iterations, every DFT structural field, nil/value/precision/mode/accuracy scaling drift for both transforms, and every Mod1 literal field. Focused count-20 (`107.080s`), package, build, vet, format and diff gates pass. Candidate production/test hashes are `F40B9243...55AA20` and `28A4D62D...19C6C`; independent code audits are active, so no Authority, artifact, keygen, L11 allocation or HE result is promoted.
  - Two independent audits of the first canonical-constructor candidate returned REVISE at 0C/2M/4m. The actual raw passed to production preparation was not the raw used by the immutability assertion, so project-level caller/accessor/iteration/polynomial detachment remained unproved. The CTS expected scaling also reused prepared `QDiff`, creating a shared-oracle test that could follow a vendor derivation error. Minor findings requested prepare-twice, a direct resident degree-3 fingerprint, sign/nil-empty/invalid-order mutations, and checked narrowing for all typed identity fields.
  - Repaired every canonical-constructor audit finding. Direct preparation now proves the same caller raw is unchanged; caller, raw/effective accessor, iteration-slice, interval and polynomial-coefficient mutations cannot change sealed state; two independent canonical preparations have identical seals and eight identities. `QDiff`, qDiv, STC base and CTS base are independently recomputed from raw `Q0` and literals, compared with complete `big.Float` metadata, and explicitly distinguish rounded scalar one from nontrivial `1/qDiff`. The mutation matrix now has 53 cases plus seven narrowing cases, and the resident degree-3 compatibility payload has test-owned fingerprint `d40394a8...f9a16`. Focused count-20 (`116.952s`), package, build, vet, format and diff gates pass. Repaired production/test hashes are `9F8D0E06...F5BEA7` and `4BA17878...975DFF`; two fresh audits are active, with no Authority or HE promotion.
  - Independently accepted the repaired canonical Route-B constructor through two fresh final audits, each 0C/0M/0m, on production/test hashes `9F8D0E06...F5BEA7` and `4BA17878...975DFF`. Both auditors reconstructed the direct detachment attacks, raw-Q0 qDiff oracle, exact effective-scaling metadata, repeated preparation, eight typed identities, resident compatibility fingerprint, complete mutation matrix and narrowing gates. This closes unique configuration identity only; runtime zero-dispatch, artifact lineage, key generation, MR0 and HE remain separate gates.
  - Froze the observed ModUp/Trace implementation contract needed before MR0. The proposed two-method vendor seam shares stock private cores, records `begin` immediately before and `complete` immediately after each real Automorphism, preserves stock error/panic/alias behavior, recovers panic only at observed entries, and freezes the L11 runtime order `[122881,114689,98305,65537]` separately from the sorted capacity set. The design SHA is `BE062EA9...DD2C54`; independent design audit is active and no vendor code has been changed yet.
  - The first observed ModUp/Trace design audit returned REVISE at 0C/1M/0m. An untagged `uint64 exponent` could not represent the standard-ring `logN=0` order-two element, which is dispatched directly as `NthRoot-1`, and the report lacked ring type to distinguish the Standard versus conjugate-invariant success shapes. The repair adds a tagged ordinary exponent, explicit ring type, exact order-two encoding, and forces a recovered nested Trace panic to yield nil from `ModUpObserved`.
  - Independently accepted the repaired observed ModUp/Trace contract at 0C/0M/0m on SHA `8FED1461...650363`. It freezes only two observed entries, shared stock private cores, invocation-local real-dispatch prefix recording, Standard/CI `logN=0` validation, L11 source order, stock-compatible error/panic/alias/NTT semantics, defensive reports and terminal Route-B discard. Core-RLWE and bootstrap implementation owners are now active in separate files; no vendor implementation or HE claim is accepted yet.

  - Accepted the repaired RBDFT type-`0x01/0x02` streaming slice at 0C/0M/0m on unchanged production hash `F6F074F4...52C4F` and repaired test hash `99D26D8C...E8199`. The successful small observed construction now carries a test-side independent five-factor ledger that recomputes exact scalar, ring-polynomial and wire framing bytes without production record/count helpers, freezes STC `(1982,2689)/(4378,3537)` and CTS `(2202,2117)/(2502,3101)/(2502,3101)`, and agrees with vendor trace plus promoted evidence. Source-after-flush, payload-flush and physical-overrun faults all physically cross the prefix into `WriteTo`. Focused count-20, complete `secureeval`, vet and format gates pass. This accepts factor streaming only; live capacity authority, artifact minting, L11 construction and HE remain open.

  - Accepted the live-capacity v4 design at 0C/0M/0m on auth/two-stage hashes `BD7332CF...B1C4E` and `16447073...A898`; the frozen audit is `EAC61047...3D0A`. Four immutable builder sizes are now distinct from sample-derived remaining and saturating duplicate excess, and `AuthorizeBuild`, `BeginBuild`, `AuthorizeReady`, install and first-operation preflight each obtain a new Authority-owned OS sample. Windows/Linux sources, one-shot owner/generation capability, non-adjacent interleaved lineage generations, package-private raw-total testing, `readying` publication and CAS-loser side-effect counters are exact. Both first-audit Majors plus the fresh concurrency/publication findings are closed. No live Authority or allocation has run yet.
  - Implemented and accepted the first live Authority slice at 0C/0M/0m. Dynamic RBAUTH wire/test hashes are `8ACD7109...14B24`/`956978FF...22D2E`; common/Linux/Windows/unsupported sampler hashes are `971C229F...823827`, `46962190...92678`, `8B5C567B...FC14`, and `0E73C645...C2AA`; Authority/test hashes are `FC90E111...C23175`/`EFD38F07...F34837A`. The zero-input production constructor samples physical memory internally, reserves a globally newer one-shot generation before each attempt, evaluates and revalidates the exact L11 capacity evidence, prepares only after admission, and returns a payload-free owner-bound permit whose report remains inert. Dynamic audit/alternate/threshold arithmetic, bounded Linux parsing, one-call Windows sampling, typed-nil, replay/foreign owner, copied-handle concurrency, failure consumption, generation exhaustion, AST gate order and a successful real-host authorization pass focused count-20, full package, vet, format and Linux compile gates. The frozen implementation audit is `research/phase3_design/secure_n16_route_b_live_authority_implementation_audit_2026-09-01.md`; `BeginBuild`, L11 factor allocation, receipt, install, MR0 and RSS remain pending.
  - Reached the author-green small-profile `BeginBuild` gate. `route_b_artifact.go` (`C5474629...17EE9`) performs the real observed 2+3 factor sequence, mints and cross-validates the complete RBDFT type-03–07 record set, enforces `0/0/0/2`, and discards every partial product on error. `route_b_live_artifact.go` (`050ED661...A9912`) adds fresh-capacity/Prepare-before-CAS use, runtime-capacity evidence, terminal failure, inert live receipt and opaque atomic-availability artifact; `route_b_authority.go` has the moving integration hash `C117A762...48B64`. Platform peak-RSS sampling uses process lifetime high-water counters, suitable for the later isolated child. Tamper/foreign/replay, capacity-block, error/panic, concurrent-copy, public-surface and AST-order tests pass count-20; full package, vet, format and Linux compile pass. This is not yet promoted: the canonical L11 builder has not run, the current artifact is not retained through Ready/install/MR0, and no isolated RSS or HE result exists.

## Test Results

| Test | Input | Expected | Actual | Status |
|---|---|---|---|---|
| Planning session catch-up | Codex bundled Python + repository path | Exit successfully and report any unsynced state | Exited successfully with no unsynced-state report | PASS |
| Graphify graph build | 248 code files + 12 documents | Non-empty graph, report, and HTML visualization | 3,559 nodes; 8,181 built edges; all three outputs created | PASS WITH HEALTH WARNINGS |
| Baseline Go packages | `go test ./...` | Establish current compile/test state | All five packages compile; every package reports `[no test files]` | PASS (compile only) |
| Integer/tree package suite | `go test -count=1 ./integer/...` | Every TDD package passes from a cold uncached run | evaluator, homchain, treecompile, treeeval, treeplan, and z2n all passed | PASS |
| Repository package suite | `go test -count=1 ./...` | Existing packages remain buildable and new tests pass | Existing five packages compile; all six new test packages pass | PASS |
| Integer/tree static analysis | `go vet ./integer/...` | No reported vet finding | No output, exit 0 | PASS |
| Integer/tree formatting | `gofmt -d integer/...` | No formatting diff | No output | PASS |
| Gao canonicalization epsilon boundary | `go test ./integer/z2n -run TestCanonicalizeArithmeticMatchesUpstreamEpsilonBoundary -count=10` | For n=8/16/32/64, coefficients at and below `2^(-n-1)` remain on the positive edge and values immediately above it wrap below zero | All registered widths and boundary cases passed in ten repetitions | PASS |
| Triangle-ring repeated regression | `go test ./integer/z2n -count=10` | All exact arithmetic, roots, and encoding tests remain stable after epsilon correction | Ten full repetitions passed | PASS |
| Encrypted arithmetic regression | `go test ./integer/evaluator -count=1` | MultShort/MultFull and codec diagnostics remain valid under the corrected plaintext oracle | Full evaluator package passed | PASS |
| Stage-1 closure repository suite | `go test -count=1 ./...` | Existing packages and all exploratory integer/tree packages pass after audit fixes | Five original packages compiled; evaluator, homchain, treecompile, treeeval, treeplan, and z2n passed | PASS |
| Stage-1 closure static checks | `go vet ./integer/...`; `gofmt -d`; `git diff --check` | No analyzer, formatting, or whitespace error | All commands exit 0; only Git's informational LF-to-CRLF warning appears | PASS |
| Stage-1 evidence schemas | bundled Python + PyYAML/jsonschema | Both claim manifests validate; every research JSON/YAML parses | 2 claim manifests, 12 JSON files, and 43 YAML files passed | PASS |
| Stage-1 DA2 final rerun | Independent read-only reviewer | Close every prior Critical/Major and accurately bound search/novelty claims | First pass 0C/1M/3m; all fixed; targeted rerun 0C/0M/0m | PASS |
| Stage-1 artifact manifest | 26 formal research/design/audit files | Every listed SHA-256 matches and aggregate hash is stable | 26/26 match; manifest SHA-256 `b2b7d5...f3c1f3ea` | PASS |
| Material Passport `repro_lock` | ARS `check_repro_lock.py` | Explicit opt-out is accepted with required warning | Exit 0, `OK (with WARN)` for `repro_lock: null` | PASS WITH WARN |
| Stage-2 Galois error contract | `go test -count=1 ./integer/homchain` | Invalid `TransformSpec` reports an error rather than silently returning no keys | Intentional red compile mismatch on old API; green after signature and callers were updated | PASS |
| Stage-2 coefficient significance contract | `go test -count=1 ./integer/z2n` | Safe mixed-precision inputs pass; coefficients whose magnitude erases the canonical epsilon fail per coefficient | Intentional red at n=8/16/32/64; full package green after constructor/validator bound | PASS |
| Stage-2 A2A-I plaintext overflow oracle | `go test -count=1 ./integer/z2n -run TestArithmeticIntegralOverflowPreservesResidueAndCanonicalizes` | Adding integral polynomial `(3,-2,1,0,...)` preserves all tested residues and canonicalizes to the same coefficient representative as the no-overflow input | Initial test oracle incorrectly compared with non-canonical raw encoding and failed at `0x10`; corrected canonical target passes | PASS |
| Ordered float32 R0 compiler view | `go test -count=1 ./integer/treecompile`; `go vet ./integer/treecompile`; `gofmt -d` | Finite float routes map exactly to unsigned keys; signed zero is canonical; non-finite values fail; source does not alias output | Full package, vet, and formatting passed; fixed-seed 100k pair property test passed | PASS |
| Gao Boolean plaintext conformance oracle | `go test -count=1 ./integer/z2n`; `go vet ./integer/z2n`; `gofmt -d` | Exact w=4 LUT values, audited n=8 bit halves, all 256 B2A round trips, strict direct B-A2B preconditions/order | Full package, vet, and formatting passed | PASS |
| Guarded A2A-I vertical slice | `go test ./integer/homchain -run 'A2AI|RawFullSlotDFT' -count=5`; repeated independent Standards+Spec audits; vet/gofmt/diff-check | Opaque normal-V/U circuit, exact admission/state/profile binding, private main+DFT execution graph, 192-bit root/matrix generation and encoding, numeric-payload witness, key/evaluator swap fail-close, residue/canonicalization trace, input immutability | The later DFT-precision reopening found 53-bit generation and rejected the first metadata-self-proving repair; corrected numeric witness `479964f8...f5fd8aa`, raw DFT `a18c6f92...581c196`, and circuit `062f533b...f82c64` passed independent 0C/0M/0m | PASS (FUNCTIONAL-NOT-SECURE ADAPTATION) |
| R0 schedule audit first pass | Independent Standards/Spec review | Four typed schedules plus faithful source control, proof gate, full tuple, fail-closed accounting | 0 Critical / 4 Major / 3 Minor | REVISE |
| R0 schedule stable rerun | Independent Standards/Spec review; `go test -count=5 ./integer/treeplan`; vet/gofmt/diff-check | Close all first-pass and follow-up binding/measurement findings | 0 Critical / 0 Major / 0 Minor; all package checks clean | PASS |
| Stage-2 schedule regression | `go test -count=3 ./integer/treeplan`; `go test -count=1 ./integer/...`; `go vet ./integer/...`; `gofmt -d`; `git diff --check` | Repaired planner remains deterministic and does not break integer packages | All tests and checks passed; only informational Git LF/CRLF warnings | PASS |
| Standalone B2A functional slice | `go test ./integer/homchain -run '^TestB2AFull' -count=5`; 256-word exhaustive oracle; vet/gofmt/diff-check; independent audit/rerun | Fused-`t^-1` C2Z path matches raw ArithmeticEncode coefficients/residues, exact state/profile, key preflight, input immutability, and source-distinguishing controls | Initial function gate passed; first audit 0C/2M/0m; profile-v2 encoder/exact-scale repair passed count=5 and stable rerun 0C/0M/0m; max observed errors remain below `1.4e-7` slots, `4.1e-8` coefficients, `7.2e-8` bits with rounding margin `0.5` | PASS |
| A2B-chain B2A seam | `go test ./integer/homchain -run '^TestB2AFull' -count=5`; real two-kernel composition; `go vet ./integer/homchain`; gofmt/diff-check; independent Standards+Spec re-audit | Consume both full-packed Boolean halves at physical level 5 and exact S35, then emit the arithmetic word at level 4 and the same exact scale without mutating either input | First audit 0C/3M/1m; exact parameter, numeric transform/compiled-plan, post-bind key identity, and builder-drift repairs closed every finding. Stable re-audit 0C/0M/0m; chain profile `01d138a6...c168588a3`. | PASS |
| R0 same-feature opportunity scan | `& research/scripts/scan_same_feature_fusion.ps1 -ModelDirectory xgbdata` | Validate raw tree arrays and quantify necessary same-feature fusion structures without treating them as proof or speed evidence | D=8/10/12: 5/13/26 same-feature edges, maximum run 2, and 0 complete uniform height-2/3/4 roots; all model hashes frozen | PASS / HYPOTHESIS DEMOTED |
| Gao isolated sine + 3DA | targeted count=5; packed-vs-scalar rounding sentinel; independent second rerun; vet/gofmt/diff-check | Exact 33-coefficient Chebyshev import, c0/2 once, full-slot 256-bit coefficient/constant dispatch, three exact DA rounds, natural scale, `[9,3,2,1,0]`, input/key/provenance gates | Q35x10/S35/P60; scalar analytic max `7.71e-11`; latest encrypted five-run max `1.53e-8..1.07e-7`; scalar53 sentinel `2.667e-17`, packed256 zero; independent 0C/0M/0m | PASS ISOLATED FUNCTIONAL KERNEL |
| Gao A2B p=16 ID/MSB coefficient freeze | dependency-free C++ replay; artifact byte comparison; `go test ./integer/homchain -run '^TestGaoA2BLUT' -count=5`; vet/diff-check | Exact upstream double/libm-to-`2^128` conversion, immutable 256-bit Lattigo monomial profiles, all 16 source points, no runtime regeneration | `-O0/-O2` identical; table digest `f3aa5c55...1564e3`; max source-grid error below `6.7e-16` in C++ and below `1e-14` in independent Go check | PASS PREREQUISITE / ENCRYPTED A2B PENDING |
| Gao A2B exp46 coefficient/oracle prerequisite | extractor digest check; `go test ./integer/homchain -run '^TestGaoA2BExp' -count=5`; independent audit; full repository tests; vet/gofmt/diff-check | Exact 47-term `2^-128` import, c0/2 once, normalized Chebyshev variable, two complex squares, defensive copies, source-distinguishing traps | Artifact `a960edc1...e36e4d`; import `d33c80bc...001f5d`; profile `9a6d918d...1c379`; 345 probes max base/final `1.365e-9`/`5.459e-9`; isolated prerequisite audit 0C/0M/0m. Live refresh fixed the composition input to `y=(I+m/16)/16`; integer-lift integration remains pending. | PASS ARTIFACT/ORACLE; ENCRYPTED COMPOSITION PENDING |
| Gao isolated encrypted A2B exp/two-LUT kernel v3 | kernel/direct-B2A/refresh all-256 targeted `count=5`; homchain package `count=5`; vet/gofmt; independent repair audit | No extra `/16`; packed-256 exp46; two squares; one shared-basis ID/MSB call targeting sealed exact S35; all code/lift points; exact state/key/profile; real B2A composition | Profile `44471389...3986519e`; ID/MSB `L5/S35`, `ID/16` `L4/S35`; levels `[17,11,10,9,5,5,5,5]`; package count-five, direct B2A and refresh all-256 pass; independent 0C/0M/0m | PASS ACCEPTED ISOLATED FUNCTIONAL KERNEL; SERIAL A2B PENDING |
| Direct rank-one sign fusion | `go test ./integer/homchain -run '^TestSignFusion' -count=5`; homchain count-one; vet/gofmt/diff-check; independent read-only audit | Convert only high-half `b7` at L5/S35 to an arithmetic sign word at L4/S35 with a sealed fused transform, block isolation, real-kernel composition, and fail-closed provenance/key gates | Profile `68b3bcc5...74a4ae`; exact trace `L5/S35 -> L5/0x1.fffffe904p+69 -> L4/S35`; oracle/generic-control/real-composition maxima `2.71e-9`/`6.23e-9`/`2.00e-7`; independent 0C/0M/0m. Encrypted-isolation performance baseline remains deferred and no speed claim is made. | PASS FUNCTIONAL FUSION |
| Fixed serial full-packed A2B | all-256 source-residual oracle; real refresh/kernel/B2A composition; exact 24-state trace; targeted count-five, package/repository tests, vet/gofmt; independent repair audit | Reproduce the fixed Gao `n=8,w=4,d=2` low-then-high residual schedule with two guarded refreshes and two shared-basis kernels under one exact keyset | Profile `d9c156fb...3dfc90`; second STC raw/execution/profile `b40ce04f...a854e` / `6381e519...95ae0` / `b4d1cb37...5b882`; all 256 messages over five runs have zero bit failures and independent audit closed at 0C/0M/0m. Scope remains dense/no-switch and `NormalizedInputCertified=false`. | PASS FIXED FUNCTIONAL A2B |
| Signed-int8 no-overflow comparator | complete `^TestSigned8` gate with `-timeout 15m`; independent modulo-256/bit7 oracle; trusted-origin/payload/key/graph negatives; frozen-pair re-audit | Compute `1-b7` for a proved no-overflow `x-t`, support public CT--PT and opaque CT--CT thresholds, and reject every detectable admission/graph/key mutation before `SubNew` | Final gate 254.030s; production/test SHA256 `A8E7061...EF7FB` / `35EAB715...62FDA`; public/opaque profiles `5075bdf6...9710bc` / `0d62c312...e2ee63`; initial audit 1C/4M/0m, repaired frozen-pair re-audit 0C/0M/0m. Trusted declared origin is explicit and non-cryptographic. | PASS FUNCTIONAL COMPARATOR |
| Signed-int8 T0 root tree | exhaustive algebra; public+opaque all-pairs encrypted gate; full fail-closed wrapper matrix; exact source-scheduled integral-lift oracle; final-hash focused gate, complete count1 and frozen count5 | One encrypted signed-int8 root comparison plus corrected-delta integer-leaf selection, with exact zero output/trace on every detectable graph/key/profile/input mutation | Frozen-hash complete count1 PASS 358.425s after focused M1/M2+minor PASS 91.594s; fresh independent audit accepted 0C/0M/0m. The same three hashes then passed targeted count-five in 1833.571s and matched exactly after execution: `0998...EEA`, `8449...D10`, `07C8...3B0D`. | PASS INDEPENDENTLY ACCEPTED BOUNDED T0 |
| Periodic selector reraiser | 256-bit all-16 oracle; public+opaque ciphertext gate; exact 19-state/operation/byte/key ledgers; real scalar-leaf bridge; true 27-case zero-op matrix; repeated independent audits; frozen count-five | Convert L4 arithmetic-root branches to L8 repeated ordinary scalar selectors without legacy decrypt-round-reencrypt | Initial 0C/2M/0m audit required serialized-byte evidence and exact same-map cache sealing; repair audit 0C/0M/1m exposed two missing nil regressions; final audit and contract review are 0C/0M/0m. Online bytes 2,942/5,054, retained local evidence 49,408; full count-five PASS 589.760s; hashes `06123A...6E4D3`, `218EC8...78ED5`, `EEDF08...97BDC6`. | PASS INDEPENDENTLY ACCEPTED FUNCTIONAL SELECTOR / NO MULTI-DEPTH OR SPEED CLAIM |
| Integer/radix candidate audit | source model scan, LCPDTE physical packing review, matched serial/B-A2B accounting, Boolean level analysis | Retain only candidates with exact semantics or explicit R1 model change and an executable physical break-even path | Old `12D vs 16D` withdrawn. Candidate A has 40/55/72 prefix classes vs 64/80/96 streams but D bootstraps; candidate C is conditional on fused nibble LUTs, L4 unary output, oblivious thresholds and one bundle reraiser. | PASS DESIGN AUDIT / NO SPEED CLAIM |
| Sage/lattice-estimator environment | pinned micromamba SHA gate; 389-package SHA-256 explicit lock; Sage version record; pinned-estimator rough smoke | Remove the missing-Sage environment blocker while preserving the exact parameter-conversion and transcript gates | Commit `048ac14` tracks and authenticates the LF policy, Sage 10.9 record, 389-package explicit lock, estimator HEAD and exact smoke output. A fresh independent audit reran syntax, normal install, live URL+SHA lock export, existing-prefix refresh guard and LF/CRLF plus malformed-version controls and closed at 0C/0M/0m. | PASS ENVIRONMENT READY |
| Exact Lattigo Gao-compatible parameter candidate | deterministic double generation; exact prime/product recomputation; NTT congruence and primality tests; authenticated canonical JSON; fresh CLI and homchain compatibility audit | Materialize the local `LogN=16`, scale-43, H/h=192/32 candidate before estimator conversion or secure-profile circuit work | Lattigo targets `LogQ=[43x21]`, `LogP=[50x7]`; actual products are Q=904 bits, P=351 bits, QP=1254 bits. Manifest digest `e2dc7b14...8b3f3d`, file SHA256 `4CA0E01E...542882`. A fresh independent audit recomputed both candidate and functional-negative chains/products/digests, reproduced two byte-identical emissions per tuple, verified refusal to overwrite and exact equality with the accepted functional A2B parameters, and closed 0C/0M/0m. Key inventory, finite sample exposure and secure circuit acceptance remain missing. | PASS INDEPENDENTLY ACCEPTED PARAMETER MANIFEST / IMPLEMENTATION+APPLICATION SECURITY PENDING |
| Route-B zero-HE precision construction permit | 49 resealed-field mutations plus raw/effective swaps, stale/foreign evidence, precision/schedule/ownership/peak/promotion controls; focused/full package tests; vet/gofmt; independent audit | Authorize only the exact 256/256-bit prebuilt-factor construction semantics after independently rebuilding the accepted L11 capacity evidence, before any provider/builder/encoder stage | Production/test SHA256 `EBB46E...CF267` / `D4962E...1727`; invalid paths call provider/builder/encoder `0/0/0`, valid path `1/1/1`, stock/default constructor count `0`; guarded requirement `7,842,848,076` B, margin `2,585,547,267` B, forbidden duplicate excess `55,815,677` B; independent 0C/0M/0m. | PASS PURE CONSTRUCTION CONTRACT / FACTORS+HE+RSS+SECURITY PENDING |
| Exact CKKS estimator conversion and sensitivity | self-test; two isolated real runs; strict repeat; exact Q/QP and distribution replay; 386 converter, 3 manifest and 23 metadata in-memory tamper probes; before/after artifact freeze; fresh independent audit | Produce a deterministic exact-modulus classical/quantum sensitivity transcript without promoting missing finite-exposure evidence | Candidate minima 150.672 classical and 136.740 quantum bits; functional negative control 11.680/10.600 bits and `FAIL`; transcript SHA256 `690a66c...db02`, complete-result digest `87e176b...4daf`, determinism digest `f698300...82fc`; final review 0C/0M/0m. | PASS EXACT UNLIMITED-SAMPLE SENSITIVITY / APPLICATION SECURITY INCONCLUSIVE |
| Exact LogN=16 secure-profile adapter | red/green exact-candidate admission; drift negatives; digest/capacity/scale/topology assertions; `count=1`, `count=5`, vet/gofmt; fresh read-only audit | Create a deep parameter-profile seam without loosening functional circuits or claiming secure execution | Production/test SHA256 `CB2FCB...5ECB2` / `D841CD...110FC2`; parameter/profile digests `c37129...a816` / `7e4f93...99b89`; 8,192 words; exact correction `8796093022208/8796090007553`; independent 0C/0M/0m. | PASS PARAMETER ADAPTER / CIRCUIT AND APPLICATION SECURITY UNVERIFIED |
| Route-B DFT construction counters/private cores | five public-entry matrix, failed-entry ordering, direct raw calls, saturation/non-monotonic delta controls; focused suite, vet/gofmt/diff-check; independent read-only audit | Make default, explicit, raw-numeric and future observed construction mutually exclusive and process-observable | All five current public entries increment exactly one field before validation and call only private cores; direct raw calls are `0/0/1/0`; independent audit 0C/0M/0m with frozen hashes `EEC368...5ECA`, `F6B222...C077`, `702809...023D`. | PASS SUPPORT ENFORCEMENT / OBSERVED BUILD PENDING |
| Depth-2 terminal float64 leaf mux | exact progress-ledger units; pre-op/decoder/local partial and post-op mutation matrices; resealed alias/foreign-output attacks; fresh public and opaque encrypted count-one; real production build; repeated independent audits | Close the authenticated four-leaf mux while preserving honest attempted/completed evidence and standalone production compilation | Shared retained-prefix validation and result/output binding reject both counterexamples; public/opaque/static gates pass in 944.120s/112.014s/327.793s; a test-only-helper build defect was repaired without semantic drift; final hashes `E33D7D...30C9`, `1BF2F6...20735`, `9B281B...A8028`; fresh audit 0C/0M/0m. | PASS FUNCTIONAL / COMPLETE ORCHESTRATION AND SECURITY PENDING |
| Pure bootstrap parameter preparation | exact frozen legacy scale/seal golden; detach/CI/nil-empty/internal-drift/invalid controls; orthogonal float-metadata fixtures; stock tiny constructor; CI shallow-copy RED/GREEN; counter delta; focused tests, vet and fresh audit | Separate pure raw/effective/Mod1 preparation from matrix/evaluator construction and route stock construction through it | `PrepareParameters` delta `0/0/0/0`; stock tiny constructor delta `2/0/0/0`; invalid-input panic paths repaired; final hashes `E6CB5F...01EC4`, `0017FC...CA40`, `0EDA41...9648`; audit 0C/0M/0m. | PASS PURE PREPARATION / PREBUILT ASSEMBLY PENDING |
| Pure RBAUTH-v1 codec | four independent golden encoders; canonical RBDFT type-05/type-07 fixtures; cross-domain identities; framing/reseal matrices; nullable-float boundaries; bounded deep-body fuzz; build/vet/fresh audit | Freeze inert Build/Ready wire records without granting live authority or relying on placeholder RBDFT hashes | Pair `210 B/f33c...282fe`, receipt `585 B/aae3...8913a`; production/test hashes `2D528E...D517F`/`4F52A9...D074FF`; 77,327 fuzz executions and fresh audit 0C/0M/0m. | PASS PURE WIRE / AUTHORITY+ARTIFACT PENDING |

## Error Log

| Timestamp | Error | Attempt | Resolution |
|---|---|---:|---|
| 2026-08-29 | `python` resolved to Microsoft Store alias; interpreter unavailable | 1 | Loaded workspace dependencies and invoked `session-catchup.py` with the bundled Python runtime. |
| 2026-08-29 | Inline Graphify detection script failed with a Python `SyntaxError` after PowerShell mangled nested quoting | 1 | Replaced the quoting strategy and reran successfully. |
| 2026-08-29 | Combined metadata-save and cleanup command was rejected before execution | 1 | Split into a non-destructive save and a second cleanup over ten validated explicit files. |
| 2026-08-29 | PDF download succeeded, but the follow-up size listing used `Get-Item -LiteralPath '*.pdf'`, which rejects wildcards | 1 | Re-ran the read-only listing with `Get-ChildItem -Filter '*.pdf'`; both PDF hashes and sizes were preserved and verified. |
| 2026-08-29 | `rg` was passed a Windows wildcard path (`research/sources/*.txt`), which the executable treated as an invalid literal path | 1 | Re-ran `rg` against the directory with `-g '*.txt'`; both reference sections were extracted successfully. |
| 2026-08-29 | Two additional `rg` calls used literal wildcard path arguments (`*.go` and `z-*.cpp`) on Windows | 1 | Replaced path globs with directory operands plus `-g` filters; subsequent source searches succeeded. |
| 2026-08-29 | Direct DOI and Crossref API pages were blocked by resolver 403/safe-URL checks in the browser tool | 1 | Retained exact DOI evidence from primary reference lists and scheduled a read-only Crossref metadata capture; no DOI was marked invalid because of the transport failure. |
| 2026-08-29 | Monitoring setup first requested `read_thread` with `turnLimit=12`, above the tool maximum of 10 | 1 | Switched to a compact `wait_threads` snapshot for current progress and retained `read_thread` only for bounded history inspection. |
| 2026-08-29 | First automation-metadata patch targeted a `progress.md` heading while still inside the `task_plan.md` hunk | 1 | Reissued the edit with explicit per-file patch hunks; no prior content was changed by the failed attempt. |
| 2026-08-29 | Full integer test runs observed missing `EncodeShortEncrypt`, `MultShort`, `MultFull`, and evaluation-key symbols while the multiplication owner was between the TDD red and green writes | 1 | Treated the failures as a bounded parallel-edit window; reran after the production API landed and obtained a clean full-package pass. |
| 2026-08-29 | Bundled Python lacked PyYAML/jsonschema, and a Node fallback lacked a YAML module | 2 | Installed pinned validators into an untracked workspace-temporary target, validated the claim-intent manifest, and excluded that temporary dependency directory from deliverables. |
| 2026-08-29 | A later claim-manifest validation retried the unavailable Windows `python` alias | 1 | Re-ran the same read-only validator with the bundled Codex Python runtime and obtained schema PASS for both manifests. |
| 2026-08-29 | A one-line documentation-count audit used a Python regex whose backtick character was consumed by PowerShell, yielding an unterminated character class | 1 | Replaced the fragile inline regex with native PowerShell line filters; confirmed 25 unique annotations, 96 unique queries, and matched reference/anchor counts. |
| 2026-08-29 | The first `check_claim_audit_consistency.py --help` invocation omitted the temporary validator path and could not import `jsonschema` | 1 | Set `PYTHONPATH` to the ignored `tmp/ars_pydeps` directory and confirmed the CLI contract; the Stage-4 claim-audit lint was not misrepresented as a Stage-1 gate. |
| 2026-08-29 | Policy rejected a guarded recursive removal of the temporary validator directory before process creation | 1 | Left the generated temporary directory untracked and out of all scoped commits; no user or repository file was deleted. |
| 2026-08-30 | A source search again passed Windows wildcard path operands for Gao `z-fhe` files to `rg` | 1 | Re-ran against the source directory with `-g` filters and obtained the exact parameter/setup anchors. |
| 2026-08-30 | An in-progress A2A-I contract search again passed `integer/homchain/a2ai*` as a Windows path operand to `rg` | 2 | Re-ran against `integer/homchain` with `-g 'a2ai*.go' -g 'dft.go'`; no file was changed by either failed read-only search. |
| 2026-08-30 | The first B2A profile-v2 negative-test patch left an obsolete `low` binding after moving scale rejection from `BindInput` to `BindLow`/`BindHigh` | 1 | Removed the unused binding, reran the intended red digest pin, then passed the complete B2A gate five times. |
| 2026-08-30 | Exploratory reads guessed nonexistent `integer/treecompile/compiler.go` and `integer/treeplan/{compile,model,types}.go` paths | 1 | Enumerated the packages with `rg --files` and read the actual `compile.go`, `binary.go`, `multiway.go`, and `radix.go` files; no file was changed by the failed reads. |
| 2026-08-30 | An optional row-by-row PowerShell route-frequency scan exceeded the 30-second command window and yielded without a retained session identifier | 1 | Identified its exact high-CPU child process by start time and stopped PID 2400; retained the completed structural scan and deferred route-frequency measurement to a compiled experiment tool. |
| 2026-08-30 | An exploratory Lattigo source read guessed a nonexistent top-level `lattigo/` directory | 1 | Located the effective fork under `vendor/github.com/tuneinsight/lattigo/v6` with `rg --files` and inspected the actual polynomial evaluator, simulation, and power-basis sources. |
| 2026-08-30 | A sine-file status search again passed the Windows wildcard path `integer/homchain/sine_kernel*` directly to `rg` | 3 | Re-ran against the package directory with `-g 'sine_kernel*.go'`; the failed read changed no files. |
| 2026-08-30 | A Lattigo DFT inspection guessed `vendor/.../dft/dft_evaluator.go`, which does not exist in this vendored version | 1 | Located the implementation with `rg`; the evaluator and matrix code are in `vendor/.../dft/dft.go`. No file changed. |
| 2026-08-30 | A prebuilt-assembler source search again passed a Windows `*.go` wildcard as a literal `rg` path operand | 1 | Re-ran all subsequent searches against the bootstrapping directory without path globs and inspected the actual evaluator/key files; the failed read changed no files. |
| 2026-08-30 | An RBDFT grammar search embedded Markdown backticks in a PowerShell `rg` pattern, so PowerShell rewrote the pattern and produced an overbroad repository-wide result | 1 | Used the directly bounded design-file excerpt as the authoritative read and stopped using backticks in shell search patterns; no file changed. |
| 2026-08-31 | An authority-API inventory search used one compound `rg` expression with an unbalanced grouping parenthesis | 1 | Re-ran the read-only inventory with separate `-e` patterns; it returned the intended L11 profile/shape/plan/report/permit APIs and changed no files. |
| 2026-08-31 | The first type-01/type-02 design-map report paired wire value `format=1` with the wrong symbol `RepackImagAsReal` even though Lattigo defines `SplitRealAndImag=1` and `RepackImagAsReal=2` | 1 | Cross-checked the enum and both frozen Route-B documents, required a focused correction, and obtained a corrected constants table; no production code had consumed the report. |
| 2026-08-31 | A follow-up Route-B search again passed Windows wildcard path operands (`research/phase3_design/*.md` and `integer/secureeval/*.go`) directly to `rg` | 1 | Re-ran against both directories with `--glob '*.md' --glob '*.go'`; the failed read was non-mutating. |
| 2026-08-31 | The first type-01/type-02 API inventory guessed nonexistent vendored `he/lintrans` and then `he` directories | 2 | Enumerated the vendored files and read the actual CKKS wrapper in `circuits/ckks/lintrans`, the common implementation in `circuits/common/lintrans`, and `ring/ringqp.Poly`; both failed reads were non-mutating. |
| 2026-08-31 | A tracking-file read guessed `pipeline_state.json` at the repository root | 1 | Located the tracked state at `research/pipeline_state.json` with `rg --files`; the failed read was non-mutating. |
| 2026-08-30 | The first A2B constant lookup guessed the implementation path `src/pke/lib/.../z-fhe-constants.h` | 1 | Located and hashed the authoritative header at `src/pke/include/scheme/ckksrns/z-fhe-constants.h`; the failed read was non-mutating. |
| 2026-08-30 | The first inline PowerShell exp46 extraction command embedded a backtick tab inside a JavaScript template and failed before execution | 1 | Replaced the tab escape with `[char]9`, then promoted the successful parser into `research/scripts/extract_a2b_exp46.ps1`. |
| 2026-08-30 | A follow-up Lattigo source read guessed the vendored major version as `v5` and the common LT filename as `linear_transformation.go` | 1 | Enumerated vendored files with `rg --files`; the active paths are under `vendor/github.com/tuneinsight/lattigo/v6`, with common LT code in `lintrans.go`. The failed reads were non-mutating. |
| 2026-08-30 | Another A2A-I test search passed `integer/homchain/a2ai*_test.go` as a literal Windows path | 1 | Re-ran against the package directory with `--glob '*_test.go'`; no file changed. |
| 2026-08-30 | The first targeted B2A-at-level RED run intersected a concurrent A2B refresh/kernel edit and failed on that worker-owned unfinished source before reaching the new missing API | 1 | Preserved both worker slices, retained the test-first B2A test, and deferred its isolated rerun until the package returned to a stable compile boundary. |
| 2026-08-30 | The first isolated exp46 kernel failure was briefly diagnosed as a missing post-refresh `/16` bridge | 1 | The concurrent live refresh trace showed CTS Scaling=`1/16` already yields `y=(I+m/16)/16`; withdrew the extra bridge, corrected the standalone test from `m/16` to `m/256`, and replaced deterministic decoding with integer-lattice plus lift-invariance gates. |
| 2026-08-30 | The first stopped refresh and isolated kernel were each green but not composable: refresh CTS ended at L17/S50 while kernel admission requires L17/S35 | 1 | Kept both bounded results, rejected any full-composition claim, and opened a TDD scale-alignment repair. The preferred candidate uses the standard `LogMessageRatio=15` relation for q0≈50/S35 plus q-level special-b0 matrix scaling so ScaleDown/ModUp/CTS preserve S35 semantically; an explicit unit-multiply/rescale bridge is only a charged fallback. |
| 2026-08-30 | The first Sage conda transaction extracted packages under WSL's `/mnt/d` DrvFS layer; one small package took nearly four minutes and projected hours of non-research work | 1 | Interrupted the incomplete transaction, verified both workspace paths, moved the partial ignored tree recoverably to `tmp/abandoned-sage-drvfs-20260830`, changed the task-specific default to native ext4 `/var/tmp/lcpdte-research-toolchains`, and completed all 389 packages. |
| 2026-08-30 | The first estimator smoke used obsolete imports/API, then three file-mode scripts attempted to derive the repository from Sage's `__file__`, which remains bound to `sage/all.py` | 5 | Switched to `LWE.Parameters` and `LWE.estimate.rough`; made the script resolve the verified working repository or task-specific override. The exact final command exits zero and normalized stdout matches the frozen record. |
| 2026-08-30 | A strict smoke-output comparison treated CRLF in the tracked record and LF from WSL as different despite identical text | 1 | Normalized only newline encoding before comparison; the content remains byte-recorded separately and the rerun matches exactly after normalization. |
| 2026-08-30 | The selector implementation twice exposed transient unused/undefined symbols while the T0 owner was running package compile/vet gates | 2 | Restored a compile-safe checkpoint, froze selector edits during the final T0 count1 window, and required the T0 gate to run on the post-format final hashes rather than accepting the earlier semantic run alone. |
| 2026-08-30 | The first fresh T0 re-audit subtask was assigned a read-only routing role and returned only a model-route recommendation, not the required C/M/m verdict | 1 | Launched the recommended fresh high-reasoning executor with the same frozen hashes; it completed the actual independent review at 0C/0M/0m. |
| 2026-08-31 | The first fresh RLWE `TraceObserved` audit was again assigned a router role and returned only a model/effort recommendation rather than inspecting code or issuing a C/M/m verdict | 1 | Preserved the routing response as non-evidence and immediately launched a fresh `codex_auto_model_executor_high` with the frozen hashes, normative design and explicit instruction to execute the read-only audit itself. |
| 2026-08-30 | The first Gao-compatible parameter test assumed every Lattigo `LogQ=b` prime has integer bit length exactly `b`; the alternating generator produced values immediately above `2^b` | 1 | Replaced the false assumption with exact prime recording plus the documented `b`/`b+1` generator invariant, NTT congruence, primality, product-bit and frozen-manifest checks. |
| 2026-08-30 | The independent parameter-manifest audit left four reproduced JSON files in a uniquely named system-temp directory; policy rejected the attempted recursive cleanup after the path and contents were verified | 1 | Left `C:\Users\26676\AppData\Local\Temp\lcpdte-security-audit-1022c4b98b8f4c9b9e5e90b01904c24b` intact. It contains only the four audit emissions, has no workspace effect, and can be removed manually. |
| 2026-08-30 | Successive estimator-conversion audits exposed incomplete runtime/repeat binding, consistently mutable nested result/toolchain fields, missing known-answer locks for returned attack fields, an unbound error-distribution dimension, permissive nested metadata schemas, and incomplete-result digest coverage | 6 | Repaired each boundary in sequence, retained every REVISE result in the pipeline history, then passed the fresh final read-only audit with all 386 converter, 3 manifest, and 23 metadata tamper probes rejected and all 17 scoped artifacts byte/mtime stable. |

### Monitoring and writing sidecar

- Located the active main task and captured its compact progress cursor/revision.
- Confirmed Stage 1 evidence closure and selected an evidence-gated drafting policy.
- Loaded ARS academic-pipeline, Nature Writing, anti-defensive writing, and HERO proportionality constraints for the monitoring/writing workflow.
- The system Python aliases were unavailable during sidecar recovery; the Codex bundled Python completed the catch-up check successfully.
- Created active heartbeat automation `ckks` for evidence-aware manuscript synchronization at 10-minute intervals; unchanged runs remain silent and the final run pauses after the source task completes.
- Created and checked the Chinese rolling technical report, English manuscript scaffold, claim–evidence matrix, terminology ledger, writing state and heartbeat monitor state under `research/manuscript/`.
- Corrected the multiplication diagnostic terminology to the production field `MaxRootError`; removed the ambiguous “root residual” label from manuscript evidence surfaces.
- Validated `monitor_state.json` with the bundled Python runtime and retained explicit placeholders for all unsupported performance, security and end-to-end claims.
- Processed source-task revision 177 immediately: added the live-audit override and downgraded affected claim-matrix rows after the canonicalizer epsilon and typed-tree issues were discovered; later heartbeat runs will promote them only after fresh regressions pass.

### 2026-08-31 Route-B live-authority capacity audit opened

- Re-read the accepted `RBAUTH-v1` wire contract against the live-authorizer reconstruction sequence before writing Authority code.
- Found a candidate inconsistency: the wire's frozen peak ledger embeds the audit fixture's `2,585,547,267`-byte remaining margin and `55,815,677`-byte duplicate excess, but live authorization is specified to reconstruct capacity from an explicit current snapshot whose available bytes can legitimately differ.
- Opened an independent-design-audit prerequisite. No fixed fixture will be presented as current runtime evidence, and no live Authority will be implemented or promoted until the invariant is shown sound or repaired and re-audited.
- Captured a read-only host sample (`33,618,251,776` total; `17,192,038,400` available) whose recomputed remaining margin is `2,625,539,968` B, differing from the wire fixture by `39,992,701` B. This confirms practical reachability is not merely a hypothetical alternate snapshot; the sample remains non-authoritative until the contract is repaired.
- The parallel vendor work returned to a compile-safe boundary; RBDFT type-`0x01/0x02` small-profile GREEN tests may resume while ModUp/Trace error and panic matrices continue.
- The RLWE half of the observed runtime seam reached its author gate and is frozen for a fresh read-only audit: `inner_sum.go` `44D329D7...79979`, `trace_observed.go` `018622A0...B543E`, and `trace_observed_test.go` `5E880B1E...D47A4`. Focused count-20, the full core/rlwe package, vet, gofmt and diff checks pass. A bounded race build was attempted but is unavailable under `CGO_ENABLED=0` with no C compiler; ordinary eight-way evaluator-isolation tests remain correctly scoped as non-race evidence.
- The bootstrapping half reached its separate author gate on `evaluator.go` `048E95EF...1E13A`, `modup_observed.go` `AE1B0CDA...56E9C`, and `modup_observed_test.go` `38ED3054...1C38`. Focused count-20, complete bootstrapping/core-rlwe packages, both vet targets, gofmt and diff checks pass; a fresh executor audit is active. It preserves stock `ModUp`, makes ordinary nested Trace errors return the original in-place pointer, normalizes nested or outer panics to nil, and remains small-parameter structural evidence only.
- The first actual fresh Trace audit returned REVISE `0C/1M/2m` on the frozen author hashes. It confirmed stock/core linearization and dismissed the pre-defer concern as unreachable, but required a real minimal N16/L11 four-key success test, direct stock-versus-observed counted key sequences, and rejection of all-complete dispatch-failure reports. The original owner is implementing only those repairs; no combined observed seam is promoted.
- The first actual fresh ModUp audit returned REVISE `0C/1M/0m`: production code and bounded semantics passed, but a real N16/L11 `ModUpObserved` success path is mandatory. Its repair is restricted to a minimal count-one four-key fixture and will run only after the Trace N16 window releases.
- The repaired Trace test's first real N16/L11 count-one run passed with exact lookup/event order `[122881,114689,98305,65537]`; fixture construction took about `79.0 ms`, observed Trace about `31.3 ms`, total allocation about `36.9 MiB`, and measured live-heap delta about `23.5 MiB`. The complete core/rlwe package then passed in `0.651 s`; this is test-resource evidence, not an L11 bootstrap/MR0 benchmark.
- The complete Trace repair candidate is frozen on `inner_sum.go` `44D329D7...79979`, `trace_observed.go` `A5AC3224...4A713`, and `trace_observed_test.go` `C5138544...02229E`. The impossible all-complete dispatch-error/panic ledgers were captured RED then rejected GREEN, and independent stock/observed counters now agree with a literal N4 sequence. A non-author high-reasoning executor is performing the final repair audit.
- Final Trace repair audit: PASS `0C/0M/0m` on the same hashes. The reviewer independently ran the real N16 count-one gate, small focused count-20 excluding it, the remaining core package, vet, formatting and diff checks; no global/callback/sorting/replay/second-dispatch surface exists. The RLWE half is accepted, while the bootstrapping half remains pending its own repaired N16 gate.
- The repaired ModUp candidate keeps production hashes `048E95EF...1E13A`/`AE1B0CDA...56E9C` and updates its test hash to `ABFD4A76...75D4`. Its real Standard N16 Q2/P1/H32 count-one test uses no dense/sparse keys, exactly four Trace keys and a deterministic nonzero ciphertext; stock/observed bytes and metadata agree, with nested `16/11` events in the accepted order. A fresh final re-audit is active.
- RBDFT type-`0x01/0x02` factor streaming reached author gate on production/test hashes `F6F074F4...52C4F` and `0B2C2DC1...B4E1`. Eleven focused tests pass count-20 plus full secureeval/build/vet; no public authority, aggregate/receipt mint, raw constructor, `GenMatrices`, whole-factor serialization or L11 artifact allocation is present. A fresh grammar/move/non-retention audit is active.
- Final ModUp repair audit: PASS `0C/0M/0m` on evaluator/report/test hashes `048E95EF...1E13A`, `AE1B0CDA...56E9C`, and `ABFD4A76...75D4`, with all three accepted Trace dependency hashes matched. The reviewer reran canonical N16 count-one, focused success/error/panic/report/identity cases, complete bootstrapping and core packages, both vet targets, formatting, diff and forbidden-surface scans. The combined vendor-owned Trace/ModUp observation seam is accepted; key/ciphertext/DFT/Route-B authority and MR0 remain open.
- The first RBDFT factor audit returned REVISE `0C/1M/1m` on hashes `F6F074F4...52C4F`/`0B2C2DC1...B4E1`. It independently reproduced the 208-byte type-01 golden and L11 poly sizes and found no production/ownership/authority defect. The repair owner is adding exact ordered five-factor byte expectations and fault injectors that pass the prefix before failing payload flush or physically over-writing one byte.
- The live-authority capacity audit returned REVISE `0C/2M/0m`. Exact arithmetic proved the host sample is admitted but rejected solely by the fixture-frozen remaining/excess fields, and code inspection proved caller-supplied snapshots lack trusted-current provenance. A design repair owner is updating only the two Route-B authority/construction documents: dynamic per-snapshot fields retain the v1 wire shape and fixture golden, while a private Authority sampler/owner/generation capability and use-time resampling replace caller declarations and mint/use byte equality.

### 2026-09-01 Canonical Route-B L11 MR0 gate

- Implemented phase-relative live-capacity plans without changing the strict 80% boundary or the accepted whole-lifecycle `AuthorizeBuild` contract. `BeginBuild`, `AuthorizeReady`, `Install` and preflight now bind exact pre-guard increments of `2,968,063,744`, `872,939,520`, `5,034,213,376` and `981,991,424` bytes. The phase plan/report/permit is reconstructed from every inert runtime snapshot; operation/phase mismatch and plan/peak tampering fail closed.
- The second real lifecycle attempt exposed the defect: after artifact and evaluator installation, preflight added the entire from-zero `7,842,848,076`-byte guarded requirement to already resident objects and blocked before HE. The amendment was registered before any successful MR0 and is frozen at SHA-256 `2132BA29...AAEF51`.
- The next attempt crossed all capacity gates and executed real Trace/C2S, then correctly failed report publication because the implementation compared source-ordered Trace events with the sorted key-set order. The pre-existing observed-Trace design already required `[122881,114689,98305,65537]` for rotations `[2048,4096,8192,16384]`; an independent `5^rotation mod 2N` red test repaired the implementation/test fixture without changing the experiment target.
- The accepted one-process run completed in `34,996,007,900 ns`, with cumulative process peak RSS checkpoints `3,273,887,744` bytes at build, `6,568,656,896` after install and `7,184,281,600` after MR0. The exact DFT constructor delta is `0/0/0/2`, levels are `20 -> 17`, and sparse C2S returns `ctImag=nil`.
- Frozen artifact: `research/reproduction/route_b/route_b_l11_mr0_2026-09-01.json`, SHA-256 `4F963B88...21B28`; report: `research/reproduction/route_b/route_b_l11_mr0_report_2026-09-01.md`. The JSON replay test passes count-20, complete `secureeval`/`secureprofile`, vet and the modified vendor packages pass. Explicit module-wide testing reached the unrelated long homchain suite and timed out at the default 10 minutes without an assertion failure; a longer-timeout rerun remains scheduled.
- `DecodedZeroMaxAbs=0.37500000002891437` is retained as a finite post-ModUp/C2S prefix diagnostic. EvalMod is deliberately absent, so this is not zero preservation, Gao integer correctness, security or performance evidence.

### 2026-09-01 Canonical Route-B L11 first A2B round

- Added a separate `a2b-first-round` live-capacity phase with a registered `1,227,427,584`-byte pre-guard increment and `1,764,298,496`-byte guarded requirement.  The special-b0 plan derives rotations `[1,2,3,2044]`, Galois elements `[5,25,125,89745,131071]`, and `N1=4/4` entirely inside the existing 38-key inventory.
- Composed special-b0, low mask, L18→L16 STC, observed sparse MR0, the exact N16/L11 degree-46/R2 Gao kernel, shared ID/MSB LUT and real recovery behind the installed Route-B authority.  The first wrapper mismatch was repaired by a proven private memory-key view; the strict homchain adapter remains unchanged.
- Two diagnostic attempts exposed one raw C2S scale, `0x1.0000000000000002f2901f1f8cf04128p+43`.  Before success, registered an exact-source/exact-target metadata-only normalization with `abs(log2(source/target))=2.3052091909425391e-19 <= 2^-40`; coefficient and non-scale metadata equality is verified, and arbitrary near values fail closed.
- A later diagnostic attempt exposed an evidence bug: explicit square/rescale states follow `s_next=s_previous^2/Q_level`, not the default-scale literal.  The report now binds the two canonical derived scales, all fourteen states and the normalization evidence.  No failed attempt published a result JSON.
- The accepted fresh lifecycle completed in `39,860,389,200 ns`; cumulative process peak RSS is `3,202,564,096` bytes after build, `6,568,886,272` after install and `9,819,832,320` after the first round.  DFT construction remains `0/0/0/2`.
- All 512 words (`0..255` twice) produce 2,048 decoded ID plus 2,048 decoded MSB values.  Oracle replay reports zero mismatches at tolerance `5e-4`, maxima `9.1463878420219515e-08` (ID), `9.2368428526136832e-08` (MSB), and `8.9380092211797199e-11` (imaginary).
- Frozen artifact: `research/reproduction/route_b/route_b_l11_a2b_first_round_2026-09-01.json`, SHA-256 `2728FC0B...F572A0`; bounded report and count-20 replay test are recorded beside it.  Claim C70 is author-side first-round functional evidence only.  The high-nibble round, complete 8-bit A2B, independent audit, repeated performance, application security and tree integration remain open.

### 2026-09-01 Canonical Route-B L11 complete 8-bit A2B

- Registered the `a2b-full` live-capacity phase before execution. Its exact pre-guard increment is `2,381,123,328 B`; the 512 MiB minimum guard yields `2,917,994,240 B`. The admission sample retained `3,483,401,676 B` below the strict 80% boundary.
- Preserved the installed L18→L16 STC and added a separately observed L3→L1 STC for the second serial round. Its profile is Q3/P6, levels `[1,1]`, diagonal counts `[63,64]`, vector length 2,048 and `5,767,272` serialized bytes per polynomial. The constructor records exactly one additional observed-streaming call and no stock/explicit/raw construction.
- Froze the supplemental STC source, scaling, trace and per-factor identities after the first canonical observation. Factor 0 records `12,971,990 / 363,339,287` numeric/encoded bytes; factor 1 records `21,566,574 / 369,106,575` bytes. The report digest is `BF47F946...52D4A`, and the existing 38-key Route-B inventory is unchanged.
- Executed the 32-boundary serial graph with two MR0 prefixes, two degree-46/shared-LUT kernels, one `ID0/16` multiply-rescale, three level alignments, three subtractions, no residual rotations and two uses of the installed CTS. Both raw CTS scales equal the registered first-round source and are metadata-normalized under the same exact pair and `2^-40` bound.
- The accepted fresh lifecycle completed in `54,297,321,400 ns`; full-circuit wall is `25,830,240,800 ns`. Cumulative process peak RSS is `3,273,900,032 B` after build, `6,569,345,024 B` after install and `12,827,648,000 B` after full A2B. DFT construction is `0/0/0/3`.
- All 512 input words (`0..255` twice) produce 2,048 low plus 2,048 high LSB-first Boolean values. Replay reports zero mismatches at tolerance `5e-4`, with maximum low/high errors `1.3688218578875624e-7 / 8.4026492208622017e-8` and maximum imaginary magnitude `8.5500948344336645e-11`.
- Frozen artifact: `research/reproduction/route_b/route_b_l11_a2b_full_2026-09-01.json`, `455,026 B`, SHA-256 `E97F8148...F785D`; full report digest `764715F2...19D42`. The hash-locked exhaustive replay passes, as do `secureeval`, `secureprofile` and vet. Claim C71 is complete 8-bit A2B functional evidence only; independent audit, application security, B-A2B/remaining ALU operators and tree composition remain open.

### 2026-09-01 Canonical Route-B signed-int8 root tree

- Registered a separate secure-profile root-tree capacity phase before execution. The pre-guard increment is `2,451,902,208 B`; the 512 MiB minimum guard yields `2,988,773,120 B`. The accepted operation sample had `33,618,251,776 B` total physical memory, `13,304,766,464 B` available memory and a `3,592,342,988 B` strict-boundary margin.
- Implemented an immutable public model for an encrypted signed-int8 feature, public signed-int8 threshold and public finite real leaves. A trusted no-overflow interval certificate admits the full encrypted feature range only when `x-threshold` remains in `[-128,127]`; this is an R1 quantized workload and not the CCS float32 comparator.
- Composed CT--PT threshold subtraction, complete Route-B A2B, a fixed four-diagonal rank-one broadcast of high-half column 3 (`b7`), `1-b7`, and one real-leaf delta multiplication. The exact eight-state path is `L20/S43 -> L20/S43 -> L5/S43 -> L4/raw -> L3/S43 -> L3/S43 -> L3/raw -> L2/S43`; the broadcast uses rotations `[1,2]` and Galois elements `[5,25]`, already present in the installed key set.
- The first attempt failed closed before HE because preflight asserted the concrete in-memory keyset instead of the authenticated wrapper interface. The second attempt executed but rejected report publication because the preregistered A2B high-half level was L4 rather than the actual L5 contract. The amendment and ledger were corrected without retagging, and neither failed attempt published JSON.
- The accepted fresh process evaluated two copies of every signed byte with threshold `0` and leaves `-2.25/3.5`: 512 words, 2,048 output slots, 256 left and 256 right words, zero mismatches at `5e-4`, maximum real error `4.1590670063484936e-7`, and maximum imaginary magnitude `6.313179629879332e-10`.
- Total wall time was `56.8712487 s`; the root-tree operation was `27.6964074 s`, including `25.8248727 s` for complete A2B. Cumulative process peak RSS was `3,273,736,192 B` after construction, `6,568,878,080 B` after installation and `13,130,219,520 B` after the tree operation. These are one-run observations, not a comparative benchmark.
- Frozen artifact: `research/reproduction/route_b/route_b_signed8_root_tree_2026-09-01.json`, `267,104 B`, SHA-256 `2410DC16...5FB67C`; report digest `FA0C3563...323FEB`. The exact artifact replay passes count-20 in `32.697 s`; `secureeval`, `secureprofile`, the command package and vet pass. Claim C72 is bounded root-tree functional evidence only. Secure depth-2 orchestration, private thresholds/leaves, CCS float32 equivalence, application security, repeated isolated timing and superiority remain open.

### 2026-09-01 Canonical Route-B signed-int8 depth-2 node batch

- Registered the R1 all-node schedule before encrypted execution: 170 queries pack root, left-child and right-child words adjacently, leaving two padding words. One complete A2B converts all 512 words; Q3 role masks and rotations `+4/+8` align all selectors into root words; three path products and three public-leaf products return L0/S43 outputs.
- A pure artifact test falsified scale-one sparse role masks because inverse-FFT coefficients rounded away. The accepted amendment encodes each role mask at `q3`, physically rescales to L2 and moves the path/leaf schedule to L1/L0. The three serialized role-mask digests are distinct.
- The first two encrypted attempts completed the circuit but failed closed at report validation: reconstructed broadcast scale used Q5 while the frozen transform was encoded at Q4. After binding both ledgers to `S*q4`, the accepted run matched all 30 state boundaries and wrote the canonical file. Failed runs wrote no JSON.
- The canonical run covered all eight branch triples and all four leaves. All 680 active slots and 1,368 inactive slots passed at tolerance `5e-4`; maximum active error is `4.0057496697443185e-7`, maximum inactive magnitude `9.3551663520329e-10`, maximum imaginary magnitude `1.0905832896081732e-9`, and both mismatch counts are zero.
- Total/depth-2/A2B wall times are `58.7611216 / 28.8937126 / 26.8006752 s`. Cumulative build/install/depth-2 peaks are `3,273,629,696 / 6,569,250,816 / 13,206,462,464 B`. These are one-run observations; C72 and C73 have different query semantics and do not define a speedup ratio.
- Frozen artifact: `research/reproduction/route_b/route_b_signed8_depth2_node_batch_2026-09-01.json`, `281,296 B`, SHA-256 `8B99E96E...D85D8`; report digest `54FB7D52...DDB1A`. Hash-locked replay passes count-20 in `33.904 s`; complete `secureeval`/`secureprofile`/command tests and vet pass. Claim C73 is public-model all-node depth-2 functional evidence; selected-child traversal, private model inputs, float32 parity, multi-depth/radix improvement, security and matched performance remain open.

### 2026-09-01 Complete functional selected-child depth-2 orchestration

- Registered the orchestration contract before implementation, then captured a real RED compile failure for the missing `NewSigned8Depth2SelectedChildCircuit` seam. The implemented circuit owns the comparator, root periodic selector, source prefix, fixed-L6 child comparator and terminal decoder/mux in one exact graph.
- The API admits only a feature-indexed vector. It derives root/child feature identifiers, all thresholds and all leaves from the immutable source tree. The root operation is one CT--PT subtraction; the selected child operation is one CT--CT subtraction after encrypted feature/threshold selection. An opaque-root overload is intentionally absent from the source-faithful seam.
- The outer provenance trace binds all three source feature payloads and every typed boundary from comparator result through terminal input. Runtime validation replays nested comparator/selector evidence, prefix operands, the child trace and terminal tuple. Self-consistently resealed linkage and operation-ledger mutations fail.
- A four-word encrypted test covers paths `[00,01,10,11]`, including equality at both child thresholds. It passes the independent `BinaryTree.Evaluate` oracle and defensive-copy/input-immutability gates. The development test completed in `187.970 s`, with `72.727 s` inside the authenticated wrapper.
- The independent CLI rebuilds keys/evaluators and re-encrypts inputs. Its final 16-slot run has zero mismatches at `1e-3`, maximum real error `7.007895456823121e-6`, and maximum imaginary magnitude `4.618971947066847e-6`. Setup/online/post-online-verification/lifecycle wall is `43.0253747 / 70.4440529 / 46.0498067 / 159.5192343 s`; the additive identity is replay-checked.
- The typed module-reported byte sum is `296,178 B`, including overlapping retained boundaries and therefore not communication or unique live memory. The terminal decoder-plus-mux unique composed ledger is `106,662 B`.
- Frozen artifact: `research/reproduction/selected_child/signed8_depth2_selected_child_2026-09-01.json`, `9,047 B`, SHA-256 `4DC5D51F...012B97`; record digest `575F1D21...BF33850`. Hash-locked replay passes count-20 in `0.390 s`; focused admission, encrypted execution, mutation gates and vet pass. Claim C74 is complete source-faithful depth-2 functionality only under N32 `functional_not_secure` parameters. It is not a Route-B selected-child result, application-security result, matched performance row, float32 reproduction or radix improvement.

### 2026-09-01 Canonical Route-B signed-int8 depth-2 selected child

- Registered C75 before implementation and amended its exact terminal scale before encrypted measurement. The final graph uses one complete root A2B, an inverse-root-weighted phase broadcast, supplemental L3→L1 STC plus observed MR0/C2S, a Gao periodic Boolean recovery, exact conditioning to L7/q7, encrypted child feature/public-threshold selection, a low-ingress tree-aware A2Sign child comparison, and an L1/q7 bilinear real-leaf mux.
- A2Sign returns only the second-round sign consumed by comparison. It retains special-`b0`, two masks, two supplemental STCs/MR0s, both Gao rounds and the required `ID0/16` update, while recording the omitted low/high self-removal values as deliberate ablations.
- Five fail-closed diagnostics repaired exact terminal algebra, numeric scale identity, the 16-state A2Sign ledger, the q7 base leaf, and the sign/GE convention. The decisive full diagnostic showed that periodic recovery returns `[x-t<0]`; Amendment B registered exact `1-sign` before the accepted run. No rejected run wrote the canonical JSON.
- The accepted fresh process evaluates 512 queries and all 2,048 output slots. The signed-byte root domain occurs twice; paths `[00,01,10,11]` occur `[127,129,130,126]` times; root, selected-left and selected-right equality counts are `2/2/2`. Mismatch count is zero at `5e-4`, with maximum real/imaginary errors `5.917360885732137e-7 / 3.2190798723753435e-7`.
- Setup/online/postprocess/lifecycle wall is `26.4030390 / 73.7762091 / 0.6222472 / 101.7464627 s`. The selected-child report wall is `69.6711752 s`, including `30.1297464 s` for complete root A2B and `20.9139054 s` for child A2Sign. Build/install/post-evaluation peak RSS is `3,273,859,072 / 6,613,004,288 / 13,889,568,768 B`; DFT construction is `0/0/0/4`.
- The final capacity gate records `2,765,426,432 / 3,302,297,344 B` pre-guard/guarded requirements and a `3,735,764,428 B` remaining strict-boundary margin. The online ledger records zero decryption oracles and zero plaintext branch decisions.
- Frozen artifact: `research/reproduction/route_b/route_b_signed8_depth2_selected_child_2026-09-01.json`, `296,089 B`, SHA-256 `4A7FBD44...ABBB2`; selected-child report digest `93876CD4...36D1D`. The bounded report SHA-256 is `18674F01...11052`, and the verified source/evidence manifest SHA-256 is `38DCAADD...D278`.
- The command-package hash-locked replay, complete `secureeval`/`secureprofile` packages and relevant vet targets pass. The monolithic `homchain` package exceeded both 10- and 30-minute package timeouts without an observed assertion failure, so it is not recorded as a pass; split encrypted-suite execution remains open.
- Claim C75 is author-side public-model signed-int8 selected-child functional evidence on Route B. Private-model security, float32 parity, arbitrary depth, representative forests, repeated performance, radix improvement, independent lifecycle audit and application-security acceptance remain open.

## 5-Question Reboot Check

| Question | Answer |
|---|---|
| Where am I? | Phase 2: Lattigo architecture, raw-state equivalence gates, and production vertical slices. |
| Where am I going? | Primary-source reproduction, Lattigo integer operators, private tree integration, optimization, verification, and delivery. |
| What's the goal? | See `task_plan.md` Goal and Completion Criteria. |
| What have I learned? | See `findings.md`. |
| What have I done? | See this session log. |

## 2026-09-01 Security and radix closure

- Registered and emitted the exact C75 application-security exposure profile.
  Authenticated main/ephemeral estimator evidence, a failing negative control,
  source bindings and the 41-key/124-sample ledger produce an
  assumption-explicit `CONDITIONAL-PASS` profile. Added report and SHA-256
  manifest under `research/reproduction/security/`.
- Registered, implemented and hash-froze C76: a 170-query signed-int8
  same-feature radix-4 interval node plus its exact binary control. Canonical
  artifacts replay every slot and bind the same Route-B parameter/key schedule.
- Added allocation-free serialization counters for input/output ciphertexts,
  the evaluation-key inventory and encoded DFT artifact while keeping legacy
  canonical artifacts replayable.
- Completed the preregistered fixed-order series (one warmup plus seven runs per
  arm). Its 7/7 favorable pairs were retained as descriptive evidence and
  explicitly downgraded because all pairs used AB order.
- Registered an independent order-balanced confirmation before measuring it.
  The eight-pair IQR trigger fired, so the experiment continued without early
  stopping to the registered maximum of 16 pairs. All 32 measured processes
  and both warmups passed zero-mismatch checks.
- Implemented a Go evidence summarizer that validates every source artifact,
  computes type-7 distributions, enforces the extension rule, binds a file
  manifest and evaluates aggregate plus order-stratified paired criteria. Its
  replay test validates both benchmark series in about 77 seconds.
- Final counterbalanced decision: registered direction test `PASS`; candidate
  promotion `NO` because complete-circuit and lifecycle effects remain below
  10% and high-variance. Exact operation/level and terminal-network savings are
  accepted. Added the C76 report and complete artifact hash manifest.

## 2026-09-01 Handoff intake and next-step planning

- Read and verified `C:\Users\26676\AppData\Local\Temp\LCPDTE_handoff_2026-09-01.md` against the active persistent plan at a planning-only stop point.
- Confirmed that no test process is running and that implementation edits, commit, visibility change and push remain deferred.
- The default `python` command still resolves to the Microsoft Store alias; session catch-up succeeded with the Codex bundled Python runtime. This recurrence is already covered by the existing `task_plan.md` error entry.
- Deferred the handoff's suggested `homchain` test restart because the user requested a next-step proposal before execution.
- Refreshed the existing `graphify` lessons and vocabulary for planning. The graph exposes 1,170 label tokens; a prior generic `Ciphertext`/`Scale`/`Tree` selected-child traversal is marked as a dead end, so the continuation query is anchored on verification, tests, evidence, manifests and concrete child/decoder nodes instead.
- Observed a non-blocking tooling warning: the installed graphify package is `0.9.51` while the local skill text reports `0.9.11`; no package or skill update was performed at this planning-only checkpoint.
- The first graph query reached the intended project anchors (`Phase 6 Verification and Innovation Audit`, `Research Evidence Classes`, and `Operator Counts and Wall-Clock Benchmarking`) but was diluted by vendored decoder/test symbols. The continuation analysis therefore narrows to verification/audit/evidence/manifest/report rather than treating vendor adjacency as a project dependency.
- Read-only worktree inspection confirms `main...origin/main [ahead 3]`, seven tracked modified files, a large intentionally untracked implementation/research surface, and no running `go.exe` process. No staging, commit, visibility change or push was performed.
- The narrowed graph traversal confirms that the existing persistent plan, findings and progress ledgers converge on Phase 6 verification/evidence audit; it did not reveal a reason to resume innovation implementation before closing the verification and integrity gates.
- Re-read the persistent task plan for the handoff. Its remaining critical path is unchanged: finish regression/deterministic checks, perform independent Standards/Spec review, reconcile integrity/provenance and manuscript claims, then complete the private remote delivery gate. Broader ALU/radix research arms remain explicitly out of the scoped completion path.
- Re-read the task-plan decision ledger and began the full findings ledger review. The recorded graph-health defects (dangling/collapsed edges) reinforce using Graphify only for navigation and verifying final claims against source, tests and hash-bound artifacts.
- Findings-ledger review confirms the scope boundaries are already explicit: reference reproduction, Lattigo adaptations, Route-B secure-profile evidence and optimization hypotheses are separate evidence classes. Closeout should preserve those labels rather than reopen completed algorithm-design choices.
- Continued the findings review through the guarded comparator/tree and Route-B construction history. Many earlier `REVISE` states were later closed by independent audits, so the integrity pass must use the latest accepted records and retain superseded diagnostics only as history; it must not flatten them into current failures or current PASS claims.
- Completed the findings-ledger review and began the full progress-ledger replay. The historical Stage-1 passport/26-file freeze predates the later experiment-backed Route-B evidence; this confirms provenance reconciliation and a new current manifest belong after regression/review repairs, not before them.
- Progress replay confirms that accepted component audits and focused count gates are extensive, but they do not substitute for the still-open complete `homchain` coverage accounting. The next execution phase should close test-name coverage first, then use the dual code review to drive only targeted reruns/repairs.
- The progress/test tables distinguish historical failures, repaired gates and current scope labels in detail. For closeout, the actionable regression gap is the timeout-split remainder plus coverage union; historical RED/REVISE entries should remain audit trail, not be rerun wholesale.
- Full progress-ledger replay is complete. Its embedded “5-Question Reboot Check” still says Phase 2 even though `task_plan.md` and the latest evidence place the project in Phase 6 / ARS Stage 2.5; the final integrity synchronization should correct this stale status together with the manuscript/passport drift.
- Rechecked the closeout state surfaces. The C76 report consistently records a structural/terminal win but no ≥10% promotion; `material_passport.yaml` still declares `no_experiments_declared`; and an independent replay of `research/stage1_artifact_hashes.sha256` finds exactly 26 checked, 4 mismatches, 0 missing—the same four intentional Stage-2 supersessions listed in the handoff.
- Manuscript-state inspection identifies concrete integrity edits for the later prose pass: several reader surfaces still say the Stage-1 manifest currently matches 26/26, and `monitor_state.json` retains superseded fixed-order C76 figures (`87.19%` terminal and about `0.95%` end-to-end) instead of the counterbalanced `86.85%`, `3.94%` and `1.25%` values. These are synchronization targets, not reasons to rerun C76.
- Loaded the ARS router for the closeout plan. It routes staged integrity/review/finalization through `academic-pipeline`; prose engines are deferred until the later manuscript-revision phase, so no Nature-language pass is started during this planning checkpoint.
- Loaded the dual-axis `code-review` contract. Its review must keep Standards and Spec findings independent and pin a resolvable fixed point. Because most project work is still untracked/uncommitted, review preflight must also ensure the candidate snapshot—not only `HEAD`—is inside the reviewed diff before any PASS can count.
- The ARS pipeline workflow confirms Stage 2.5 is a mandatory integrity boundary: it must produce an integrity report, repair/recheck failures (up to three rounds), and stop for explicit confirmation before advancing. This gate precedes manuscript peer-review/finalization, while software Standards/Spec review remains an additional repository closeout gate.
- ARS Stage 2.5 requires five integrity phases (references, citation context, statistical/data claims, originality, and claims) plus the blocking seven-mode AI research failure checklist. The closeout plan therefore separates software regression/review from the manuscript integrity verdict and does not treat green Go tests as an ARS integrity PASS.
- The integrity-agent contract additionally requires item-by-item external existence/metadata verification for every reference and an audit trail; prior Stage-1 bibliography work can be reused as evidence, but current Stage-2.5 cannot inherit a 100% PASS merely from the historical checkpoint.
- Completed the integrity-agent and mandatory failure-mode protocol review. The passport contradiction is a blocking declaration-anchored provenance failure under ARS C4: `no_experiments_declared` cannot coexist with experiment-backed manuscript/results claims.
- Stage 2.5 must also rule all seven research failure modes explicitly. Modes 1, 3, 5 and 6 cannot be left as `INSUFFICIENT EVIDENCE`; saved commands, exit states, raw benchmark artifacts and code-to-claim bindings are therefore closeout inputs, not optional appendices.
- The staged plan will preserve the historical Stage-1 hash file as an immutable checkpoint and explain its four current mismatches as later supersessions. A fresh current manifest is generated only after integrity and review repairs stabilize the candidate.

## 2026-09-01 workspace consolidation and library productization

- Loaded the implementation, TDD, codebase-design, Graphify, planning and proportionality contracts for the new acceptance-library request.
- Ran session catch-up and fetched/pruned all refs. Branch inventory is now definitive: only local `main`, remote `origin/main`, and one worktree exist, so the requested branch merge/delete phase is complete without mutations.
- Confirmed `main` is three commits ahead of `origin/main` and holds seven tracked modifications plus the large untracked integer CKKS, Route-B, tree, command and research surface. No files have been discarded, staged, committed or pushed yet.
- Refreshed Graphify lessons and queried the existing graph with exact vocabulary for integer CKKS library/evaluator seams. The result was vendor-heavy, so source/test inspection is the next authority.
- Initial `go doc dt_go/integer/...` invocations returned no useful output; switching to relative package arguments (`go doc ./integer/...`) resolved package documentation inspection.
- Public-surface inspection confirms that `integer/evaluator` covers encrypted arithmetic values but explicitly excludes refreshing/A2B, while `homchain` and `secureeval` expose hundreds of research-construction types. Productization therefore needs a deep entry module, not aliases for every implementation symbol.
- Inspected the exact Lattigo v6.1.1 example sources from the local module cache and the official repository. The planned acceptance examples follow its applications/templates/tutorials split and self-contained `main.go` style.
- Source fixtures prove the functional 8-bit chain can be constructed without exposing research setup: one constructor can create the `LogN=5` parameter chain, 192/256-bit encoders, required relinearization/Galois keys and custom bootstrap evaluator, then bind A2B/B2A/comparator circuits internally.
- Grouped every untracked file by top-level ownership and checked for oversized payloads. The source/evidence set is suitable for a normal commit; Graphify session state should be ignored, while the abandoned Sage temporary tree should be removed after exact-path validation.
- Exact-path recursive deletion and a recoverable directory move were both rejected by the host command policy before execution; all four ignored `tmp` directories remain unchanged. They do not affect normal Git cleanliness and will not be bypassed with another shell.
- Changed `.gitignore` so all rebuildable `graphify-out` reports, caches, query memory and rendered graphs stay local instead of entering the library consolidation commit.
- Baseline fast-suite PASS: `z2n`, arithmetic evaluator, treeplan/treecompile/treeeval, securityparams/secureprofile, secureeval and every command package passed with `-count=1`; the benchmark summarizer completed in 83.462 s. The oversized `homchain` suite remains reserved for split execution.
- Committed the complete pre-productization implementation/evidence baseline on `main` as `6ecb505` (`chore: consolidate integer CKKS research implementation`). No remote push was performed.
- Replaced the placeholder module path `dt_go` with `github.com/nc26676027/LCPDTE` and mechanically rewrote all Go imports. The fast `z2n`, evaluator and tree package group passed after the rename.
- Added the first external-package TDD seam for `ckksint.Context`. RED was the expected missing package; GREEN now covers modular add/subtract/full multiply and rejects values from an independently keyed context.
- The first Context implementation tried to forward a two-result evaluator call together with an operation label, which Go rejects; split each call into explicit value/error assignments. A second RED exposed Go's permitted coalescing of pointers to zero-sized context tokens; the ownership token now has non-zero size and the cross-context gate passes.
- Added the `Functional8` public seam and proved it from the external `ckksint_test` package. A2B -> B2A, all eight decrypted bits, signed `>=`, selectors and the source-faithful depth-2 tree all pass without importing `integer/homchain`; the complete depth-2 public API test passed in 133.68 s.
- Added self-contained Lattigo-style programs under `examples/ckksint/{basic,conversion,depth2}` plus package/root documentation. Both basic and conversion programs were executed successfully; all example packages compile, and the depth-2 path is covered by the equivalent public-package test.
- Updated the module README to identify `github.com/nc26676027/LCPDTE/ckksint`, the actual Go directive, repository-root experiment commands, and the explicit `DemoOnly` boundary.
- The ignored historical `tmp` tree contains an inaccessible Conda directory and caused `go list ./...` to fail before package enumeration. Physical deletion and quarantine were already policy-blocked, so a local ignored `tmp/go.mod` now creates a nested-module traversal boundary; `go list ./...` and `go vet ./...` both pass.
- Re-ran every non-`homchain` package after the canonical module rename. All packages passed; the benchmark summarizer took 80.873 s, `secureeval` 47.666 s and the arithmetic evaluator 23.536 s.
- Enumerated 181 `homchain` tests and partitioned them with mutually exclusive rules whose union is exactly 181/181. All shards passed: terminal public (709.90 s), terminal opaque (89.48 s), terminal remainder (245.29 s), selected child (223.77 s), child/selector/source-prefix (1014.42 s), root/comparator (575.83 s), A2B (93.25 s), A2A (1.50 s), B2A (1.55 s), and remaining transforms/sine/finalizers (49.03 s).
- Final new-library suite PASS: `go test -count=1 -timeout=15m -v ./ckksint` in 136.648 s. Final static analysis PASS: `go vet ./...`.
- Committed the initial library candidate as `1c33890` (`feat: package integer CKKS as a public library`) and ran the required independent Standards/Spec review against baseline `6ecb505`.
- Spec review confirmed branch state, public coverage, examples and no-push status. It identified the still-present policy-blocked `tmp` directories and missing Route-B status in the library documentation; the plan now leaves physical deletion unchecked and `ckksint/README.md` records that Route-B is outside the facade and remains `CONDITIONAL-PASS`.
- Standards review identified three facade-specific issues. The Lattigo import escape hatch is now named and documented as `ImportTrustedCiphertext`; public semantic enums are owned by `ckksint` rather than aliased from research packages; and negative tests cover cross-engine values, certified feature/threshold ranges and rejection of B2A output as fresh ingress.
- Post-review verification PASS: the complete external-package `ckksint` suite, including the full selected-child depth-2 path, passed in 138.377 s; all Lattigo-style example packages compile; and `go vet ./...` passed again.

## 2026-09-04 Gao/OpenFHE performance acceptance

### Phase-10 TDD repair started

- **Status:** in progress
- Loaded the TDD, implementation, Graphify, file-planning and proportionality contracts; the public test seams are the reusable prepared evaluator and the benchmark artifact/parser.
- Planning catch-up attempt 1 failed because Windows `python` resolves to the Microsoft Store alias. Attempt 2 used Graphify's recorded Python interpreter and completed with no unsynchronized-session report.
- Recorded the two confirmed defects before implementation: Route-B online time contains construction/precomputation, and the previous comparison uses incompatible warmup/repeat and packing protocols.
- Refreshed Graphify lessons, expanded the query only with graph vocabulary, and traced the evaluator/bootstrap/benchmark/packing neighborhood. Direct source inspection remains authoritative because the traversal is broad and vendor-heavy.
- Inspected Route-B full-A2B, installed lifecycle, public depth-2 wrapper and benchmark comparator. No production source has been edited yet; the next action is the first RED public-behavior test.
- TDD slice 1 RED: `TestComparisonRejectsDifferentPacking` failed because the comparator returned success for 8,192 versus 512 lanes. GREEN: `Compare` now rejects the mismatch; the focused test passes. The superseded v1 success test will be converted only after a matched fixture exists.
- TDD slice 2 RED/GREEN: a packing-matched pair with OpenFHE `1/5` and Lattigo `0/1` was accepted; `Compare` now rejects timing-protocol mismatches and the focused test passes.
- TDD slice 3 RED was a compile failure because `Measurement` had no timing-scope contract. The minimal GREEN adds `TimingScopePreparedOnline` and rejects any ratio unless both measurements declare it; the focused test passes.
- Public prepared-A2B E2E attempt 1 completed the first conversion, then correctly stopped before the second HE dispatch because transient allocations left process use at 26.19 GiB and the existing first-operation capacity estimate would cross the 80% gate. The operational path now releases transient heap before its fresh capacity sample; this is outside prepared-online timing.

- User narrowed production acceptance to Gao/OpenFHE performance comparison and an end-to-end usable library example; malicious-security, side-channel and unrelated hardening tracks are outside this implementation phase.
- Updated `CONTEXT.md` so `Production Acceptance` means matched Gao performance evidence plus an end-to-end workflow, and `End-to-End Example` names setup, encryption, server evaluation, decryption and verification through the public library boundary.
- Queried the existing project graph with `gao, performance, comparison, example, ckks, integer, operator, evaluation, full, source, depth, tree`, then confirmed all benchmark facts against the pinned upstream source.
- Confirmed the Gao entry point, `zN=8` workload, five-repeat timing macro, full-packing lane count and committed source log. Confirmed that current Route-B commands already measure complete A2B and depth-2 lifecycles but are not yet exposed as a `ckksint` end-to-end example.
- Implemented and independently reran `internal/benchcmp` plus `cmd/compare-ckksint`. Focused tests and vet pass; the real-artifact invocation reports both call latency and normalized throughput, explicitly records the 8,192-versus-512 lane mismatch, and rejects correctness-failure artifacts.
- Implemented the split Route-B session in `integer/secureeval`, wrapped it with public-only `ckksint` types, and added `examples/ckksint/routeb_depth2`. Fast public-API tests, example compilation and affected-package vet pass; the real N=2^16 example remains queued behind the OpenFHE build so both memory-heavy acceptance runs execute serially.
- Independent benchmark review found two evidence-admission defects before merge: mismatched word widths/ring dimensions were only labelled instead of rejected, and a truncated Gao log could omit the correctness warmup marker while still claiming it. Both are assigned to a focused TDD fix; lane-count mismatch remains an allowed and explicitly reported packing difference.
- Closed both benchmark findings with TDD and reran the valid artifact pair. Independent Route-B API review then found two P2 acceptance gaps: only the first of four replicated output slots was checked, and padded throughput had one ambiguous label. Decryption now validates every requested replica and imaginary component, while the example prints effective-query/s and packed-query/s separately; focused tests and vet pass.
- Planning catch-up attempt 1 failed because Windows `python` resolves to the Microsoft Store alias; attempt 2 failed because `py` is not installed. The third, different path used Graphify's recorded Python interpreter and completed with no unsynchronized-session report.
- Delegated three non-overlapping implementation tracks: a Gao/Lattigo comparison parser/normalizer, a fixed-profile split client/server Route-B facade with public example, and a README-faithful WSL build/run of the pinned OpenFHE benchmark in an ignored build directory.
- Confirmed the host/WSL capacity and that Go can be run inside WSL, enabling a same-hardware comparison attempt after both executables are ready.
- Built the pinned Gao/OpenFHE commit with Release Clang 14 plus HEXL/NTL/OpenMP/tcmalloc, passed the complete verify mode, then completed the single-thread benchmark with exit 0 and no correctness error. Local complete A2B is `19.5693 s` for 8,192 words; the full process peaked at `24,058,188 KiB` RSS.
- Ran Lattigo Route-B in the same Ubuntu WSL environment with `GOMAXPROCS=1`. Complete A2B is `32.693327852 s` for 512 words with zero mismatches and `12.11 GiB` post-A2B peak RSS. The stable same-host comparison reports a `1.670644x` OpenFHE call-latency advantage and `26.730299x` effective-throughput advantage.
- Executed the exact public example command on Windows. It returned all four expected leaves, maximum error `2.88e-7`, zero mismatches and a non-empty trace in `106.183339 s`; measured evaluation was `68.5201695 s` and sampled peak working set was `13.700573 GiB`. Exact stdout and metrics are checked into the reproduction directory.
- Final affected verification is green: complete `ckksint` tests passed in `141.503 s`; complete `integer/secureeval` tests passed in `46.283 s`; the benchmark command, parser and all `examples/ckksint` packages passed; scoped vet and full-repository `go vet ./...` passed; `git diff --check` reports no whitespace error.
- First independent Phase-9 review returned one external-consumer P1, one benchmark-admission P1 and one shared reproducibility P2. A fresh module could not compile against official unpatched Lattigo; forged `input_words` could inflate throughput; and the same-host comparison referenced an ignored raw Lattigo JSON.
- Moved the complete 143-file patched Lattigo v6.1.1 subset plus LICENSE/NOTICE from dependency-only `vendor` placement into the main module's `lattigo/` tree, mechanically rewrote all 737 Go imports, removed the official Lattigo module requirement and regenerated ordinary third-party vendor metadata. A temporary downstream module now imports `ckksint` successfully with vendor mode disabled.
- Added five canonical-shape rejection tests and strict Route-B parser gates, checked in the byte-identical 455,289-byte local Lattigo envelope, regenerated same-host provenance from tracked inputs and documented the exact OpenFHE/Lattigo versions, build switches, machine and replay commands.
- Post-fix verification is green: complete `ckksint` tests passed in `203.987 s`; complete `integer/secureeval` tests passed in `61.212 s`; the focused external-module test, benchmark/parser/CLI, all examples, all embedded Lattigo patch tests, module-mode compilation and corresponding vet gates passed.
- Final review tightened benchmark admission from selected JSON fields to the complete `RouteBCanonicalL11A2BFullResult`: the parser now requires a real completion timestamp and explicit mismatch ledger, rejects unknown/trailing or truncated input, and calls `Validate()` to replay all 4,096 decoded slots, the plaintext oracle, digests, lifecycle linkage and error maxima. Tests cover truncated summaries, missing fields, zero completion time, trailing JSON and every canonical-shape mutation.
- Corrected the resource column to `Whole-command peak RSS` and recorded that the OpenFHE and Lattigo command scopes differ, so those two peaks are not an operator-level memory ratio. The latency and packed-throughput comparison remains unchanged and both source-artifact and same-host summaries replay byte-identically from tracked inputs.
- Closed the Apache-2.0 derivative notice gate by adding a prominent modification notice to all 137 embedded Lattigo Go files and recording the v6.1.1 origin and modification categories in `lattigo/NOTICE`; `go mod tidy -diff` and `go mod verify` are clean.
- Production acceptance for the frozen Phase-9 scope is complete. Final Standards and Spec reviews both returned PASS with no P0-P2 findings; focused benchmark/CLI tests and vet pass, every module package compiles with `-mod=readonly`, full-repository vet passes, and the accepted implementation is committed on `main` as `d23e93a`, `415f26f`, and `b3733a4` without a remote push.

### Phase-10 prepared A2B repair

- TDD RED/GREEN completed for a public reusable Route-B A2B session, prepared-online timing metadata, five raw samples, strict matched-artifact admission, and a steady-state operation that consumes sealed resident state without repeating full artifact validation.
- Added `NewCanonicalRouteBA2B` with opaque session-bound input/output types and a Lattigo-style `examples/ckksint/routeb_a2b` program covering setup, encryption, reusable evaluation, decryption, byte reconstruction, timings and zero-mismatch failure handling.
- Added `benchmark-ckksint-a2b`, canonical benchmark schema v2, strict `compare-ckksint` admission, a focused 512-word Gao sparse driver, and a canonical 8,192-word Gao full driver. Both OpenFHE drivers compile against pinned commit `08f1eb8` in Release/HEXL/native mode.
- Real public A2B reuse passed twice with nonzero first-call preparation, zero second-call preparation and every requested bit correct. The initial second-call capacity failure was fixed by releasing transient Go heap before the existing fresh capacity sample.
- Performance profiling showed that in-timer copies and evidence checks are under `1%`; the dominant online costs are DFT/ModRaise and polynomial kernels. Removed the measured steady-state over-defense outside the timer: repeated 2.64-GiB resident hashing, duplicate full-report validation, capacity resampling, and per-request `FreeOSMemory`.
- Final-code Lattigo benchmark passed 1 warmup + 5 verified calls at `25.2243475468 s` mean for 512 words and zero mismatches, improving `22.85%` over the previous cold one-shot result. Whole command exited 0 and peaked at `15,097,008 KiB` RSS.
- Gao/OpenFHE canonical full passed 1 warmup + 5 verified calls at `22.0393063512 s` mean for 8,192 words and zero mismatches. Whole command exited 0 and peaked at `24,067,576 KiB` RSS.
- The matched 512-word Gao sparse candidate passed its independent 4,096-bit codec self-check but failed the pinned upstream A2B warmup with 2,107 mismatches, exited before timing and emitted no artifact. No matched ratio is published; the comparator rejects the two valid native artifacts because their packing identities differ.
- Raw artifacts, resource records, commands, environment and interpretation are recorded under `research/reproduction/benchmarks/gao_openfhe_vs_lattigo_prepared_2026-09-04`.
- Final verification passed on the accepted tree: affected Go packages, benchmark/comparison commands and examples pass; `go vet ./...`, full module compile-only traversal, module integrity checks, Python artifact validators, shell syntax checks, checksum replay and `git diff --check` all pass. The historical `homchain` suite was not rerun monolithically because that attempt again entered its known long-running path; its exhaustive 181/181 sharded result remains the applicable unchanged-package evidence.
- Final independent Standards and Spec reviews both report PASS with no P0--P2 or P3 findings. The OpenFHE source copy used by the build helpers is generated only inside the ignored clean-source build tree; the pinned upstream checkout has no project-driver residue.
- Phase 10 is complete and committed locally on `main`. No remote push was performed.

### Phase-11 full-packed parity opened

- The acceptance target is now strict same-host parity with Gao/OpenFHE rather than a descriptive native-packing comparison: both sides must use `N=65536`, `zN=8`, `w=4`, 32,768 complex slots, 8,192 useful bytes, one warmup, five verified samples and one execution thread.
- The fast red-capable loop is `go run ./cmd/compare-ckksint -openfhe-json .../openfhe-full.json -lattigo-json .../lattigo.json`. It deterministically exits 1 with `packing_slots mismatch: OpenFHE=32768 Lattigo=2048`, reproducing the first exact acceptance failure in 3.2 seconds.
- Final performance evidence must rerun the pinned OpenFHE executable on this machine immediately before Lattigo. The pass gate is the raw-sample mean, `Lattigo <= OpenFHE`, after both artifacts pass full correctness and identity validation.

### Phase-11 full-packed implementation and TDD repair

- Added the public `ckksint.NewGaoFullPackedA2B` client/server API and the executable `examples/ckksint/gao_full_a2b`. One ciphertext carries 8,192 caller bytes across all 32,768 complex slots; public-key encryption, opaque session-bound inputs/outputs, low4/high4 ciphertexts, decryption and complete 65,536-bit verification are inside the supported workflow. Short batches are zero-filled rather than repeated.
- Repaired the OpenFHE-consumed-depth versus Lattigo-remaining-level translation. The live graph now follows L20 input, L4 special-B0 ingress, L3 halves, L2 masked STC ingress, L0 STC, L20 ModUp, L17 C2S and L5 kernel output; nonzero encrypted round-trip tests cover L20-to-L4 and the fused `ID0/16` L5-to-L3 adjustment.
- Replaced timed factor-file reads/deserialization/revalidation with resident, prevalidated CTS/STC sources. Setup loads each factor exactly once, verifies it, removes its temporary backing store and exposes only in-memory reads to every warmup/timed evaluation.
- CPU profiles identified DFT and polynomial evaluation as the remaining online cost. The accepted live-output graph uses real-only C2S, fuses `ID0/16` scaling with level adjustment, skips first/final self-removal values that cannot affect the returned low4/high4 outputs, and evaluates only the live MSB polynomial in round two. A measured BSGS-ratio-3 trial increased rotations/residency and was reverted to ratio 2.
- Canonical-v3 artifacts bind the shared Gao contract and complete backend-native parameter evidence: ordered Q/P moduli, individual and aggregate bit lengths, actual first Q, main/ephemeral secret distributions, error sampler, key-switch decomposition, and security selector/evidence. The comparator requires the same host/OS/architecture and exact full-packed output container; it passes only when both mean and median Lattigo/OpenFHE ratios are at most 1.
- Optimized full E2E passed in WSL with all 8,192 words and 65,536 bits correct. The test took `134.97 s` (`136.029 s` package time), peaked at `16,054,192 KiB` RSS, used no swap, and improved complete setup-plus-one-evaluation wall by about 19% from the earlier `166.72 s` diagnostic.
- The monolithic historical `homchain` command again exceeded its 10-minute package timeout while running unrelated encrypted depth-2 tests. It also exposed one stale source-inspection path left from the embedded-Lattigo relocation; the path now targets `lattigo/`, and the affected Gao kernel/source tests pass in `11.031 s`.
- The first strict full-packed comparison was a performance RED, not a correctness failure: OpenFHE samples were `24.031383242`, `23.668598105`, `22.153038363`, `23.642148594`, `22.353221169` seconds; Lattigo samples were `24.429328146`, `24.952130022`, `23.596116065`, `23.468847153`, `23.120016030` seconds. Both had zero mismatches. Median ratio passed at `0.998053`, while mean ratio failed at `1.032094`.
- A TDD diagnostic tested whether the explicit full-heap `runtime.GC()` after warmup caused the first-run tail. Removing it worsened samples to `25.622146957`, `28.425220433`, `25.028029141`, `24.589791539`, `24.086690233` seconds, raised mean from `23.9132874832` to `25.5503756606 s`, and raised peak RSS from `18,541,300` to `19,988,064 KiB`. The hypothesis is rejected and the post-warmup GC is restored. The strict gate remains unchanged.
- A 22-GiB memory-limit diagnostic was also rejected after a severe fifth-sample stall; the accepted benchmark policy stays at `GOMEMLIMIT=20GiB`, `GOGC=100`, and explicit post-warmup GC. An earlier v4 run is excluded because it used old source under concurrent load.
- TDD repair removed both identity mask multiplies/rescales and combined the first-round ID/MSB LUT into one Hermitian complex-channel evaluation. Unit tests bind the exact 16-root mapping, reject packed-LUT tampering, verify encrypted value/scale preservation across identity-mask level drops, and freeze the new trace graph.
- The optimized full-packed public E2E passed every one of 65,536 output bits in `133.70 s`; `/usr/bin/time` reports `16,360,848 KiB` maximum RSS and no swap.
- Submission tests are green after the v3 parameter pass: complete `integer/secureeval` (`55.282 s`), complete public `ckksint` including the encrypted 8,192-word E2E (`153.614 s`), benchmark/comparator packages, repository-wide vet and compile-only traversal, 15 Python validator tests, both Bash scripts, module tidy/verification, and `git diff --check`.
- A pre-benchmark independent review caught and closed four artifact-integrity gaps: the OpenFHE stamp now binds both driver and binary SHA-256, the pinned checkout gate includes untracked files, required null/zero fields cannot disappear during JSON decoding, and setup timing includes client encoder/session construction. The full-packed Lattigo artifact accurately reports `full-packed-profile-not-assessed`; the separate C75 selected-child `CONDITIONAL-PASS` is not reused.
- The final same-host serial replay passes. OpenFHE samples are `26.309255459`, `24.602206521`, `24.941294921`, `23.132755381`, and `22.342342028` seconds (mean `24.265570862`, median `24.602206521`); Lattigo samples are `23.373264006`, `24.798070640`, `23.192962832`, `23.427420347`, and `23.273868112` seconds (mean `23.6131171874`, median `23.373264006`). Both sides verify all 65,536 bits in every run with zero mismatches.
- The strict comparison passes: Lattigo/OpenFHE mean ratio `0.973112`, median ratio `0.950047`, and effective-throughput ratio `1.027631`. Raw v3 artifacts, time/RSS logs, build hashes, environment and replay commands are preserved under `research/reproduction/benchmarks/gao_openfhe_vs_lattigo_full_2026-09-05`.

### Phase-11 exact-parameter lifecycle acceptance

- Replaced aggregate-only parameter admission with exact ordered OpenFHE Q/P equality plus matched numerical fields. Source `cda2a24` now binds the generated first Q, secret weights, error sigma/effective bound, and key-switch decomposition while preserving backend-native mechanism, sampler, security-evidence, scale-schedule, and DFT-planner identities.
- Diagnosed the later-sample latency tail as an output-lifecycle mismatch: the Go harness retained transient outputs/checkpoint reachability after verification, whereas OpenFHE destroyed its result outside the timed call. The TDD repair consumes those transient results and runs inter-sample GC after verification and outside the timer; it does not remove A2B computation-graph work.
- Completed the required local serial replay on the same host, running OpenFHE immediately before Lattigo with one thread, one verified warmup and five verified timed samples. OpenFHE nanosecond samples are `22906563026`, `22213842741`, `22467630012`, `23326414391`, and `23530424238`; Lattigo samples are `23355570348`, `23049589430`, `22892136729`, `22500004924`, and `22362395515`. Both artifacts report zero mismatches across every 65,536-bit result.
- Final acceptance passes: mean latency ratio `0.9975081674607521`, median latency ratio `0.9993702111930268`, and effective-throughput ratio `1.0024980572796622`. This final exact-parameter lifecycle run supersedes the earlier passing aggregate-aligned artifact for acceptance while retaining it as historical diagnostic evidence.
